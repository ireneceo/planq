# 할 일 총정리 — 2026-08-21

운영 미완료 피드백 **85건** 기준(운영 DB 실측). 전날 목록(`WORK_TODO_2026-08-20.md`)을 오늘 작업분으로 갱신.

분류 기준은 전날과 같다 — **무엇을 만들지가 이미 정해졌는가.**

- **A군** — 요구가 명확해 바로 구현. Opus 가 설계·구현·검증까지.
- **B군** — 무엇을 만들지부터 정해야 한다. 설계 선행.
- **C군** — 설계가 이미 있거나, **Irene 결정·조치만 남았다.**

> 이번 사이클은 Irene 지시로 **Fable 미사용**. 전부 Opus 자체 검증이며,
> 나중에 Fable 이 검증할 수 있도록 `docs/FABLE_VERIFY_QUEUE.md` 에 인수인계를 남겼다.

---

## 0. 오늘 처리 — 배포 완료 (장부에서 닫음)

| # | 내용 |
|---|---|
| #304 | 코드박스가 여러 개로 쪼개지던 것 |
| #337 | 엑셀 복사가 이미지로 들어가던 것 |
| #341 | 임시저장본과 저장본이 따로 남던 것 |
| #348 | 정기업무 회차가 워크스트림·태그·기간을 상속 못 받던 것 (+운영 백필 3건) |
| #350 | 자정에 회차 알림이 4~6건 연속 발송되던 것 |
| #351 | 밀린 회차가 하루 1건씩만 생성돼 밀림이 고정되던 것 |
| #357 | **운영 백엔드가 하루 12회 크래시·재시작하던 것** (신고는 "로그 노이즈" 였다) |
| #365 | Q docs 영상 첨부 시 저장 실패 + 첨부가 편집 모드에서 통째로 유실되던 것 |
| #366 | 검색결과에 완료 업무가 섞이던 것 |

**신고에 없던 것도 같이 잡음** — 문서 자동저장이 2초마다 자기를 재예약하던 루프 ·
편집 중 새 빌드 감지로 페이지가 새로고침돼 편집이 닫히던 것(문서·메모) ·
채팅 만들 때 고객이 참여자로 안 들어가던 것 · @목록이 대화 참여자가 아닌 워크스페이스 전원이던 것 ·
방 이름이 프로젝트명 prefix 만큼 잘리던 것.

## 0-B. 오늘 처리 — **운영 미반영** (배포 대기)

커밋은 됐고 dev 반영·검증 완료. **운영은 아직 옛 코드다.**

| # | 내용 |
|---|---|
| #316 #319 #320 #321 #322 | Q info AI·CSV 가져오기 — 항목 생성 · 자유 헤더 · 카테고리 · 중복검사 · 잘림 고지 |
| #323 #325 #326 #327 #328 #329 #330 #331 #332 #333 | Q info 항목 화면 — 줄바꿈 손실(데이터 손실) · 값 잘림 · 시크릿 보기/복사 · URL 링크 · 카테고리 칩 · 번역키 노출 · 리스트표시 토글 · 인덱싱 상태 거짓표시 · 항목만 등록 |
| #334 | 통합검색이 비밀번호·API 키 값까지 훑던 것 |
| #310 | AI 문서 생성 시 고른 프로젝트가 안 붙던 것 |
| #336 | 새 표가 편집 화면으로 안 열리던 것 (커밋 타이밍 경쟁) |
| — | 메일 **전달 시 본문이 사라지던 것** (base64 이미지 자리표시자) |
| — | 메일 검색 **관련도 정렬** (제목·보낸사람 우선) |
| — | **띄어쓰기 무시 검색** 양방향 (메일·통합) |
| — | 통합검색 **와일드카드 방어** (`%` 하나로 전건 매칭되던 것) |
| — | **고객 검색이 처음부터 죽어 있던 것** (`Client.email` 유령 컬럼 — 신규 발견) |
| — | **문서 편집 권한을 공개 범위에 맞춤** (볼 수 있으면 고칠 수 있다) |
| #363 | 문서 에디터 정렬 (좌/가운데/우) — **"표 좌우 폭" 은 #311 로 남음** |
| #362 | SNS 링크 미리보기 OG 서버 렌더 — **nginx 적용 필요(C군)** |
| #359 | 오등록 유발 주석·문구 정리 — **네이밍 결정은 C군** |

---

## A군 — 바로 구현 (설계 불필요)

### A-1. 입력 유실 방지 (2건 — 같은 뿌리, 묶어서)
| # | 내용 |
|---|---|
| #222 | 새 메일 폼에서 리스트 클릭 시 이탈 불가 — 자동저장 + 재열기 시 복원(유저 단위) |
| #367 | 댓글·채팅 쓰다가 다른 곳 가면 사라짐 — 초안 보존 |

> Q docs 자동저장에서 이미 겪은 문제다(#341 · 자동저장 루프). 같은 규칙을 메일·댓글·채팅에 이식한다.

### A-2. 업무·그래프 (4건)
| # | 내용 |
|---|---|
| #254 #300 | 리스트 실제시간과 주간 진척 그래프 숫자 불일치 — **같은 값의 공식이 여러 벌** 계열. 정본 통합이 선행 |
| #255 | 나의 업무보고에 진척 그래프가 안 나옴 |
| #302 | 요청받은 업무에서 외부컨펌 버튼이 비활성 |

### A-3. Q mail (7건)
| # | 내용 |
|---|---|
| #213 | 필터 접기/펴기 + 기본 접힘 |
| #260 | 메일 상세 풀버전·팝업 보기 |
| #263 | 업무추출 버튼 누락 · AI 담당자가 "1, 2" 로 표시 · 불필요하게 긴 AI 문구 |
| #308 | 모두읽음 버튼만 큼 — 필터와 같은 크기·회색 |
| #314 | 다른 탭에서 "답변필요" 로 보내기 |
| #324 | 보낸메일 탭 표시이름이 다름 |
| #220(일부) | 팀메일에서 누가 보냈는지 표시 |

### A-4. 문서·에디터 (6건)
| # | 내용 |
|---|---|
| #311 | 표 열 너비 고정 옵션 (#363 의 "표 좌우 폭" 포함) |
| #312 | AI "다시 만들기" 가 기존 요청·수정 내역을 리셋 — 전 AI 작성 공통 |
| #338 | 목차 기능 (노션식 상단 목차) |
| #342 | 노션 등에서 복사 시 이미지가 미리보기로 안 붙음 |
| #257 | 문서 리스트가 열람만 해도 최신으로 올라옴(수정순이어야) + 제목 2줄 |
| #339 | 프로젝트 설명이 설정에 있음 — 상세정보로 이동하거나 입력칸 확대 |

### A-5. 화면 공통 (5건)
| # | 내용 |
|---|---|
| #232 | 첨부를 드래그드롭 회색 라운드박스로 **전면 통일** |
| #287 | 알림 아이콘이 종류와 안 맞음 — 메일/채팅/기타 구분 |
| #306 | 새소식이 우측 패널로 열림 → 알림처럼 아래로 + 모두보기 페이지 |
| #313 | 스크롤 시 탭이 헤더에서 떨어져 올라감 (공개링크 OG 는 #362) |
| #340 | 앱 재시작 시 열려 있던 탭·마지막 위치 복원 |

### A-6. 정기업무 후속 (3건)
| # | 내용 |
|---|---|
| #347 | 반복 UI 가 평일만·BYSETPOS·분기를 만들지도 표시하지도 보존하지도 못함 |
| #349 | 미수행 회차가 지연으로 쌓여 프로젝트가 상시 red — miss_policy 신설(스키마 추가) |
| #352 | 반복 지침(description)이 회차에 사본으로 박제 — 표시 규칙 정리 |

### A-7. 검증 요청 (3건)
| # | 내용 |
|---|---|
| #267 | 모바일 비디오 첨부 · 첨부 기능 통일 · 드라이브 연동 시 동작 |
| #303 | Cue 가 채팅에 실제로 대응하는지 · 설정 화면이 안 보이는 이유 |
| #315 | Q info CSV 업로드·다운로드 — 업로드는 오늘 해소, **다운로드 확인 남음** |

### A-8. 닫기 후보 (1건)
| # | 내용 |
|---|---|
| #364 | 한글 자모 분리 입력 — **Irene 확인 결과 "문제 없음"**. 답글 달고 닫기 |

---

## B군 — 우선순위 (2026-08-22 재정렬)

기준: **업무가 실제로 막히는 순서**, 그 안에서 Fable 소모가 적은 것 먼저.
Fable 잔여 5% — 1군에만 쓴다. 나머지는 회복 후.

### 1군 — 지금 일이 막힌다
| 순위 | # | 내용 | Fable |
|---|---|---|---|
| 1 | **#302** | 컨펌 대기 중 외부컨펌이 안 걸린다. 승인·수정요청 전에 고객 컨펌을 받아야 하는 일이 실제로 막혀 있다. 백엔드가 "라운드 오염" 을 이유로 의도적으로 차단 중 — **전이 규칙 하나를 다시 판정**하면 된다 | 소 |
| 2 | **#349** | 미수행 회차가 지연으로 쌓여 프로젝트가 상시 red. 사람이 매주 손으로 취소 중 — `miss_policy` 컬럼 1개 | 소 |
| 3 | **#271 #307** | 결과물 버전 관리. 수정요청 왕복이 댓글로만 돌아 무엇이 최신인지 알 수 없다 | 중 |

### 2군 — 매일 쓰는데 불편
| 4 | **#330 재정의** | Q info 비밀번호 값 **`***` 로 가리고 보기/숨기기 토글**(Irene 안). q_records 에 이미 있는 패턴 이식 + 본문에 직접 쓴 자격증명을 색인·번역에서 뺄지 **Fable 과 정한다**(= 옛 #317·#318 정책 질문을 이 한 건으로 합침) | 소 |
| 5 | #236 #250 #309 | 업무 태그 — 부여·정렬·팝아웃 표시·퀵애드 옵션. 같은 요구가 3회 반복 | 중 |
| 6 | #344 #345 | 메일 중요표시 학습 · 보관 + 태그별 보기 | 소 |
| 7 | #261 | 메일 주소 기준 기본 기능 묶음(리스트·복사·검색·새메일·고객저장) | 소 |

### 3군 — 설계 끝남. 구현만 남았다 (Fable 재사용 불필요)
| 8 | **#208 #285** | **출퇴근·휴가 관리** — `docs/ATTENDANCE_LEAVE_DESIGN.md` 완성. DB 테이블 신설 포함. 규모가 커서 우선순위 뒤로 두되 **설계 재검토는 불필요** |
| 9 | **#259** | **게스트 링크(고객 무로그인 접근)** — `docs/GUEST_LINK_DESIGN.md` 완성. 남은 **결정 6건은 Fable 과 확정한다**(Irene 에게 되묻지 않는다): 게스트 표시명 주체 · 카드 미리보기 개방 범위 · 만료 정책 · 읽기전용 옵션 · 횟수 제한(권고: 제거) · Cue 자동응답 v1 미발화 |

### 4군 — 규모 큼
| 10 | #227 #230 #233 #235 | Cue 확장 — 파일 읽고 답하기 · Today's 브리핑 · 검색 AI · 자동추출 |
| 11 | #353 #354 #355 #358 | AI 루틴 설계 모드 |
| 12 | **B-1 실시간 공동 편집** | TipTap 협업 + Yjs 도입. 미도입 상태 |

### 5군 — 판단·조사
#211 #361 제품 방향 · #299 노션/슬랙 가져오기 · #225 워드/PDF/엑셀 내보내기 ·
#289 #346 공지 자리 + 배포 자동 공지 · #284 #360 저장 구조 · #343 워크스페이스 간 컨펌

---

## Irene 조치 대기 (2건 — 이것만 사람 손이 필요)

| # | 내용 | 정확히 무엇을 하면 되나 |
|---|---|---|
| **#356** | 운영 메일 암호화 키 미설정 | **"해" 한 마디.** 열쇠 생성 → 기존 암호문 재암호화 → .env 반영 → 재시작 → 메일 송수신 확인까지 Opus 가 전부 수행. `.env` 는 irene 소유라 sudo 불필요. 실행 전 DB 백업 |
| **#362** | SNS 링크 미리보기 | **sudo 두 줄.** `/etc/nginx/` 는 root 소유이고 Opus 의 sudo 는 비밀번호를 요구한다(2026-08-22 실측). Opus 가 완성된 설정 파일을 서버에 올려두면 Irene 이 `sudo cp` + `sudo systemctl reload nginx` 만 실행 |

---

## [옛] B군 원본 — 설계 선행
## B군 — 설계 선행

### B-1. 실시간 공동 편집 ★ 신규 (2026-08-21 요구)
> "같이 화이트보드처럼 대화하면서 업데이트해야 하는데 그게 안 되네. 노션처럼."

- 오늘 **편집 권한**은 공개 범위에 맞춰 열었다(볼 수 있으면 고칠 수 있다).
- 남은 것은 **동시 편집**이다. 지금은 낙관적 잠금이라 두 사람이 동시에 저장하면 나중 사람이 409.
- 필요한 것: TipTap 공식 협업 확장 + Yjs(CRDT), 기존 Socket.IO 로 동기화 채널.
  **미도입 확인함** — 협업 패키지 0개.
- 설계에서 정해야 할 것: 서버 Y.Doc 수명·영속화(현 `content_json` 과의 관계) ·
  기존 자동저장/낙관적 잠금과의 충돌 · 커서·참여자 표시 · 오프라인 병합 · 리비전 기록 ·
  방(room) 권한 검사 · **Q Note 는 본인 전용이라 대상 아님**.

### B-2. 제품 방향 (2건)
| # | 내용 |
|---|---|
| #211 | B2B 그룹웨어로서 보완·탁월 지점 검토 |
| #361 | 경쟁 분석(잔디·플로우·다우오피스) 기준 판단 — 원문이 fable 지목 |
| #299 | 노션/슬랙 데이터 가져오기 · 일반 파일 DB화 |

### B-3. AI·Cue 확장 (4건)
#227 워크스페이스 파일을 Cue 가 읽고 답하기 + 우측 패널 · #230 Today's 브리핑 ·
#233 통합검색에 AI · #235 자동추출 실동작 + 메일 탭 단위 설정

### B-4. 업무 워크플로 구조 (4건)
#236 #250 #309 **업무 태그**(부여·정렬·팝아웃 표시·퀵애드 옵션) ·
#271 #307 **업무 결과물 버전 관리** · #273 요청받은 업무 외부컨펌 정책 ·
#343 고객이 자기 워크스페이스를 가진 경우 컨펌 연결

### B-5. AI 루틴 설계 모드 (4건)
#353 후보→확정 배관(RRULE·워크스트림·지침·링크·priority) · #354 루틴 설계 모드 ·
#355 권장 순서 · #358 캔버스 AI 초안이 일반론

### B-6. 운영·공지 (2건)
#289 #346 워크스페이스 공지 업로드 자리 + 배포 시 자동 공지 · #225 워드/PDF/엑셀 내보내기

### B-7. 메일 정책 (3건)
#344 답변필요/확인권장 학습 + 중요표시 · #345 보관 + 태그별 보기 ·
#261 주소 기준 리스트·블록·복사·검색·새메일·고객저장 묶음

### B-8. 저장 구조 (2건)
#284 문서 → Info 항목별 정리 · #360 누적 로그를 담을 자리(행 누적 자산)

---

## C군 — 설계 있음 / **Irene 결정·조치 대기**

| # | 내용 | 막고 있는 것 |
|---|---|---|
| **#356** | 운영 `EMAIL_ENCRYPTION_KEY` 미설정 → JWT_SECRET 파생 fallback. `decrypt failed` 이미 2회 | **키 교체 전 기존 암호문 재암호화가 선행.** 운영 .env 변경 = Irene |
| **#362** | SNS 링크 미리보기 — 서버 렌더는 완료 | **nginx 한 줄 추가(root 권한).** `docs/OG_PREVIEW_NGINX.md` |
| **#359** | "Q record" 사용자 표기를 남길지 없앨지 | Irene 결정. 정하면 화면 문구·URL 처리까지 마무리 |
| #208 #285 | 출퇴근·휴가 관리 | 설계 완료(`docs/ATTENDANCE_LEAVE_DESIGN.md`). **승인만 남음** |
| #259 | 고객 무로그인 접근(게스트 링크) | 설계 초안 있음. **미결 질문 6건**(`docs/GUEST_LINK_DESIGN.md` §12) |
| **#317 #318** | Q info 공개범위 기본값 · 본문에 직접 쓴 자격증명의 색인·번역 | 가져오기 경로의 secret 은 오늘 차단. **본문 텍스트 정책은 결정 필요** |

---

## 권장 순서

1. **배포** — 0-B 의 8커밋. 특히 Q info 12건과 메일 전달·검색은 매일 쓰는 기능이다.
2. **C군 결정 3건** — #356(보안·운영) · #362(nginx) · #359(네이밍). 답만 주면 즉시 마무리.
3. **A-1 입력 유실**(#222 #367) — 쓴 글이 사라지는 계열이라 우선.
4. **A-2 그래프 숫자**(#254 #300 #255) — 공식 정본화가 선행.
5. **A-3 Q mail 7건** — 매일 쓰는 화면.
6. **B-1 실시간 공동 편집** — 오늘 요구. 규모가 커서 설계부터.
7. 나머지 A군 → B군.

## 규칙 (유지)

- 배포는 Irene 의 명시 지시로만.
- 이번 사이클은 Fable 미사용 — 자체 검증분은 `docs/FABLE_VERIFY_QUEUE.md` 에 인수인계.
- 고친 것은 **답글을 달고** 장부를 닫는다. 답글 없이 닫으면 사용자에겐 무시당한 것과 같다(#276).
