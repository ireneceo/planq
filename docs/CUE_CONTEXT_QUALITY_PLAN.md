# Cue 대화 품질 — 맥락 이해와 정보 파악 범위 (2026-09-19 착수 대기)

> Irene 2026-09-19: *"Cue 대화수준이 너무 낮아서 곤란해."* ·
> *"대화 맥락을 이해하는 수준이 너무 저급하고 워크스페이스에 대한 정보파악 범위가 너무 좁아.
> 위 내용 전반적으로 보완하고 나중에 fable에게도 한번 더 검토 시켜서 추가 보완 2차 해줘."*

**상태: 착수 전.** 아래는 실제 대화 1건을 코드·운영 데이터로 파악한 결과와 수리 계획이다.
**다음 세션에서 이 문서부터 연다.**

---

## 1. 무엇이 일어났나 (실측)

신고된 대화(요약): Irene 이 ①*"업무지연이 많은 직원은 누구야?"* 를 묻고 ②그걸 **개발요청으로
어떻게 정리하면 좋을지** 물었는데, Cue 는 ①에 답하지 못했고 ②에서는 **전혀 다른 주제(퍼플히어 POS
회의록)** 로 빠졌다. 같은 질문을 두 번 반복해도 같은 답이 돌아왔다.

### 근거 1 — 퍼플히어가 끌려온 이유는 **단어 하나**다
질문: *"개발요청을 하려는데 이걸 **어떻게** 정리해서 요청하면 될지 물었잖아"*
`cue_context.queryTerms()` 가 쪼갠 검색어:
`개발요청을 · 개발요청 · 하려는데 · 이걸 · 어떻게 · 정리해서 · 요청하면 · 될지 · 물었잖아`

`likeAny()` 는 이 단어들의 **OR + `LIKE %term%`** 이고 **관련성 순위가 없다.**
운영 q-note 실측 — 퍼플히어 노트 2건 중:
| 노트 | 길이 | 걸린 검색어 |
|---|---|---|
| #2 퍼플히어 수정필요 | 651자 | **없음** |
| #9 퍼플히어 | 2,904자 | **`어떻게` 하나** |

→ 2,904자 회의록이 일상어 한 단어로 «관련 자료» 가 되어 프롬프트에 실렸고, 모델은 들어온 자료를
  쓰려고 했다. Cue 의 사후 설명(*"검색 결과에 있어서 같이 보여준 거고요"*)은 **사실이 맞다** —
  다만 그게 왜 잘못인지를 모른다.

### 근거 2 — 「누가 지연이 많은가」는 **구조적으로 답할 수 없다**
- `services/cue_tools.js` 의 도구는 **6개이고 전부 쓰기**:
  `create_task` · `create_event` · `create_document_draft` · `submit_review` · `complete_task` ·
  `add_task_comment`. **조회·집계 도구가 0개.**
- `cue_context.getWorkspaceOverview()` 가 업무에 대해 주는 것은 두 가지뿐:
  - `ov.overdue` — 워크스페이스 **전체 지연 건수 하나**(담당자 축 없음)
  - `ov.urgentTasks` — 마감 임박순 **limit 10**
- Cue 의 답 *"임박·지난 업무 10건 중 담당자가 명시된 건 전부 irene"* 은 **그 10줄을 그대로 읽은 것**이고,
  거기서 *"irene 쪽에 몰려 있다"* 로 **넘겨짚었다.** 10건 표본으로 사람을 평가하는 결론은 위험하다.

### 근거 3 — 메타 질문인데도 워크스페이스를 뒤진다
*"이걸 어떻게 정리해서 요청하면 좋을까"* 는 **워크스페이스 데이터가 필요 없는 질문**이다.
파이프라인은 질문 종류를 가리지 않고 **항상** 검색한다 → 상관없는 자료가 들어오고 답이 그쪽으로 끌린다.

### 근거 4 — 맥락 유실은 «이력 부족» 이 아니다
`HISTORY_TURN_LIMIT = 10` 으로 이력은 넘어간다. 문제는 **검색 결과 덩어리가 프롬프트를 지배**해
「무엇을 묻는지」가 묻힌 것이다. 같은 질문을 두 번 해도 같은 자료가 들어오니 같은 답이 나온다.

---

## 2. 수리 계획 (우선순위)

| 순위 | 무엇 | 왜 · 판정 |
|:--:|---|---|
| **1** | **검색에 관련성 기준을 건다** — ①일상어 불용어 제외(어떻게·정리·요청·확인·필요·하려는데…) ②**한 단어만 걸린 것은 버린다**(최소 2개 일치 또는 가중치 합 기준) ③제목 일치 가중 ④점수순 상한 | 지금은 «어떻게» 한 단어로 2,904자가 딸려온다. **모든 대화에서 일어난다** — 체감 품질 저하의 가장 큰 몫. R=0·S=0·F=1(질문 표본으로 «걸린 자료» 를 세면 참/거짓이 갈린다) |
| **2** | **조회 도구를 준다** — 담당자별 지연·부하 집계 등 **읽기 도구**. 지금은 쓰기 6개뿐 | «누가 지연이 많나» 류는 도구 없이 영영 못 답한다. ★ **지표를 Irene 이 정해야 한다** — 지연 건수 / 지연 비율 / 평균 지연 일수 / 기간 |
| **3** | **표본으로 단정하지 않게** — 잘린 목록만 본 상태면 «10건 기준» 임을 말하게(프롬프트 계약) | 사람 평가에 표본 추정은 위험하다. 지금은 그 사실을 숨기고 단정했다 |
| **4** | **메타 질문은 검색을 건너뛴다** — "어떻게 쓰면 좋을까" 류 | 없는 맥락을 끌어와 엉뚱한 답을 만든다 |
| **5** | **정보 파악 범위 확대** — Cue 가 못 보는 영역(개인 캘린더·고객 활동 이력 등) 중 **무엇을 열지** 정리. 권한 축(`access_scope`)과 같이 본다 | Irene 의 «정보파악 범위가 너무 좁아» 는 여기다. **R=1 영역이 섞인다**(권한·격리) — 열 범위를 정하는 것은 Fable 판단이 필요하다 |

### 하지 말 것
- 검색을 끄거나 상한만 줄이는 것 — 그러면 정작 필요한 자료도 사라진다. **순위를 만들어야 한다.**
- 도구를 늘리면서 권한 검사를 건너뛰는 것 — 조회 도구도 `access_scope`·`business_id` 를 탄다.

---

## 3. Fable 2차 검토 (Irene 지시)
1차 보완을 끝낸 뒤 **Fable 에게 다시 검토시켜 2차 보완**한다. Fable 에게 물을 것:
- 관련성 기준이 **놓치는 쪽**(있어야 할 자료가 안 걸리는 경우)을 실제 질문 표본으로 재 달라 —
  나는 «덜 걸리게» 만들 수는 있어도 «맞게 걸리는지» 를 혼자 가르기 어렵다.
- 조회 도구가 열 **데이터 범위와 권한 축** — 멤버별 지연 통계는 **인사 정보**에 가깝다.
  누가 볼 수 있어야 하는가(owner/admin만? 본인 것만?)는 R=1 판단이다.
- 정보 파악 범위 확대(5번)에서 **열면 안 되는 것**.

---

## 4. 관련 파일
- `dev-backend/services/cue_context.js` — `queryTerms`(382) · `likeAny`(402) · `getWorkspaceMatches`(406) ·
  `getWorkspaceOverview`(617, `overdue`·`urgentTasks` limit 10) · `HISTORY_TURN_LIMIT`(105)
- `dev-backend/services/cue_tools.js` — 도구 6개(전부 쓰기)
- `dev-backend/services/cue_orchestrator.js` · `cuePrompts.js` · `routes/cue.js`

---

## 1차 보완 실행 기록 (2026-09-20, [Opus] · **Fable 미검증(자체 검증)**)

### 한 일 — 1·3순위. 4순위는 1순위에 흡수됐다

**1순위 · 검색 관련성 관문** (`services/cue_context.js`)

| 장치 | 내용 |
|---|---|
| `KO_STOPWORDS` | 말버릇·질문형 어휘(«어떻게» «물었잖아» «하려는데» …) + 일반 업무 어휘 + 동사 활용형(«정리해서» «작성해서» …)을 검색어에서 뺀다 |
| `KO_WEAK_TERMS` | «고객» «업무» «프로젝트» 처럼 **어디에나 있는 말**. 이것만으로는 못 걸린다 |
| `termWeight` + `rankByRelevance` | 긴 말일수록 무겁게, **제목 일치는 본문의 3배**. 통과 조건 = 강한 말 1개 이상 **또는** 서로 다른 말 2개 이상 |
| SQL 후보 상한 | tasks 6→40 · projects 4→30 · clients 4→30. 넓게 긁고 **관문에서 좁힌다**(옛 방식은 좁게 긁어 엉뚱한 6건이 확정됐다) |

**3순위 · 표본으로 단정하지 않게** — 검색 결과 블록 머리에 계약 4줄을 붙였다.
*"이건 관련도 상위 일부다(전수 아님). «가장 많은/몇 건/누가 제일» 을 이 목록만으로 단정하지 마라."*
전수는 아래 「워크스페이스 현황」의 집계 숫자뿐이라는 것도 같이 적는다.

**4순위 · 메타 질문 검색 건너뛰기 — 별도 분류기를 만들지 않았다.**
불용어만으로 신고된 질문이 후보 0건이 됐다(아래 표). 질문을 «메타냐 아니냐» 로 가르는 분류기는
오분류하면 **찾아야 할 것을 못 찾는** 쪽으로 틀리므로, 효과가 이미 나온 이상 더하지 않는다.

### 운영 데이터 실측 (biz 1, 읽기 전용)

| 질문 | 고치기 전 | 고친 뒤 | 통과한 것 |
|---|---:|---:|---|
| 개발요청을 … 어떻게 정리해서 요청하면 될지 | 노트 1건이 답을 지배 | **0건** | — (신고된 오염의 근원) |
| 퍼플히어 회의록 정리해줘 | 업무 29 | **3** | 퍼플히어 파비콘 · 퍼플히어 데모 오류 · 출판사 미팅 회의록 |
| 워프로랩 청구서 상태 알려줘 | — | **6** | 전부 청구서 업무 (K-DINE 계약서·프린트 결제·기율 구독) |
| 고객 중에 제안서 보낸 곳 어디야 | 업무 38 | **0** | 운영에 「제안서」가 든 업무가 **실제로 0건** — 막은 게 아니다 |
| 이번 주에 마감인 업무 알려줘 | 업무 72 | **0** | 마감 목록은 「워크스페이스 현황」이 항상 싣는다(검색 몫이 아니다) |
| 업무지연이 많은 직원은 누구야? | — | **0** | 집계 질문 — 2순위(집계 도구)의 몫 |

### 자체 검증
- `health-check` 44/44 EXIT 0 · `guard-invariants` 58/59 EXIT 0(경고 1 = 문서 신선도) · `e2e --suite tenant` 실패 0
- 표본 계약 **양성/음성 대조군**: 검색 결과가 있을 때만 문구가 나오고, 없으면 안 나온다
- 운영 측정은 **읽기 전용**(코드를 보내 `require` 로 부르고 지움). 운영 코드·데이터 변경 0

### Fable 2차 검토에 올릴 것 (Irene 지시)
1. **임계값** — `strongHits < 1 && hits < 2` 가 맞는가. 「제안서」 0건은 데이터가 없어서였지만,
   **없는 것과 막힌 것을 사용자는 구별하지 못한다.** 0건일 때 Cue 가 뭐라고 말해야 하는가.
2. **`KO_WEAK_TERMS` 는 손으로 적은 목록이다** — 목록 방식은 늘 샌다(`feedback_mask_by_whitelist_not_denylist` 계열).
   빈도 기반(그 워크스페이스에서 너무 흔한 말은 자동으로 약해짐)으로 갈 수 있는가.
3. **2순위(집계 도구)·5순위(범위 확대)는 Irene 판단이 선행**한다 — 어떤 지표를 볼지 · 어디까지 열지(권한 축, R=1).

### Fable 검증 결과 — **FAIL → 수리 (2026-09-20)**

Fable 이 커밋 `9553b2f6` 을 독립 검증해 **실제 회귀 2건**을 잡았다(①diff범위 PASS ②가드3축 PASS
③실호출 **FAIL** ④배포안전성 PASS — 성능은 `EXPLAIN` 으로 6→40 차이 없음을 확인했다).

| Fable 이 잡은 것 | 무슨 일이 났나 | 수리 |
|---|---|---|
| **파일 관문이 없는 컬럼을 읽었다** | `r.original_name \|\| r.filename` — File 모델에는 **`file_name`** 뿐이다. 제목이 항상 `''` 이라 **이름이 정확히 맞은 파일까지 전부 탈락**(운영 실측 5건 → 0건). 본문은 실리는데 목록은 없어 커버리지 선언과 모순 | `r.file_name` |
| **「문서 뭐 있어」 폴백이 죽었다** | 2026-09-08 에 일부러 넣은 계약(영역을 물으면 최근 N건)을 관문이 **같은 약한 말**로 재판정해 전부 버렸다. posts 5→0 · mail 4→0 · files 8→0 | **폴백을 관문 뒤로** 옮겼다 |

★ 폴백을 «관문 앞에서 건너뛰기» 로 막는 것은 **부족하다** — LIKE 가 약한 말로 한 건이라도 걸면
폴백 기회를 잃고 그 한 건마저 관문에서 죽어 결국 0건이 된다(dev 실측). 그래서 순서를 바꿨다:
**관문을 통과한 것이 없고 사용자가 그 영역을 물었으면, 그때 최근 목록을 싣는다.**

회귀: **`dev-backend/scripts/checks/cue-search-gate.js`** (4검사 + 양성 대조군 2).
★ 이 검사기를 **한 번 고쳤다** — 검사 ①이 「…파일 어디 있어」로 물어 `hints.files` 를 켜는 바람에
**폴백이 빈 결과를 채워** 결함을 되살려도 통과했다(거짓 초록). 지금은 「파일」 이라는 말을 빼고
**그 파일이 목록에 들어 있는지**를 판정한다. 두 결함을 각각 되살려 빨간불이 켜지는 것을 확인했다.
