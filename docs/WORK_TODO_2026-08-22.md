# 할 일 우선순위 — 2026-08-22

운영 미완료 **33건** 실측 기준. 전날 목록(`WORK_TODO_2026-08-21.md`)을 오늘 작업분 + 신규 3건으로 갱신.

기준: **지금 손해가 나고 있는 것 → 일이 막힌 것 → 불편한 것 → 규모 큰 것**.
Fable 잔여 5% — 아래 ⓪⓵ 을 묶어 **한 번만** 쓴다.

---

## 오늘 처리 완료 — 배포 대기 (커밋됨, 운영 미반영)

| # | 내용 | 실측 |
|---|---|---|
| **#302** | 컨펌 대기 중에도 외부컨펌 · 해제하면 그 라운드로 복귀 | 진입 200 → 해제 후 reviewing·round·컨펌자 보존 |
| **#349** | 미수행 회차 자동 넘김 (`miss_policy`, 기본은 종전 동작) | 지난 미수행만 마감 · 진행중/미래 무변경 · 멱등 · 이력 기록 |
| **#271 #307** | 결과물 회차별 박제 (`task_deliverable_versions`) | 1차 초안 / 2차 반영본이 round 1·2 로 각각 보존 |
| **#369** | 문서 목차 — 보는 사람이 켜고 끄기 · 접힘 기억 · 본문과 좌우 정렬 | 빌드 통과 |
| **#356** | 운영 메일 암호화 키 (이미 운영 반영·장부 닫음) | 재암호화 10건 성공 · IMAP 3계정 연결 확인 |
| **#222 잔여** | 전달 메일 초안 보존 — 이탈 차단 제거 | 빌드 통과 |

---

## ⓪ 지금 손해가 나고 있다 — 최우선 (신규, 2026-08-22)

세 건은 **한 뿌리**다: PlanQ 알림메일이 우리 메일함으로 되돌아와 매일 쌓인다.
**멈추는 것이 먼저, 치우는 것은 그다음.** 오늘 하루도 계속 쌓이는 중이다.

| 순위 | # | 내용 | 지금 상태 |
|---|---|---|---|
| **1** | **#370** | 알림메일 로고가 매번 File 로 저장 | 워프로랩 Q File 의 **52%(524건·20.5MB)가 planq-logo.png**. 하루 ~15건, 8/20 은 31건. 사용자 파일 목록이 쓰레기로 덮인다 |
| **2** | **#371** | 자기발신 알림메일을 Q Mail 이 재수집 | inbound 2,274건 중 **826건(36%)**. 건당 58KB. #370 의 상위 원인 |
| **3** | **#372** | 스토리지 카운터가 실제와 불일치 | 집계 14건/53MB vs 실제 **961건/183MB**. **쿼터가 실사용을 못 막는다**(biz 5 는 17건 쓰는데 카운터 0) |

**작업 순서 (원인 → 결과 순으로)**
1. **#371 수집 차단** — `auto-submitted: auto-generated` 헤더는 이미 붙어 있다. 수집 단계에서 스킵할지 격리할지만 정하면 된다.
   > ⚠️ **Irene 판단 1건**: "PlanQ 알림을 메일함에서도 보고 싶다" 면 스킵이 아니라 격리(기본 필터에서 제외)로 간다. 어느 쪽?
2. **#370 저장 게이트** — `saveAttachmentAsFile()` 에서 로고를 거른다. **표시 술어(`isEmbedded`)는 손대지 않는다** — 그건 #215 가 세금계산서 숨김 사고를 막으려고 fail-open 으로 둔 것이고 축이 다르다. 저장 단계에 게이트를 따로 둔다.
3. **#372 카운터** — `saveAttachmentAsFile()` 이 `storageUsage` 를 경유하게 + 전 워크스페이스 재계산 백필 1회.
   > ⚠️ `storageUsage.js:72` — 자체 트랜잭션을 열므로 다른 트랜잭션 안에서 부르면 안 된다.
4. **누적분 정리** — 로고 File 524건 삭제 + 물리파일 정리. 카운터는 **차감하지 않는다**(반영된 적이 없어 빼면 더 틀어진다).
5. 부수로 같이: 메일첨부가 `content_hash` 를 안 채워 중복제거를 못 탄다(524건 전부 NULL).

---

## ⓵ 일이 막혀 있다

| 4 | **#273** | 요청받은 업무에서도 외부컨펌 — #302 로 상태 제약은 풀렸다. 남은 것은 **권한**(컨펌자도 걸 수 있는가) 판정 |
| 5 | **#343** | 고객이 자기 워크스페이스를 가진 경우 컨펌 연결 |

---

## ⓶ 매일 쓰는데 불편

| 6 | Q info 값 가리기 | `***` + 보기/숨기기(Irene 안). q_records 패턴 이식 + 본문 자격증명 색인·번역 정책 |
| 7 | **#344 #345** | 메일 중요표시 학습 · 보관 + 태그별 보기 |
| 8 | **#261** | 메일 주소 기준 기본 기능 묶음 |
| 9 | **#235** | 채팅 업무 자동추출 실동작 검증 + 메일 탭 단위 설정 |

---

## ⓷ 설계 끝남 — 구현만 (Fable 재사용 불필요)

| 10 | **#208 #285** | 출퇴근·휴가 — `docs/ATTENDANCE_LEAVE_DESIGN.md`. DB 테이블 신설 포함 |
| 11 | **#259** | 게스트 링크 — `docs/GUEST_LINK_DESIGN.md`. 결정 6건은 **Fable 과 확정**(Irene 에게 되묻지 않는다) |

---

## ⓸ 규모 큼 — 설계 선행

| 12 | #353 #354 #355 #358 | AI 루틴 설계 모드 · 배관 확장 · 캔버스 초안 품질 |
| 13 | #227 #230 #233 | Cue 파일 읽기 · Today's 브리핑 · 검색 AI |
| 14 | #360 #284 | 누적 로그 자리 · 문서→Info 항목별 |
| 15 | 실시간 공동 편집 | TipTap + Yjs 미도입 |

---

## ⓹ 판단·조사

#211 #361 제품 방향(경쟁 분석) · #299 노션/슬랙 가져오기 · #225 워드/PDF/엑셀 내보내기 ·
#289 #346 공지 자리 + 배포 자동 공지

---

## Irene 손이 필요한 것 (2건)

| # | 무엇을 하면 되나 |
|---|---|
| **#362** | **sudo 두 줄.** `docs/OG_PREVIEW_NGINX_PROD.md` 를 운영서버 세션에 주면 된다. `/etc/nginx/` 는 root 소유이고 Opus 의 sudo 는 비밀번호를 요구한다(2026-08-22 실측) |
| **#371 판단** | PlanQ 알림메일을 메일함에서 **안 보이게(스킵)** 할지, **보이되 기본 필터에서 빼기(격리)** 할지 |

## 규칙

- Fable 은 돈·보안 경계·마이그레이션·신규 시스템에만. 나머지는 Opus 자체 검증 + **그 사실을 명시**.
- 같은 기능의 후속 수정은 묶어서 한 라운드.
- 배포는 Irene 의 명시 지시로만. 고친 것은 **답글을 달고** 장부를 닫는다.
