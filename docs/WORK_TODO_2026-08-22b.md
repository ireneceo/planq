# 할 일 우선순위 — 2026-08-22 (근태 배포 후)

운영 미완료 **25건**. 이번 배포로 #208·#285 를 답글과 함께 닫았고, **새 신고 2건이 들어왔다.**

기준: **지금 사용자가 겪고 있는 것 → 일이 막힌 것 → 불편한 것 → 규모 큰 것**.
Fable 은 이번 주 소진. 고위험(돈·보안경계·마이그레이션·신규시스템)만 남겨 쓰고 나머지는 Opus 자체 검증 + 그 사실 명시.

---

## 방금 배포됨 (닫음)

| # | 내용 |
|---|---|
| #208 | 출퇴근·휴가 — 기록·승인·잔여 + Q Task 가용시간 자동 차감 + 업무 시작/퇴근 연동 |
| #285 | 〃 (밀린 게 아니라 시작된 적 없던 기능) |

운영 스키마 4표·ENUM 2건 자동 반영 확인. Fable 게이트 PASS.

---

## ⓪ 지금 사용자가 겪고 있다 — **2건 (새 신고)**

| 순위 | # | 내용 |
|---|---|---|
| **1** | **#373** | **#362 가 아직 안 됐다.** nginx 는 적용됐는데 크롤러는 여전히 기본 홍보문구를 받는다. `ogMetaMiddleware` 의 마지막 분기가 `next()` 없이 기본 OG 로 응답을 끝내서, 봇 요청이 `routes/og.js` 에 **영영 도달하지 않는다**. 크롤러용으로 만든 코드에 크롤러가 못 닿는 상태 |
| **2** | **#374** | **운영 지뢰 (임시조치됨).** `routes/og.js` 가 SPA index 경로를 `dev-frontend-build` 로 하드코딩 → 운영에서 `/insights/:slug` 전면 404 였다. 운영 `.env` 에 `SPA_INDEX_PATH` 를 넣어 살려뒀지만, **코드는 그대로다.** 같은 저장소에 후보배열 패턴이 이미 두 곳 있다(`ogMeta.js:263`, `emailService.js:15`) |

두 건은 한 덩어리로 고치는 게 맞다 — 같은 파일이고, #374 를 고치지 않으면 #373 을 고쳐도 같은 지뢰가 남는다.
곁들일 것: 읽기 실패 시 404 대신 `next()` 로 정적 폴백(#374 지적), dev→운영 문서에 **운영 추가 env** 절 신설.

---

## ⓵ 일이 막혀 있다

| 3 | **#273** | 요청받은 업무에서도 외부컨펌 — 상태 제약은 #302 로 풀렸다. 남은 것은 **권한**: 컨펌자도 걸 수 있어야 하는가 |
| 4 | **#343** | 고객이 자기 워크스페이스를 가진 경우 컨펌 연결 |

---

## ⓶ 매일 쓰는데 불편

| 5 | Q info 값 가리기 | `***` + 보기/숨기기. q_records 패턴 이식 |
| 6 | **#344 #345** | 메일 중요표시 학습 · 보관 + 태그별 보기 |
| 7 | **#261** | 메일 주소 기준 기본 기능(리스트·복사·검색·새메일·고객저장) |
| 8 | **#235** | 채팅 업무 자동추출 실동작 검증 + 메일 탭 단위 설정 |

---

## ⓷ 설계 끝남 — 구현만

| 9 | **#259** | 게스트 링크 — `docs/GUEST_LINK_DESIGN.md`. **공개 표면 + 쓰기 권한**이라 Fable 게이트 필수 → 예산 회복 후 |
| 10 | 근태 Phase 3 | 월별 통계 화면 · 관리자 정정 화면(라우트는 있음) · 주간보고 휴가 표시. `docs/ATTENDANCE_DEPLOY_NOTES.md` |

---

## ⓸ 규모 큼 — 설계 선행

| 11 | #353 #354 #355 #358 | AI 루틴 설계 모드 · 배관 확장 · 캔버스 초안 품질 |
| 12 | #227 #230 #233 | Cue 파일 읽기 · Today's 브리핑 · 검색 AI |
| 13 | #360 #284 | 누적 로그 자리 · 문서→Info 항목별 |
| 14 | 실시간 공동 편집 | TipTap + Yjs 미도입 |

---

## ⓹ 판단·조사

#211 #361 제품 방향(경쟁 분석) · #299 노션/슬랙 가져오기 · #225 워드/PDF/엑셀 내보내기 ·
#289 #346 공지 자리 + 배포 자동 공지

---

## 후속 과제 (비차단)

| 내용 | 왜 |
|---|---|
| `weeklyReviewSnapshot.js:611` 휴가 차감 인라인 공식 통합 | Fable 권고. 지금은 값이 같지만 "같은 값의 공식 여러 벌" 의 씨앗 |
| `hold_prev_status` 컬럼 분리 | 보류와 외부컨펌이 한 컬럼을 공유 — 연달아 걸면 복귀 지점이 덮인다 |
| #349 자동 취소에 broadcast·notify | cron 새벽 실행이라 수용했으나 §13·§16 관점 결손 |
| 카운터 재계산 사각 | 파일 0건인데 카운터>0 인 워크스페이스는 교정 안 됨. BSU LEFT JOIN 으로 |

---

## 규칙

- 배포로 고친 것은 **답글을 달고** 장부를 닫는다. 답글 없이 닫으면 무시당한 것과 같다.
- Fable 은 돈·보안 경계·마이그레이션·신규 시스템에만. 나머지는 Opus 자체 검증 + **그 사실 명시**.
