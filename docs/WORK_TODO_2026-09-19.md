# 남은 일 — 2026-09-19

운영 미처리 신고 9건 기준. **"왜 안 됐나" 를 항목마다 적는다** — 이유가 없으면 다음 사람이
같은 조사를 처음부터 다시 한다. (Irene 2026-09-19: *"A에서 안된 건 왜 안된거야. 포함해서 남은 일로 저장해둬."*)

> ★ **먼저 정정.** 2026-09-19 세션에서 내가 Irene 에게 보고한 «#417 안 된 것» 목록은 **틀렸다.**
> WIP 커밋(`4844256d`)의 메시지만 읽고 «거기 안 적힌 것 = 안 된 것» 으로 추측했다. 실제 코드를 보니
> 중첩 폴더·다중선택 삭제·ZIP 다운로드는 **이미 되어 있었다.** 커밋 메시지는 diff 를 대변하지 않는다
> (memory `feedback_commit_message_understates_diff`). 아래는 **코드를 확인한** 결과다.

---

## #417 · Q file (09-16)

| 요청 | 상태 | 근거 |
|---|---|---|
| 폴더 이름이 1글자만 나오고 잘림 | **됨** | 아이콘 5개 → OverflowMenu (`4844256d`) |
| 폴더 안에 또 폴더 | **됨** | `FileFolder.parent_id` + `createFolder(projectId, name, parentId)` · UI 가 `parentId` 를 넘긴다(DocsTab:817·849) |
| 파일 편집에서 폴더 이동 | **됨** | [폴더로 이동] 메뉴 (`4844256d`) |
| 폴더 전체 다운로드 | **됨** | 〃 |
| 파일 다중선택 다운로드·삭제 | **됨** | `selectedDownloadable` → [ZIP 다운로드 (n)] · `selectedDeletable` → 일괄 삭제 + 확인창 |
| **프로젝트>파일에서 만든 폴더가 Q file 에 안 나온다** | **됨 (2026-09-20)** | Q file 의 **파일 목록은 프로젝트 파일을 이미 담고 있었다**(`GET /api/files/:biz` 는 `project_id` 로 거르지 않는다) — **폴더만 빠져** 있어서 파일은 보이는데 그 폴더로 갈 수 없었다. `/api/folders/workspace/:biz` 가 프로젝트 폴더와 **하위 폴더까지** 주고, 트리가 프로젝트별로 묶어 그린다. 실측 6검사 통과(하위·parent 사슬·프로젝트 이름·음성 대조군) |
| ~~(옛 기재)~~ | ~~안 됨~~ | `file_folders` 는 `project_id` 로 갈린다. Q file(워크스페이스)은 `project_id IS NULL` 만 본다 — **설계대로 갈라져 있다.** Irene 의 요청은 «Q file 에서 프로젝트를 누르면 그 하위 폴더가 보이게» 이므로, 목록을 합치는 것이 아니라 **Q file 에 프로젝트 축을 하나 더 그리는** 일이다. 화면 구조 결정이 먼저다 |
| **용량 큰 파일이 안 올라간다** | **조치함 (2026-09-20)** | 파일당 한도를 올렸다 — Free 5→**25MB** · Starter 20→**100MB** · Basic 50→**250MB** · Pro 100→**500MB** · Enterprise 200MB→**1GB**. 총 저장공간(=유료 축)은 **그대로**라 요금 기준에 영향이 없다. 운영 nginx 는 600MB 까지 통과하는 것을 실측했다(dev 는 50M 이라 그 위는 dev 에서 시험 불가 — sudo 필요). multer 는 diskStorage 라 메모리에 안 올린다 |
| ~~(옛 기재)~~ | ~~조치 미정~~ | `services/plan.js upload_file` — 플랜별 `file_size_max_bytes`(Free 10MB · Basic 30MB · Pro 50MB)가 상한. Drive 로 라우팅되면 5GB. 예전엔 **문구가 «최대 50MB» 로 하드코딩돼 거짓말**이었고 그건 고쳤다. 남은 판단: 한도를 올릴지, 큰 파일은 Drive 로 유도할지 — **Irene 결정 사항** |

---

## #418 · 구글드라이브 (09-16)

| 요청 | 상태 | 왜 |
|---|---|---|
| PNG 등 미리보기가 안 뜬다 | **됨** | 브라우저가 형식을 안 알려준 파일도 미리보기 대상 (`4844256d`) |
| **드래그앤드롭으로 파일 이동** | ~~안 됨~~ → **됨** (2026-09-20 정정) | **또 틀리게 적었다.** 코드를 안 보고 적은 것이다 — `DocsTab.tsx` 의 `useFolderDrop` + `onDropToFolder` 로 **이미 구현돼 있고**, OS 로 끌어내는 다운로드도 `hooks/useFileDragOut.ts` 로 붙어 있다(서명 URL 5분). 같은 실수를 #417 에서 하고 바로 다음 항목에서 또 했다 — memory `feedback_commit_message_understates_diff` |
| 드래그해서 **OS 로 다운로드** | **직접 올린 파일만 됨** | `isDraggableOut` 이 `source === 'direct'` 로 막는다. 이건 «화면만 엄격한 것» 이 아니다 — `chat-45` 의 숫자는 **MessageAttachment id**, `task-7` 은 TaskAttachment id 라 `files.id` 가 아니다. 조건만 풀면 **엉뚱한 파일을 내보낸다**(2026-09-20 에 한 번 풀었다가 되돌렸다). 서버가 출처별 id 를 받게 해야 하는데 그건 **무인증 서명 URL 표면 확장(R=1)** 이라 Fable 판정이 선행돼야 한다. 대기열 #228-b |
| **파인더처럼 쓰기 / 양방향 동기화** | **안 됨 (구조)** | 우리는 `drive.file` scope 만 갖는다 — **PlanQ 가 만들었거나 사용자가 PlanQ 로 연 파일만** 보인다. 드라이브 전체를 읽는 동기화는 이 scope 로 불가능하고, 넓히면 구글 검증을 다시 받아야 한다. Irene 이 #419 에서 던진 질문(*"파인더 접근을 안 해도 되게 PlanQ 에 집중하는 게 날까?"*)과 **같은 결정**이다 |

---

## #411 · 일정 (09-11)

| 요청 | 상태 | 왜 |
|---|---|---|
| 프로젝트 연결 · 참석자(멤버·고객) · 프로젝트로 후보 좁히기 · 미팅자료 연결 | **됨** | `30daccc5` + 후속 |
| **파일·문서를 연결했을 때 참석자에게 알릴지 묻기 (수정 시에도)** | **안 됨** | 알림 발송 자체는 있는데 «물어보고 보낸다» 가 없다. 확인 창 + 수정 시 재확인이 필요하고, 외부 발송이라 **CLAUDE.md 상 확인을 받아야 하는 계열**이다(보낼 대상을 문구에 적어야 한다) |
| **미팅내용 정리 → Q note 열기 (녹음/메모/녹화 선택)** | **안 됨** | 오늘 만든 「상담 진행」 드롭다운이 **같은 모양**이다(`components/QNote/newNoteMenu`). 일정 상세에 그 메뉴를 얹으면 된다 — **가벼운 편**. 다만 «일정에서 시작한 노트» 를 그 일정에 연결하는 축(`calendar_event_id`)을 세션에 남길지 결정이 필요하다 |
| **화상미팅 발화자 인식·녹화** | **안 됨 (큰 작업)** | 네이티브 앱이 되었어도 화상회의 오디오를 **화자별로** 가져오는 것은 플랫폼(구글 Meet 등) 쪽 기능이 있어야 한다. 지금 Q note 는 마이크 한 채널을 받는다 |

---

## #382 · Q sale 통화 녹음 (08-23)

| 요청 | 상태 | 왜 |
|---|---|---|
| 상담에 녹음 추가 → Q note 로 정리 → 파일 저장 → 점핑·동기화 | **대부분 됨 (2026-09-18)** | [상담 진행] → Q note → [종료하고 상담 저장] → 상담 기록 → [노트 열기] |
| **상담 요약이 자동으로 정리되어 들어가기** | **반만 됨** | 요약이 **있으면** 저장 창의 메모에 미리 채운다. 하지만 **요약을 자동 생성하지는 않는다** — 사람이 Q note 에서 [요약] 을 눌러야 한다. 자동 생성은 비용(LLM)이 붙으므로 **언제 돌릴지 정책 결정**이 먼저 |
| **업무추출 연결** | **안 됨** | 메일에는 [업무 추출] 이 있다. 상담 기록에는 없다. 메일과 같은 문(`requestExtractTasks`)을 상담에 붙이는 일 — **가벼운 편** |

---

## 답을 기다리는 것 (Irene 결정)

- **#421** — 문구 축약은 2026-09-19 에 했다. 남은 질문: *"[문의] 와 같은 다른 태그 필요한 거 없어?"*
  → 지금 사유는 5종(자동업무·청구결제·견적계약·확인권장·확인필요). 「문의」를 넣으려면 **무엇을 문의로 볼지**
    판정이 필요하고, 그건 Q sale 상담 유입 기준(`saleMailCriteria`)과 같은 술어를 써야 한다. 두 벌이 되면 갈라진다.
- **#415** — 본문이 «추가로 궁금합니다.» 한 줄뿐. #416 과 1분 차이라 그 건의 추가 문의로 보인다. 무엇을 물으신 건지 확인 필요.
- **#419** — 드라이브를 고객·멤버가 보게 할지 vs PlanQ 안에서 끝낼지. **#418 과 한 덩어리**다.
- **#412** — 고객의 ChatGPT 히스토리까지 대응. 장기 전략.
- **#381** — 게스트 단위 영업/상담 히스토리. **2026-09-18 에 정한 «상담 = 고객 축의 기록» 과 직결**된다.
  게스트를 같은 축에 올리면 자연스럽게 풀리는 구조다.

---

## 가벼워서 바로 할 수 있는 것 (다음 세션 후보)

1. **상담 기록에 [업무 추출]** — 메일과 같은 문을 붙인다 (#382)
2. **일정 상세에 [미팅내용 정리]** — 오늘 만든 새 노트 메뉴를 얹는다 (#411)
3. **#421 의 남은 판단** 이 정해지면 태그 1종 추가
