# Fable 검증 대기 목록

> Fable 사용량 소진(2026-08-19, 98%) 동안 **개발·커밋은 하되 배포하지 않는다**.
> 사용량이 회복되면 **아래 순서대로** 게이트를 태우고, **통과한 것만** 배포한다.
> 한 번에 몰아 검증하지 말 것 — FAIL 이 나면 어느 작업 탓인지 특정할 수 있어야 한다.

---

## A. 소급 검증 — 이미 운영에 나가 있음 (롤백 아님, 사후 확인)

Fable 사용량이 떨어진 뒤 Opus 자체 검증만으로 배포된 4건. **위험도 순.**

### A-1. `c6f276bd` 메일 성능 — 본문 base64 이미지 분리 + 응답 압축  ★최우선
- 본문의 97~100%가 base64 이미지였고 nginx `gzip_types` 주석 처리로 JSON 이 압축조차 안 되고 있었다.
  `services/emailInlineData.js` 신설(내려보낼 때만 `data:` → `cid:planq-embed-N`, **DB 무변형**),
  `GET .../messages/:messageId/embedded/:index` 신설, `compression` 미들웨어 추가.
- **반증 항목**
  1. 실 메일 3건 이상에서 본문 이미지가 **깨지지 않는지**(iframe contentFrame 으로 특정해 측정할 것 —
     첫 프레임을 보면 이미지 없는 스레드도 "정상" 으로 세는 함정이 있다. Opus 가 그 함정에 빠졌다)
  2. 접힌 메시지의 이미지가 **요청되지 않는지** (펼칠 때만 요청)
  3. `embedded/:index` 의 멀티테넌트 — 타 워크스페이스 스레드 404/403
  4. strip/extract 의 **인덱스 규칙 일치** — 4KB 미만·이미지 아닌 data URI 제외 조건이 양쪽 같은지
  5. 압축이 Stripe webhook(raw body 서명검증)에 영향 없는지
  6. 회선 바이트는 `response.buffer()` 가 0 을 돌려준다 — **CDP encodedDataLength** 로 잴 것

### A-2. `c2a47409` 별칭 문구 + 주소별 서명 UI
- 반증: 저장→재조회 일치 · 발송 경로가 그 주소 서명을 고르는지(`signatureSource='alias'`) ·
  비우면 null 저장 후 기본 서명으로 내려가는지 · 서명 없는 별칭 무영향

### A-3. `a0fe91fb` 설정 폭 제한 철회 + 입력칸 상한
- 반증: 설정 전 탭 여백이 다른 화면(고객 목록)과 동일 · 입력칸 ≤420px · 가로 스크롤 0 ·
  **페이지 이동이 실제로 되는지 확인하며 측정할 것**(full load 가 라우트를 복원해 네 페이지가
  같은 값으로 찍히는 함정 — `gotoSPA` + url/제목 확인)

### A-4. `175bfe50` 개인정보처리방침 Gmail 조항
- 반증: ko/en 양쪽에서 `mail.google.com` 0건 · 캘린더 조항과 Limited Use 유지

### A-5. `74b791e6` 서명 계층 표시 (문구) — Irene 지시로 즉시 배포
- 섹션 제목 `서명`→`기본 서명` · 별칭 배지 `공통 서명`→`기본 서명 사용` · 토글 `자동 첨부`→`서명 사용`
  \+ 스코프 안내
- **반증 (핵심)**: Opus 가 문구로 **단언한 내용이 사실인지** 실측할 것 —
  1. 토글 OFF 상태에서 **전용 서명이 있는 주소로 보내도 서명이 안 붙는지**
     (`emailSend.js` 의 `disabled` 가 최상위라는 주장)
  2. 우선순위 무변경: 토글 OFF → 없음 / 별칭 전용 → 메일함 기본 → 워크스페이스
  3. ko/en 양쪽 신규 문구 노출 + 옛 문구 잔존 0 · i18n parity

---

## B. 배포 대기 — 커밋됨, 운영 미반영

(현재 없음 — `74b791e6` 은 Irene 지시로 즉시 배포해 A-5 로 옮겼다.)

---

## C. 다음 세션에 착수할 것 (아직 개발 전)

1. `docs/MAIL_SIGNATURE_I18N_DESIGN.md` — 서명·표시이름 다국어 설계 (스키마 + 발신 경로 → **설계 게이트 필수**)
2. B군 26건 (운영 피드백) — 설계 게이트는 불필요하나 **구현 검증은 Fable**. `#258·#280` 팝아웃 핀부터
3. C군 14건 — 설계 게이트부터. 신규 시스템(출퇴근·가져오기·채팅방 링크)은 **개발도 미룬다**

상세 목록은 `.claude/session-state.md`.
