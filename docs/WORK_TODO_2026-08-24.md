# 할 일 목록 — 2026-08-24 (재작성)

운영 미완료 **28건** (전 회차 21 → 신규 7건 접수: #378~#384).
기준: **지금 사용자가 겪고 있는 것 → 일이 막힌 것 → 불편한 것 → 규모 큰 것 → 판단·조사**.

**Fable 규칙:** 돈·보안 경계·마이그레이션·신규 시스템·구조적 결정만 Fable. 나머지는 Opus 직접 검증 + 그 사실 명시.
**Fable 대기 항목은 Opus 가 손대지 않고 `FABLE_VERIFY_QUEUE.md` 에 근거와 함께 표시만 한다.**

---

## ⓪ 오늘(08-23~24) 처리 완료 — 운영 배포 22회

| 건 | 내용 |
|---|---|
| 검색 | 탭 + 검색창이 화살표·사이드바 아래로 깔리던 것(stacking context) → `document.body` 포탈 + 카나리 |
| z-순서 | **모든 팝업·우측패널이 세로 화살표 위로** — 핸들 z-index 900→90 (오버레이 15곳 대신 한 줄) |
| 태그 | 리스트·팝아웃에서 붙이고 떼기(+ 메뉴) · owner 권한 누락 · 태그별 칩 사라짐 · 관리 버튼 통합 |
| 메일 | 라벨로도 검색 · **우리 알림 메일을 통째로 버리던 것**(수집 단계 skip) → 저장 + 확인 권장 + 백필 7건 |
| 가용시간 | 진척 0 → 계획/진척/남은 한 기준 · 민트 복귀 · % 를 바에 부착 · 상단 칩 정리 |
| 리스트 | 열 폭 재배분(제목·담당자 +50px) · 반복 아이콘화 · 상세 반복 배지를 단계 배지 줄로 |
| 근태 | **자동 마감 cron 이 한 번도 동작한 적 없던 것**(DATEONLY→Invalid date) · 자동 출근 안내를 팝업으로 |
| 인증 | **원터치 Google 로그인 개방**(검증 승인 반영) · 회원가입에도 구글 버튼(공용화) |
| 시계 | 워크스페이스 시간이 기준(흰색) · 클릭 시 해당 설정 항목까지 스크롤 |
| 문서 | 편집 버튼을 글자로 승격 · 자동저장 실패로 이동이 막힐 때 사유 노출 |
| 보고서 | **`already exists` 의 진짜 원인**(week_start 형식 불일치로 기존 보고서를 영영 못 찾음) → 작성/수정/보고서 보기 |
| 팝아웃 | 완료 가리기(칩 줄) · **완료가 이번 주에서 사라지던 것**(completed_at UTC vs 주간 KST) · [오늘 내 업무] 버튼 |
| 프로젝트 | 탭바가 헤더에서 떨어지던 것(sticky top 이 음수 마진 무효화) |
| 리뷰 | 오늘의 업무 리뷰 신설 → **스타일 미달로 Fable 이관** |
| 하니스 | 카나리 flaky 근본 수정(`/login` 선방문이 `/dashboard` 로 튕김) |

---

## ① 지금 사용자가 겪고 있다

| 순위 | # | 내용 | 판단 |
|---|---|---|---|
| **1** | **#378** | 문서 본문에 넣은 이미지·파일이 **파일 메뉴에 안 잡힘**. 드래그로 넣으면 **크기 조정도 안 됨**. "통일해서 일반적인 기능으로" | Opus. 인라인 자산 → File 등록은 이미 정책이 있다(`feedback_inline_assets_as_files`) — **구현이 반쪽**인지 확인부터 |
| **2** | — | **Google Drive `invalid_grant`** — 첨부 열기·업로드 실패 중(운영 로그 15회) | **Irene 조치**(재연동). 오늘 OAuth 게시 전환으로 옛 토큰 무효화된 것으로 보임 |
| **3** | **#377** | 메일 상세 **업무 추출 버튼 무반응** + 별표·팔로우 상태가 안 보임 + **전달 시 원문 깨짐** | Opus. 버그(추출·전달)부터 재현 |
| **4** | **#376** | 메일 라벨 `자동 발송 · 업무 관련` 문구가 길어 잘림 | Opus (소) |

## ② 일이 막혀 있다

| 5 | **#375** | 외부컨펌 걸면 **내 업무 리스트에서 사라짐**. "컨펌 완료 시점을 관리하는 게 나니까" | Opus. `weekTaskSet` 술어 — 담당자 축만 보고 **건 사람**을 안 본다 |
| 6 | **#273** | 요청받은 업무에서도 외부컨펌 — 남은 것은 **권한**(컨펌자도 걸 수 있나) | **Fable**(권한 경계) |
| 7 | **#343** | 고객이 자기 워크스페이스를 가진 경우 컨펌 연결 | **Fable**(테넌트 경계) |

## ③ 매일 쓰는데 불편

| 8 | **#384** | 메일 보내고 **답 없으면 그냥 멈춤** — "언제까지 답 없으면 다시 액션" 리마인더 설정 | Opus. 스레드에 `follow_up_at` 1컬럼 + cron. 확인 필요로 승격 |
| 9 | **#380** | 채팅 **이모티콘 전송 + 상대 메시지에 이모지 반응** | Opus. `message_reactions` 테이블이 **이미 있다** — 전송/반응 UI만 |
| 10 | **#383** | Q Note에 **녹음파일 업로드 → 텍스트화** | Opus 가능(STT 배관 있음) · 단 **과금 원장**(`qnote_usage_events`)에 업로드분을 어떻게 셀지는 **Fable** |
| 11 | — | Q info 값 가리기(`***` + 보기/숨기기) | Opus |
| 12 | **#235** | 채팅 업무 자동추출 실동작 검증 + 메일 탭 단위 설정 | Opus |

## ④ 규모 큼 — 설계 선행 (**전부 Fable**)

| 13 | **#381 #382** | **Q sale** — 게스트/영업 단계 고객, 채팅·메일·전화 통합 히스토리, 상담 녹음 → Q Note 연결, 계약 성사 → 서명·프로젝트 생성 | **Fable**. 신규 시스템 + 고객 데이터 모델 확장 |
| 14 | **Context Review** | Action Center / **오늘의 업무 리뷰** / **Project Brief** / 프로젝트 중심 Context 연결 / 상담 Context | 분석 보고 완료(2026-08-24). **Project Brief MVP 는 Opus 가능**, 리뷰 요약 문장화는 **Fable** |
| 15 | **#379** | Google Drive **양방향 동기화**(Drive → PlanQ 역방향) | **Fable**. 외부 상태 동기화·충돌 해소·증분 커서 — 이 저장소가 이미 데인 영역 |
| 16 | **#353 #354 #355 #358** | AI 루틴 설계 모드 · 배관 확장 · 캔버스 초안 품질 | **Fable** |
| 17 | **#227 #230 #233** | Cue 파일 읽기 · Today's 브리핑 · 검색 AI | **Fable** |
| 18 | **#259** | 게스트 링크(공개 표면 + 쓰기 권한) | **Fable** |
| 19 | **#360 #284** | 누적 로그 자리 · 문서→Info 항목별 | **Fable** |
| 20 | **#289 #346** | 공지 자리 + 배포 자동 공지 | Opus 가능(공지 테이블 + 배포 훅) |

## ⑤ 판단·조사

`#211` `#361` 제품 방향(경쟁 분석) · `#299` 노션/슬랙 가져오기 — **전부 Fable**

---

## 후속 과제 (비차단, Opus)

| 내용 | 왜 |
|---|---|
| **시간대 규약 전면 적용** | 오늘 두 건이 같은 뿌리였다(근태 자동마감 · 팝아웃 완료 실종). `completed_at` 외 **보고서·통계·다른 라우트**도 같은 방식으로 워크스페이스 tz 환산 필요 |
| **시간대 가드 신설** | `guard-invariants` 에 "날짜 판정에 브라우저/서버 tz 직접 사용" 래칫. 기존 부채 동결·증가만 실패 |
| `weeklyReviewSnapshot.js:611` 휴가 차감 공식 통합 | 값은 같지만 "같은 값 여러 벌" 의 씨앗 |
| `hold_prev_status` 컬럼 분리 | 보류와 외부컨펌이 한 컬럼 공유 |
| #349 자동 취소에 broadcast·notify | §13·§16 결손 |
| 카운터 재계산 사각 | 파일 0건인데 카운터>0 인 워크스페이스 |

## Fable 대기 큐 (손대지 않고 표시만)

`docs/FABLE_VERIFY_QUEUE.md` — §2-B 가용시간 바 · §2-C 오늘의 업무 리뷰 · §2-D 고정핀 채로 열기
\+ 이번 주 작업 전체 소급 검증(특히 근태 정정·시간대 환산·멀티테넌트 스코프)

---

## 규칙

- 배포로 고친 것은 **답글을 달고** 장부를 닫는다. 답글 없이 닫으면 무시당한 것과 같다.
- Fable 은 돈·보안 경계·마이그레이션·신규 시스템·구조적 결정에만. 나머지는 Opus 자체 검증 + **그 사실 명시**.
- **Fable 필요 항목은 Opus 가 구현하지 않는다** — 큐에 근거와 함께 적어 두고 넘긴다.
