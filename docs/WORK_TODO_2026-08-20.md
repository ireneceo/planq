# 할 일 총정리 — 2026-08-20

운영 피드백 미완료 **92건**(pending 91 + wontfix 1) 전수 + 남은 작업 매칭.
분류 기준은 **Fable 설계가 필요한가** 하나다.

- **A군** — 요구가 명확해 바로 구현. Opus 가 설계·구현·검증까지 한다.
  (구현 검증은 가드/실호출로 직접. 돈·보안 경계에 닿으면 그때만 Fable 로 올린다.)
- **B군** — 무엇을 만들지부터 정해야 한다. Fable 설계 게이트 선행.
  (제품 방향 · 정책 결정 · 보안 경계 · 여러 화면에 걸친 구조)
- **C군** — **설계가 이미 있다.** Fable 을 다시 쓰지 않는다. Irene 승인·답변이나 구현만 남았다.

> Fable 사용량이 한정 자원이다. B군도 **묶어서** 올린다 — 건별로 올리지 않는다.

---

## 0. 먼저 처리 — 고쳤는데 장부가 안 닫힌 것 (3건)

| # | 내용 | 상태 |
|---|---|---|
| #280 | 팝아웃 핀·아이콘·탭 정렬·필터 | 오늘 배포 완료 (`5cebf6d0` 외 팝아웃 8커밋) |
| #301 | 검색 즉시 반응 | 배포 완료 (`360f5e60`) |
| #305 | 검색 전 최근 항목 | 배포 완료 (`360f5e60`) |

→ **답글 달고 닫기.** 답글 없이 닫으면 사용자에겐 무시당한 것과 같다(운영 #276 규칙).
→ #299 의 "한글 입력 안 됨" 도 사용자 확인상 해소(2026-08-20). 단 원인 미규명이라 재발 시 화면을 받아야 한다. 같은 건의 "노션/슬랙 가져오기" 는 B군.

---

## A군 — 바로 구현 (Fable 설계 불필요)

### A-1. Q info 정리 (#316~#334 중 12건) — **한 배치로 묶어서**
`routes/kb.js` batch/ai-ingest/csv-ingest 3경로가 같은 뿌리다. 한 번에 손대는 게 맞다.

| # | 내용 |
|---|---|
| #316 | AI 추가·CSV 가 항목(custom_columns)을 못 만든다 — 표형 자료가 body 덩어리로 저장 |
| #319 | CSV 헤더가 title/body 고정 — 실제 업무 CSV(서비스명·링크·아이디…)를 못 받는다 |
| #320 | 일괄 저장이 다중 카테고리를 덮어쓴다 (전부 manual 로 뭉침) |
| #321 | 가져오기에 중복 검사 없음 — 같은 CSV 두 번이면 2배 |
| #322 | AI 20개·CSV 500행 초과분이 조용히 잘린다 (잘렸다고 알려야 한다) |
| #323 | 정렬 — 날짜만 가운데, 나머지 좌측정렬로 통일 |
| #325 | 목록 카테고리 칩에 사용자 카테고리가 안 나옴 (legacy 6종 하드코딩) |
| #326 | 카테고리 뱃지에 `cat.` 접두어 노출 — 번역키가 화면에 찍힘 |
| #327 | 긴 텍스트 줄바꿈 소실 — longtext 를 한 줄 input 으로 편집 |
| #328 | "리스트에 표시" 토글이 등록 모달에만 있음 — 상세에서 못 바꿈 |
| #329 | 상세 우측 패널 항목 값이 200px 에서 잘림 |
| #331 | URL 항목에 링크가 안 걸림 + 열 좁아지면 주소 잘림 |
| #332 | 본문 없이 항목만으로 등록 불가 (`body_or_attachments_required`) |
| #333 | 인덱싱 상태가 전부 "사용 가능" 인데 실제로 Cue 가 못 씀 (87건 중 82건) |

### A-2. 업무·주간 그래프 (3건)
| # | 내용 |
|---|---|
| #254 · #300 | 리스트 실제/예측 시간과 주간 진척 그래프 숫자가 다름 (2.5h vs 1.5h / 2h vs 4.5h) — **같은 값의 공식이 여러 벌** 계열. 정본 하나로 합치는 것이 선행 |
| #255 | 나의 업무보고에 진척 그래프가 안 나옴 |
| #236(후반) | Q task 리스트 가로가 좁으면 업무명이 가려짐 — 끝까지 보이게 |

### A-3. Q mail (7건)
| # | 내용 |
|---|---|
| #213 | 필터 접기/펴기 + 기본 접힘 |
| #222 | 새 메일 폼에서 리스트 클릭 시 이탈 불가 — 자동저장 + 재열기 시 복원(유저 단위) |
| #260 | 메일 상세 풀버전/팝업 보기 (높이 답답함) |
| #263 | 업무추출 버튼이 안 뜨는 곳 · AI 업무추가 담당자가 "1, 2" 로 표시 · 불필요하게 긴 AI 설명문 |
| #308 | 모두읽음 버튼만 크다 — 필터와 같은 크기·회색 |
| #314 | 다른 탭에서 "답변필요" 로 보내기 |
| #324 | 보낸메일 탭 표시이름이 다름 |
| #261(일부) | 특정 메일이 아예 안 들어옴(Rose Mazlimi 건) — **수신 누락 버그** |
| #220(일부) | 팀메일에서 누가 보냈는지 표시 ("[나]" 만 나옴) |

### A-4. 문서·편집기 (8건)
| # | 내용 |
|---|---|
| #304 | 코드 블록에 붙여넣으면 코드박스가 여러 개로 쪼개짐 |
| #310 | AI 문서 만들 때 연결한 프로젝트가 새 문서에 동기화 안 됨 |
| #311 | 표 열 너비 고정 옵션 (지금은 내용따라 제각각) |
| #312 | AI "다시 만들기" 가 기존 요청·수정 내역을 리셋 — 전 AI 작성에 공통 |
| #336 | 새 문서에서 표 선택 시 제대로 안 열림 |
| #337 | 엑셀 복사 붙여넣기가 이미지로 들어감 — 표로 |
| #338 | 목차 기능 (노션식 상단 목차) |
| #341 | 임시저장 상태에서 저장 시 오류 — 임시저장본이 따로 남음 |
| #342 | 노션 등에서 복사 시 이미지가 미리보기로 안 붙음 |
| #257 | 문서 리스트가 열람만 해도 최신으로 올라옴(수정순이어야) + 제목 2줄 |

### A-5. 화면 공통 (6건)
| # | 내용 |
|---|---|
| #232 | 첨부를 드래그드롭 회색 라운드박스로 **전면 통일** (버튼 형식 전부 찾아서) |
| #287 | 알림 아이콘이 종류와 안 맞음 — 메일/채팅/기타 구분 |
| #306 | 새소식이 우측 패널로 열림 → 알림처럼 아래로 + 모두보기 페이지 |
| #313 | 스크롤 시 탭이 헤더에서 떨어져 올라감(고정 유지) + 공개링크 SNS 미리보기(OG) |
| #339 | 프로젝트 설명이 설정에 있음 — 상세정보로 이동하거나 입력칸을 넓게 |
| #329 | (A-1 참조) |
| #340 | 앱 재시작 시 열려 있던 탭·마지막 위치 복원 |

### A-6. 검증 필요 (요구는 "제대로 되는지 봐줘")
| # | 내용 |
|---|---|
| #267 | 모바일 비디오 첨부 · 첨부 기능 통일 · 드라이브 연동 시 동작 |
| #303 | Cue 가 채팅에 실제로 대응하는지 · 설정 화면이 안 보이는 이유 |
| #302 | 요청받은 업무에서 외부컨펌 버튼이 비활성 — 눌리게 |

---

## C군 — 설계 이미 있음 (Fable 재설계 불필요)

B군으로 잡았다가 `docs/` 확인 결과 **설계가 이미 존재**하는 것들. Fable 을 다시 쓰지 않는다.

| # | 내용 | 설계 문서 | 지금 막고 있는 것 |
|---|---|---|---|
| #208 · #285 | 출퇴근·휴가 관리 | `docs/ATTENDANCE_LEAVE_DESIGN.md` (Fable, 2026-08-18) | **Irene 승인만 남음** — 승인되면 Opus 구현 착수. #285 답도 문서에 있다(밀린 게 아니라 시작된 적 없음) |
| #259 | 고객 무로그인 접근 (게스트 링크) | `docs/GUEST_LINK_DESIGN.md` (설계 초안) | **Irene 답변 6건** — 문서 §12 미결 질문. 답만 주면 구현 |
| #347 · #348 · #349 | 정기업무 (RRULE·상속·미수행 skip) | `docs/RECURRING_AUTOMATION_SPEC.md` | 신고 3건이 스펙 대비 **미구현/결함**인지 대조 먼저. 대조는 Opus 가 한다 |
| #316 · #319~#322 | Q info AI/CSV 가져오기 | `docs/KB_AI_INGEST_DESIGN.md` (2026-05-05) | 설계는 있고 **구현이 설계를 못 따라간 것**. A-1 배치로 처리 |
| #284 | 문서 → Info 항목별 정리·자료 연결 | `docs/Q_INFO_DOCS_INTEGRATION_DESIGN.md` | 설계 있음. A-1 뒤에 붙이면 됨 |
| #227(일부) | Cue 대화형 실행 | `docs/CUE_CONVERSATIONAL_EXECUTION_DESIGN.md` (#81, 선행조건 전부 완료) | 우측 패널 배치·파일 읽기 범위만 새로 정하면 됨 |

→ **B군에서 빠진 것**: 출퇴근·휴가, 게스트 링크, 정기업무 3건, Q info 가져오기, 문서→Info.
→ B군에 남는 것은 아래 목록에서 위 항목을 뺀 나머지다.

---

## B군 — Fable 설계 선행

### B-2. 제품 방향 (2건)
| # | 내용 |
|---|---|
| #211 | 업무대행·에이전시·고객서비스 B2B 그룹웨어로서 부족한 것 / 탁월한 것 검토 의견 |
| #299(일부) | 노션/슬랙 등 타 솔루션 데이터 가져오기 · 일반 파일 DB화 |

### B-3. AI·Cue 확장 (4건)
| # | 내용 |
|---|---|
| #227 | 워크스페이스 파일을 Cue 가 읽고 답하기 + Q helper 채팅창을 채팅·메일 **우측 패널**로 |
| #230 | Today's 브리핑 (메일·채팅·이슈 요약을 상단 접이식, 컨설팅 수준) |
| #233 | 통합 검색에 AI — 자료 검색 + 결과 텍스트 + 페이지/메뉴 이동 |
| #235 | 채팅 업무 자동추출 실동작 검증 + **메일 탭 단위 자동추출 설정** |

### B-4. 업무 워크플로 구조 (4건)
| # | 내용 |
|---|---|
| #236 · #250 · #309 | **업무 태그** — 태그 부여, 태그 기준 정렬, 팝아웃 리스트 표시, 퀵애드에서 필터 기준별 옵션(태그/프로젝트/마감일) |
| #271 · #307 | **업무 결과물 버전 관리** — 확인요청 시 메시지, 수정요청 후 버전 올려 재제출, 히스토리 연결. 지금은 결과물 1개라 댓글로 주고받음 |
| #273 · #302 | 요청받은 업무에서도 외부컨펌 사용 (#302 의 버튼 비활성은 A군, 정책은 여기) |
| #343 | 고객이 자기 워크스페이스를 가진 경우 컨펌 요청이 그쪽으로 연결 |

### B-5. 보안·공개범위 (4건 — Fable 게이트 필수)
| # | 내용 |
|---|---|
| #317 | Q info AI/CSV 일괄 저장이 **항상 워크스페이스 공개(L3)** — 자격증명이 팀 전체에 노출 |
| #318 | 자격증명이 **임베딩·번역으로 외부 LLM 에 전송** + secret 항목 값을 꺼낼 방법이 없음 |
| #334 | 통합검색이 secret 값까지 훑어서 검색됨 |
| #330 | 비밀번호 항목에 가리기 + 보기/복사 (q_records 에는 이미 있음 — 그 패턴 이식) |

### B-6. 운영·공지 (2건)
| # | 내용 |
|---|---|
| #289 · #346 | 워크스페이스 스피커 공지를 **어디서 올리는가** — 플랫폼 관리자에 메뉴 없음. 배포 시 업데이트 내역 자동 공지 |
| #225 | 문서·노트를 워드/PDF/엑셀로 내려받기 (범위가 넓어 형식별 우선순위부터) |

### B-7. 메일 정책 (3건)
| # | 내용 |
|---|---|
| #344 | 특정 메일을 답변필요/확인권장으로 보내는 **학습** + 중요표시 |
| #345 | 메일 보관 기능 + 태그별 보기 |
| #261 · #220 | 주소 기준 리스트·블록·복사·검색·새 메일·고객 저장 등 메일 기본 기능 묶음 |

---

## 권장 순서

0. **Irene 결정 2건** — 출퇴근·휴가 설계 승인 / 게스트 링크 미결 질문 6건. 둘 다 답만 주면 구현으로 넘어간다.
1. **0번(장부 3건)** — 답글 + 닫기. 10분.
2. **A-1 Q info 12건** — 한 배치. `routes/kb.js` 3경로가 공통 뿌리라 같이 고치는 게 싸다.
3. **A-3 Q mail** 중 #261 수신 누락 — 메일이 안 들어오는 건 데이터 손실이라 우선.
4. **A-2 그래프 숫자** — 정본 공식 하나로 통합.
5. **B-5 보안 3건** — 자격증명이 지금 팀 전체에 보이고 외부 LLM 으로 나간다. B군 중 가장 급하다.
6. 나머지 A군 → B군 순.

## 규칙 (오늘 확정)

- Fable 은 **돈·보안 경계·마이그레이션·신규 시스템** 에만. 나머지는 Opus 가 직접 검증하고 **"Fable 미검증(자체 검증)"** 을 명시한다.
- 같은 기능의 후속 수정은 **묶어서 한 라운드**로 올린다. 건별로 올리는 것이 소모의 주범이었다(2026-08-20 팝아웃 하루 8회).
- 배포는 Irene 의 명시 지시로만.
