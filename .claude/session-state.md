## 현재 작업 상태
**마지막 업데이트:** 2026-08-30
**작업 상태:** 운영 배포 2회 완료 · 커밋 5건 · 미배포 0건

### 진행 중인 작업
- 없음 (다음: #383 Fable 설계 게이트)

### 완료된 작업 (이번 세션)

**모바일 긴 제목 잘림 (Irene 신고) — 배포됨**
- 메일 상세 제목이 헤더 밖으로 흘러 **앞뒤가 잘리고 아래 버튼을 덮던 것**.
  원인은 서로 다른 날 쓰인 두 규칙의 충돌 — 제목은 `white-space: normal`(줄바꿈 허용)인데
  Bar 는 `height: 56px` 고정(2026-08-25). 나중에 잠근 쪽이 이겨 넘칠 곳이 없었다.
  실측: 제목 112px / 헤더 56px → **+27px 겹침**, 가운데 세 줄만 보였다.
- 수리(Fable 판정): Bar 폰 규칙 `height` → `min-height`, 제목 2줄 clamp + 탭하면 전체 펼침.
  **목록 행의 한 줄 말줄임은 정상 설계로 유지** — 목록은 훑는 화면이고 긴 제목은 열어서 읽는다.
- 조사 중 발견: **Q docs 상세 제목이 한 글자씩 세로로 쌓이던 것** — 액션 8개가 375px 를 다
  먹고 flex-shrink:0 이라 제목 칸이 **0px**(폭 15px/높이 202px). Bar 에 flex-wrap 추가.
  ★ 제목 접기는 **순수 문자열일 때만** — Q docs 는 제목 안에 링크가 있어 접으면 그게 죽는다.

**메일 알림 딥링크 — 배포됨**
- 알림 링크에 **폴더가 안 실려** 확인권장 메일을 눌러도 "답변 필요" 탭에 서 있었다.
  classify 축 그대로 `?folder=` 를 실어 보낸다.
- 상세가 안 열리던 것: **근본 원인 미확정.** 발송·payload·sw.js·NativeBridge 전 경로가
  코드상 정상이고 dev 재현 실패. Capacitor 네이티브 위장으로 아이폰 부팅 경로까지 재현했으나
  그 조건에서도 정상 동작 — "루트는 공개 표면이라 chrome nav 가 no-op" 가설은 **증명 안 됨**.
  → 원인 특정 대신 **조용히 실패할 수 없게** 만들었다. 이동이 목적지에 도달했는지 확인하고
  안 됐으면 전체 로드로 착지. ★판정은 "주소가 바뀌었나"가 아니라 "목적지에 있나" —
  아이폰은 그 사이 /inbox 로 리다이렉트돼 주소가 실제로 바뀌기 때문(첫 구현의 구멍).

**글씨 크기 키우기 (#Irene 요청) — 배포됨. Fable 설계·구현 게이트 2회 통과**
- 전환 전 실측: rem **0곳**, font-size 전부 px. ≤11px 26.9% / ≤13px 62.5% / 크기 20종 이상.
  OS·브라우저 글꼴 설정이 이 앱에만 안 먹었다. Fable 판정 **부적정**.
- px→rem **4,022곳 / 314파일** + 루트 배율 `html { font-size: calc(16px * var(--planq-font-scale,1)) }`
  + 설정 3단(1.0/1.15/1.3, **localStorage 기기별** — 폰과 데스크탑은 필요 크기가 다르다).
- 세트로: viewport `maximum-scale=1.0` 제거(안드로이드 핀치줌 복원) + 폰 입력칸 16px 하한.
- codemod 가 못 잡은 것을 손으로: 동적 11곳 · **PlanQSelect(셀렉트 값 전부 13px 에 멈춰 있었다)**
  · 확장자 뱃지 삼항 · theme.ts 7값.
- 가드 신설 **fontpx** — 베이스 0 fail-closed. 6형태를 하나씩 따로 심어 전부 FAIL 확인.

### ⚠️ 남은 것 / 정직하게 적는 것

1. **메일 알림 근본 원인 미확정** — 폴백으로 막아둔 상태. 아이폰에서 또 안 열리면
   폴백 이전 단계(탭 이벤트 자체가 앱에 안 닿는 것)로 좁혀야 한다.
2. **iOS 자동확대 실기기 미확인** — Puppeteer 재현 불가. 대신 유발 조건을 결정적으로 쟀다:
   24화면 · 모달 24개 · 입력칸 124개 **전부 ≥16px**. 조건 불성립이므로 발생하지 않아야 한다.
3. **Q docs 액션 "템플릿으로 저장" 버튼이 화면 밖** — 모바일에서 누를 수 없다. 기존 결함,
   이번 절단면 밖으로 남김.
4. **메일 자동추출은 전 계정 `off`(기본값)** — 켜면 업무 후보가 실제로 생성돼 Irene 계정
   데이터에 영향. 임의로 켜지 않았다.

### 다음 할 일

- **#383 Q Note 녹음파일 업로드** — Fable 설계 게이트. ★전제가 바뀌었다: ffmpeg 이 없어
  길이를 못 잰다던 진단은 **절반 틀렸다.** q-note venv 에 `soundfile`(libsndfile 1.2.2)이
  이미 있고 **헤더만 읽어 0.8ms** 에 길이를 준다(실증). WAV·MP3·FLAC·OGG 등 26종 즉시 가능.
  **M4A/AAC 만 미지원** — 아이폰 음성 메모가 m4a 라 이 구멍이 핵심. MP4 `mvhd` 헤더 파싱
  (순수 파이썬 ~40줄)이 남은 조각.
- **#382·#381 Q Sale** — 신규 시스템이라 Fable 설계 게이트
- 남은 운영 피드백 19건 (#379 #378 #361 #360 #358 #355 #354 #353 #343 #273 #259 #235 #233 #230 #227 #211)

### 🔑 Irene 만 할 수 있는 것

| 항목 | 내용 |
|------|------|
| **Android** | Play Console $25 등록 → **앱 서명 키 SHA-256 지문**을 주면 assetlinks 치환 + 워크플로 + release 서명을 한 번에 붙임 |
| **iOS 심사** | App Store Connect → 「배포」 맨 아래 **App 심사 정보**에 데모 계정 입력 (`appreview@planq.kr` / `PlanQReview!2026` — 이미 만들어 둠) · 스크린샷 6.9" · App Privacy 설문 · 카테고리/연령등급은 「App 정보」 |
| **Google Drive** | Drive 전용 GCP 프로젝트 분리 + Restricted `drive` scope 심사 (4~8주) |

> ⚠️ Apple Developer 계정은 **help@wor-pro.com** — 2단계 인증 6자리가 그 메일함으로만 온다.

---

## 복구 가이드

새 Claude 세션 시작 시 아래 내용을 붙여넣으세요:

```
이전 세션 이어서 작업하고 싶어.
/opt/planq/.claude/session-state.md 읽어줘.
```
