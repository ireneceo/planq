## 현재 작업 상태
**마지막 업데이트:** 2026-09-17 UTC
**작업 주체:** [Claude Code] / [Opus]
**작업 상태:** 배포 완료 (v1.52.9) · **운영 백필 2종 미실행** · Fable 미검증분 있음

### 진행 중인 작업
- 없음 (아래 «사람이 해야 할 것» 이 선행)

### 사람이 해야 할 것 (막혀 있음)
1. **운영 백필 2종** — 권한 정책(공유 리소스 변경)이 막아 Claude 가 실행 못 함. **순서 중요**
   ```
   ssh irene@87.106.78.146 'cd /opt/planq/backend && node scripts/recall-personal-mirrors.js --apply'
   ssh irene@87.106.78.146 'cd /opt/planq/backend && node scripts/recount-storage-usage.js --apply'
   ```
   - 회수 대상 **38건**(개인 L1 12 · 삭제됨 24 · 프로젝트 없는 L2 2). **이걸 안 돌리면 개인 파일이
     Drive 공유 폴더에 그대로 있다** — 코드 수리는 «앞으로» 를 막은 것이고 기존분은 이 스크립트가 거둔다.
   - 재계수: biz1 588개/209.3MB → 614개/240.0MB (DB 카운터만)
2. **Drive 원본 2건(id 10·11, 개인 등급)이 공유 폴더 직계에 있다.** 스크립트가 못 지운다 —
   사본이 아니라 파일 자체다. **[파인더 공유] 를 켜기 전에** Drive 에서 옮기거나 등급을 바꿀 것.
3. **개발 전용 구글 계정** — 지금 dev 와 운영이 **같은 Drive** 를 본다(미해결 ⑬).
   Fable 이 추가 Drive 라운드의 선결 조건으로 걸었다. #417·#418·#419 가 여기 막혀 있다.
4. **메일 목록 발신자 12px → 15px 여부** — 제목은 이미 15px. #283 에서 의도적으로 내린 값이라
   임의로 안 바꿨다.

### 이번 세션에 한 것 (2026-09-17)
- `cf0e11af` 문서 목록 제목 폰 13→15px(데스크탑도 규격 14px) + 가드 `listrowtitle` 신설 — **Fable 10차 PASS**
- `3547a5e7` Fable 10차 비차단 9건 수리 — **Fable 11차 FAIL(차단 2)**
- `6dbffa40` 11차 FAIL 7건 수리 — **Fable 미검증(자체 검증)**. 장부: `docs/FABLE_GATE_QUEUE.md`
- `7f54e3b9` 개발현황 → **배포 v1.52.9** (EXIT 0, 운영 health·CSP·PDF OK)

핵심: «이 바이트를 아직 누가 쓰고 있는가» 공식이 **여섯 벌**이었고, File(절대경로) ↔ 첨부(상대경로)라
**비교가 한 번도 참이 되지 않았다**. 운영 채팅첨부 48건 전부 상대경로 = 전부 보호 밖이었다.
`services/fileRefs.js` 한 술어로 모았고, 운영 산 파일 **7건**이 처음으로 보호받는다(실제 삭제 피해는 0).

### 다음 할 일
- [사람] 위 1~4
- [Claude Code] 미처리 운영 피드백 12건: #407 · #411 · #412 · #413 · #414 · #415 · #416 ·
  #417 · #418 · #419 · #381 · #382 (#420 은 이번 배포로 해소, 운영 번들 확인함)
- [Claude Code] 다음 Fable 라운드에 묶을 것은 `docs/FABLE_GATE_QUEUE.md` 맨 아래 5개 항목

### 알아 둘 것
- dev 에 `canary-trash-*` 픽스처 **65행**(2026-08-29, 다른 카나리) 잔존 — 소프트 삭제라 화면엔 없지만
  dev biz5 사용량 드리프트(-91개)의 일부다. `--suite drivemirror` 에는 시작 시 자기 흔적을 쓸어내는
  장치를 넣었다.
- 같은 서버 POS 세션이 MySQL 전역 커넥션을 물면 게이트·카나리가 `Too many connections` 로 실패한다.
  코드 결함이 아니다 — 비면 재시도한다(이번 배포 1차 중단 사유).
