# PlanQ 세션 상태

**마지막 업데이트:** 2026-09-06 15:30 UTC
**작업 상태:** 완료 · **운영 배포 2회 끝남** · 가드 3축 통과
**운영 최신 커밋:** `e1712142`

---

## ✅ 오늘 운영에 나간 것 (배포 2회 · 커밋 7건)

| 배포 | 커밋 | 백업 |
|---|---|---|
| 15:05:36 (289s) | `ae05e118` 외 6건 (인계 대기분 포함) | `/opt/planq/backups/20260906_150536` |
| 15:22:32 (288s) | `e1712142` | `/opt/planq/backups/20260906_152232` |

배포 후속도 **끝났다** — 운영 위키 시딩 완료(카테고리 15 · 아티클 57 · 임베딩).
**스키마 변경 없음**(마이그레이션 전부 "변경 0 · 멱등 확인").

### 상세 헤더를 두 밴드로 통일 (`ae05e118`)
Irene 신고 셋이 한 뿌리였다 — "메일 상세에 흰 여백만 많고 내용은 적게", "2줄에서 끝날 방법
없어?", "노트는 틀어지네". **흰 여백은 렌더러 문제가 아니었다** — 운영 메일 #3401 을 앱과 같은
렌즈로 재니 프레임이 내용을 정확히 따라갔고 잉여는 어느 폭에서나 86px 고정(발신 템플릿 여백).
정체는 **헤더가 197px 를 먹은 것**이었다.

- 규격을 하나로: `PanelHeaderBar`(밴드1 60px) export + `DetailMetaBar/Left/Right`(밴드2 57px) 신설.
  Q Mail · Q docs · Q Note(음성·메모) 가 **같은 컴포넌트를 상속**한다.
- 신규 `components/Common/ChipPopover.tsx` + `popoverAnchor.ts`(좌표·Esc·포커스 공용).
- 실측: 본문 시작 y **Q Mail 197 → 117** · Q docs 157 → 117(보기·편집 동일).
  좌우 밑줄 세 화면 **190 / 247** 일치. 메일 폴더 탭 43→57 · Q Note 검색줄 61→57 ·
  Q Talk 좌측 리스트 61→60 (**데스크탑 한정** — 폰은 이을 상대가 없고 #406 회귀가 돌아온다).

### 구글 로그인 — 앱이 없어도 끝난다 (`ae05e118` + `e1712142`)
"앱으로 돌아가기 버튼 누르면 웹으로 가서 그냥 dns 에러." 복귀 페이지의 출구가 `planq://`
**하나뿐**이었다. 안드로이드는 Play 심사 중이라 설치본이 없어 브라우저가 `planq` 를 호스트로
읽고 ERR_NAME_NOT_RESOLVED. **로그인은 서버에서 이미 끝났는데 세션만 못 받는 상태.**

- `GET /api/auth/google/web-return?code=` — 같은 일회용 code·같은 단일사용 원장. 세션 심고 `/inbox`.
- **자동 이동** — 스킴 시도 후 1.8초 뒤에도 화면이 보이고 포커스를 쥐면 웹으로 넘어간다.
  (1차 수정은 폴백을 "1.6초 뒤 뜨는 보조 버튼" 으로 둬서 Irene 이 같은 큰 버튼을 다시 눌렀다 —
  **고쳐 놓고 닿지 못하게 만든 것**. 버튼 순서도 뒤집어 기본 = 웹.)
- `oauth_error` 를 로그인 화면이 **읽는다**(읽는 곳 0곳이었다) + OAuth 실패 서버 로그
  (운영 pm2 에 흔적 0줄이었다. `utils/oauthLog` 는 있었고 로그인 경로만 안 쓰고 있었다).

---

## ⏭ 다음 세션이 할 것

### 1. 안드로이드 구글 로그인 — **Irene 재시도 결과 확인부터**
자동 폴백이 배포됐다. 그래도 실패하면 이번엔 로그가 있다:
```bash
ssh irene@87.106.78.146 'grep -iE "auth/google" ~/.pm2/logs/planq-prod-backend-error.log | tail -20'
```
★ **미해명 하나** — 운영 `refresh_tokens` 의 안드로이드 접속은 전부 일반 Chrome(`client_kind='web'`)
인데 네이티브 복귀 페이지가 떴다. 가설: Custom Tab 이 Chrome 과 쿠키를 공유해 `oauth_native`(10분)
가 남는다. 이제 분기 판단이 UA 와 함께 기록되므로 다음 발생 때 확정할 수 있다.

### 2. 운영 옛 프론트 청크 정리 (급하지 않음)
```
전체 33,220개 506MB · 살아있음 268개 5MB · 안 쓰임 32,952개 501MB
디스크 72% (67G 여유)
```
★ **인계 노트의 "index.html 참조 기준" 을 그대로 쓰면 앱이 깨진다** — index 가 직접 참조하는 건
**61개**뿐이고 나머지 화면은 lazy 라 JS 안에서 문자열로 참조된다. 동적 import 를 **재귀로** 훑어야
268개가 나온다. → **직전 N회 배포분을 남기는 스크립트**로 만들 것(백업 디렉터리들의 index.html 도
같이 훑어 유예 집합 구성). 지금 지우면 옛 index 를 캐시한 사용자가 404 → 강제 새로고침.

### 3. Q Talk 가운데 채팅 헤더 65px — Irene 답 대기
부제가 헤더 **안에** 쌓여 CLAUDE.md 금지 항목("헤더 안에 여러 줄 쌓기 금지")에 걸린다.
고치면 좌 60 / 중 60 / 우 60 으로 세 패널 밑줄이 다 이어진다. 팝아웃(`$embedded`)·모바일 채널
줄이 얽혀 있어 신고 없는 화면을 배포 직전에 건드리지 않았다.

### 4. Q Note 메모 상세의 기능 밴드 2개 — Irene 답 대기
헤더 2밴드 아래 "AI 요약"(64px)·"업무 추출"(84px) 이 본문을 y=265 까지 민다. 접으면 148px 를
돌려받지만 진입점이 한 단계 숨는다 — **기능을 숨기는 결정이라 임의로 정하지 않았다.**

---

## ★ 이번 세션의 정책 변경 — Fable 기준

Irene: *"fable은 꼭 필요한 부분에만 사용해. 남용하지마. 이거 가이드로 잡아"* →
*"복잡하고 구조화 필요한 검증과 판단, 고품질 결과가 중요한 것에 한해 조건부로."*

**CLAUDE.md 정본을 "전 설계·검증 무조건" → 조건부 판정식으로 바꿨다:**
```
R 가역성 : 되돌릴 수 없나 (돈·삭제·외부발송·운영스키마·무인증 표면)
S 구조   : 무엇을 검증할지부터 설계해야 하나
F 반증   : 내가 만들 기계 검사로 참/거짓이 갈리나

Fable 에 올린다 ⟺ R=1 OR (S=1 AND F=0)
내가 검증한다   ⟺ R=0 AND (S=0 OR F=1)
```
★ 같은 지시가 2026-08-20 에도 있었는데 **CLAUDE.md 가 "무조건" 이라 두 번 무력화됐다.**
메모리와 정본이 어긋나면 **정본부터 고칠 것.**

---

## 이번 세션에 내가 틀린 것 (같은 실수 반복 금지)

1. **폭만 재고 "보인다" 고 보고했다** — Q Note 폰 제목 rect 176×24 인데 부모 높이 0 +
   `overflow:hidden` 이라 **한 픽셀도 안 그려졌다.** 화면은 좌표 + **가시성**(조상 클리핑 +
   `elementFromPoint` + 스크린샷)까지 잰다.
2. **검증 축을 데스크탑에만 뒀다** → 폰 회귀 3건 유출. 화면이면 폰·태블릿·데스크탑 전부.
3. **주석에 거짓을 썼다** — "칩은 span" 이라 적었으나 `VisibilityChip` 은 `styled.button`.
   넓은 선택자(`> button`) 대신 **묶음으로 범위를 좁힌다**(`MetaActions`).
4. **정규식으로 코드를 지우다 인접 정의를 삼켰다**(`CtrlBtn`). `tsc` 가 잡았다.
   지운 뒤 **HEAD 와 export 목록 전수 대조** 할 것.
5. **styled 주석에 백틱** → 템플릿이 끊겨 `TS1109`. (`feedback_styled_comment_backtick`)
6. **내 검사기가 두 번 거짓말했다** — 패널 선택자가 껍데기를 주움 / `overflow:visible` 인
   0×0 래퍼를 "잘림" 으로 셈. **검사기를 고쳤으면 양성 대조군으로 증명할 것.**
7. **경과 시간을 잘못 읽고 "부분 배포" 라고 단정했다.** `date` 로 실제 시각을 확인할 것.

---

## 복구 방법

```
이전 세션 이어서 작업하고 싶어. /opt/planq/.claude/session-state.md 읽어줘.
```
