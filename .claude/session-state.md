# PlanQ 세션 상태

**마지막 업데이트:** 2026-09-04 13:20 UTC
**작업 상태:** 완료 (v1.48.11 운영)

배포 4회 — `20260904_112429`(v1.48.8) · `121508`(v1.48.9) · `124712`(v1.48.10) · `130xxx`(v1.48.11)

---

## 진행 중인 작업
- 없음

---

## 완료된 작업 (이번 세션)

### 운영 피드백 14건 → 3건
| # | 내용 |
|---|---|
| #403 | 반복 업무 적용 범위를 **목록 편집에서도** 묻는다 (판정을 `utils/taskSeries.ts` 한 곳으로) |
| #404 | 업무 첨부를 앱 안에서 연다 (공용 `AttachmentPreviewDrawer`) |
| #405 | 탭을 워크스페이스별로 보관 (`planq_tabs_v1::<범위>`) |
| #406 | 셀렉트 16→13px · 헤더 아이콘 44 통일 · 메일 상단 3줄→2줄 · 설치 배너 78→58px |
| #360 | Q docs 표 진입로 (`?kind=table` + `meta.tableCount`) |
| #343 | 확인필요 화면·백엔드가 이미 크로스 워크스페이스임을 확인, 회신 |
| #233 · #353 · #379 | **이미 구현돼 있던 것**을 실측 확인 후 닫음 |
| #211 · #361 | Fable 시장·경쟁 분석 회신 후 닫음 |

### 앱 아이콘 배지
- **규칙 원복** — 인박스(확인 필요) + Q Talk. 내가 임의로 알림 수로 바꿨던 것을 되돌림
  (`routes/notifications.js`·`public/sw.js` 는 `3679ce3d` 와 **diff 0**)
- **진짜 결함 수정** — 앱을 열 때마다 카운트 도착 **전에** `clearAppBadge()` 가 먼저 불렸다.
  대조군으로 확정(가드 빼면 `clear`→`set 35`, 있으면 `set 35`)

### 보관기간 이행 (Fable 설계 게이트)
- `services/retentionPolicy.js` 단일 원천 — 요금제 값을 코드가 읽는다
- **래칫 업**: 기록 시점 약속과 현재 플랜 중 긴 쪽 → 다운그레이드 절벽 원천 차단
- fail-closed: `getPlan()` 폴백 미사용, 모르면 보존, 건너뛴 사유 집계
- 1단계 삭제 0 → 운영 리포트 2회 0건 확인 후 `RETENTION_PURGE_APPLY=1` **켬**
- 운영 백필: 감사 222건 귀속 복원(post 212·task 10) · 스탬프 967 · NULL 226→4
- 운영 실측: pro 90일/3년 · enterprise 365일/7년 · starter 14일/90일

### 그 밖
- **휴지통 복원 온전성** — Q info 청크·Q docs 첨부를 휴지통행 때 지우던 것을 영구삭제로 이동
- **`paginatedResponse` 가 추가 키를 버리던 것** — `retention_days` 가 한 번도 안 나갔다
- **운영 FCM 켬** — 구글 인증 통과, 안드로이드 알림 나감
- 위키·방침 문구를 코드에 맞춤 (30일 단언 3곳 제거)

---

## 다음 할 일

1. **안드로이드 링크 열기 마무리** — Play Console → 테스트 및 릴리스 → 설정 → **앱 서명** →
   "앱 서명 키 인증서" 의 **SHA-256** 을 받아 `node scripts/android-set-cert.js <지문>` → 빌드 → 배포.
   **업로드 키 지문이 아니다** (넣으면 링크가 조용히 안 열린다).
2. **iOS 배포 범위 결정** — TestFlight 유지 vs App Store 정식. 런북의 2026-08-24 결정을 바꿔야
   "고객이랑 똑같게" 가 된다. 안드로이드는 정식 경로로 갔다.
3. **Fable 권고(#211) 착수 우선순위** — ①도달(운영 서명 0건·게스트링크 1건: 고객 대화방 첫 진입에
   "링크 보내기/확인 받기" 기본 동선) ②시간→청구 경로(단가 컬럼이 청구에서 0줄 읽힘) ③고객용 외부
   프로젝트 페이지(설계 완료, 결정 3건 대기) ④첫응답시간 지표
4. **posts/kb 만료 후 자동 영구삭제** — 복원 결함을 고쳤으니 이제 안전하게 얹을 수 있다
5. **Q Sale** (#381 · #382) — 별도 사이클

---

## 지금 운영 상태 (실측)

- v1.48.11 · 헬스 OK · `RETENTION_PURGE_APPLY=1` (현재 삭제 대상 **0건**, 매일 04:00 UTC)
- FCM 설정 완료 · 미처리 피드백 3건(전부 Q Sale 계열)

---

## 복구 가이드

새 Claude 세션 시작 시 아래를 붙여넣으세요:

```
이전 세션 이어서 작업하고 싶어.
/opt/planq/.claude/session-state.md 읽어줘.
```
