# PlanQ 세션 상태

## 현재 작업 상태
**마지막 업데이트:** 2026-09-02 01:14 UTC
**작업 상태:** 완료 (개발완료 절차 수행) · Fable 게스트 링크 구현 검증만 대기
**HEAD:** 아래 개발완료 커밋 (미커밋 0)
**게이트:** health 37/37 · 불변식 28/28 · e2e tenant 0실패 · 위키 커버리지 통과 (dev·운영 반영)

---

## 이번 세션에서 운영 배포한 것

| 건 | 내용 |
|---|---|
| **#378** | 본문 이미지 전체 — Q info 404 수정 · 크기조절 4화면 통일 · Drive 동기화 통일 · dedup 중복 미러 차단 · **메일 본문 이미지(CID 첨부)** |
| **#402** | 답장한 메일이 확인권장으로 자리만 옮겨 앉던 것 |
| **#358** | AI 캔버스가 일반론 워크스트림·지표를 만들던 것 (근거 없으면 안 만들고 이유를 말함) |
| **#353 ⑤** | AI 가 매긴 중요도가 저장 자리가 없어 매번 버려지던 것 (`tasks.priority_level`) |
| **보안 Stage 0** | 무인증 이미지 경로 4곳 — `editor-image` 가 DB 를 아예 안 보던 것 · rate-limit 버킷 교정 |
| **보안 Stage 1** | 이미지 전용 쿠키 발급 + would-deny 계측 (게이트는 Stage 2) |
| **개인메일** | 개인메일에 붙인 그림이 팀 파일 목록으로 새던 것 |
| **#259 1단계** | 게스트 링크 — **플랫폼 킬스위치 OFF 상태로 배포**(표면 닫힘) |

**장부 닫음:** #378 · #402 · #355 · #358 · #353
**답글만 달고 열어둠:** #360 (정보 구조 결정 필요)

---

## 지금 대기 중인 것

1. **Fable 게스트 링크 1단계 구현 검증** (진행 중)
   - 통과 시 운영 `platform_settings.guest_links_enabled = true` 로 켜면 열린다
   - **켜는 것은 Irene 판단** — "링크를 아는 사람 = 그 고객" 보장이 없다는 전제를 받아들인 기능
2. **메일 본문 이미지 실수신 육안 확인** (Irene)
   - `irene@irenewp.com` 으로 확인 메일 발송·왕복 도착 확인됨(21:03, `cid:` → 수신측 `data:`)
   - 남은 것: Gmail 화면에서 **"이미지 표시" 를 누르지 않고** 보이는지
3. **보안 Stage 2** — 운영 7일 관측 후 판단
   - 기준: `viewer=<숫자>` 0건 + `has_session=1 viewer=anon` 0건 + 나머지 anon 건별 설명 가능
   - 집계: `grep '[imageGate:would-deny]'` → `(route,file,viewer)` distinct

---

## 다음 할 일 (우선순위 순)

1. **게스트 링크 킬스위치 ON 판단** (Fable 통과 후, Irene 결정)
2. **#360** — 누적 로그를 어디에 담을 것인가. 정보 구조 결정이 먼저(제안 3안 중 택1)
3. **#343 · #233 · #211** — Irene 이 "fable이 검토해줘" 라고 적은 설계 판단 건
4. **#382 · #381 · #379 · #361** — 기획·시장분석이 먼저
5. **게스트 링크 2·3단계** — 승격 claim · 게스트 이미지 업로드 · 게스트 소켓 ·
   `clients.stage`·`phone` · 공개 상담 문 · Q Note `client_id` · 카카오 알림톡
6. **별건으로 남긴 것**
   - `UISPEC` 가드 모순 — 라벨은 토큰 `36/40/44` 인데 정규식 `4[0-3]` 이 40 을 위반으로 센다.
     기존 코드에 40px 46곳이 베이스라인에 잡혀 있다. **내 코드를 통과시키려 가드를 고치지 않았다.**
   - `/api/files/public-image` 가 `security_level`·`vlevel` 을 무시하는 **기존 노출**(Stage 2 대상)
   - SVG 인라인 서빙(운영 4건) — 지금은 CSP 가 막지만 완화 시 XSS
   - richresize·imgresize 카나리가 매 실행 파일을 남김
   - `resolveAttachments` 가 Drive 저장 첨부를 `fs.existsSync` 로 조용히 떨굼

---

## Irene 이 답해야 열리는 것

**카카오 알림톡 발신 명의** — PlanQ 단일 채널인가, 워크스페이스마다 자기 채널인가.
채널 개설·발신프로필·템플릿 심사·대행사 계약이 사업자가 하는 일이라 코드로 정할 수 없다.
게스트 링크 **3단계** 항목이라 1·2단계는 막히지 않는다.

---

## 이번 세션에서 배운 것 (반복 방지)

- **한 방향만 반증하면 반대 방향이 뚫린다.** 이미지 토큰이 일반 인증에서 통했다 —
  "일반 토큰이 이미지 자리에서 거부되는가" 만 쟀고 그 반대를 안 쟀다. 열쇠를 갈라 고쳤다.
- **주석이 거짓말하면 다음 사람이 확인하지 않는다.** "드래그아웃과 같은 술어" 라고 써놓고
  실제로는 갈라져 있어 남의 개인 파일이 메일로 나갈 수 있었다.
- **백엔드에만 넣고 화면 목록에 빠뜨리면 도달할 수 없다** (시리즈 중요도).
- **sync-database 는 모델에 없는 컬럼을 DROP 한다.** 마이그레이션 주석에 그렇게 써놓고 당했다.
- **가드는 스냅샷을 본다** — 컬럼을 만들면 `node scripts/dump-schema.js` 를 같이 떠야 한다.
- **`curl -I` 는 HEAD 다** — GET 게이트를 재면 거짓 실패가 난다.
- **동시 빌드는 SIGTERM 으로 죽는다** — 같은 서버에서 POS 가 Playwright 를 띄운다.
  배포 전 `free -m` 확인. 백엔드 전용 커밋이면 `--skip-build`.
- **배포 exit 0 을 믿지 말 것** — 143(SIGTERM)로 죽어도 알림은 완료로 보인다.
  착지 3점(헬스·PM2 uptime·청크 시각)으로 확인.
- **프로세스를 죽일 땐 PID 로.** `node -e` 패턴으로 훑었다가 POS 프로세스를 건드렸다(무해했으나 실수).

---

## 복구 방법

새 세션에서:
```
이전 세션 이어서 작업하고 싶어.
/opt/planq/.claude/session-state.md 읽어줘.
```
