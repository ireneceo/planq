## 현재 작업 상태
**마지막 업데이트:** 2026-09-14
**작업 상태:** 완료 (운영 배포 `55c90dfa` · **v1.52.1** · 09:49 · 353초 · 백업 `/opt/planq/backups/20260914_094402`)

### 진행 중인 작업
- 없음

### 완료된 작업 (이번 세션)
- **★ 알림 메일이 설정을 우회하던 것** — Irene 재신고 *"내가 메일설정 껐는데 메일로 계속 와."*
  운영 실측이 답이었다: irene 계정은 메일 **10종이 전부 0** 인데 어제 만든 `push_fallback` 만 1(기본값).
  그 항목을 **개별 설정과 별개 스위치**로 둔 것이 곧 "껐는데 온다" 였다. 이제 게이트가 두 겹이고
  **둘 다** 통과해야 나간다(종류별 email AND push_fallback). 전용 스위치는 **좁히기만** 한다.
  판정을 `selectEscalatable()` 로 떼어내 크론 본체를 운영과 같은 설정으로 돌려 **0통** 확인(7/7).
- **공개 링크가 보는 사람의 언어를 반만 따랐다** — 감지(navigator)는 되고 있었는데 공개 화면 문구
  **95건**이 ko/en 어느 쪽에도 키가 없어 소스의 한국어 기본값으로 떨어졌다. 패리티 가드가 못 잡는
  계열이다(한쪽에만 없는 게 아니라 **양쪽 다 없다**). 95 → 0. 공개 업무 상태칩·청구서 금액표기
  (`1,000,000원` ↔ `₩1,000,000`)·`<html lang>` 동기화. 카나리 `--suite publiclang` 신설
  (**ko 를 음성 대조군으로 같이 잰다** — en 은 fallbackLng 라 영어만 재면 감지가 죽어도 통과한다).
- **업로드 실패에 이유가 없었다** — 화면이 사유 코드를 상태에 담아 두고 **그리지 않았다**("실패" 만).
  공용 `utils/uploadError` 로 문장화(왜 + 다음에 뭘). `SELF_STORAGE_MAX_BYTES = 50MB` 하드코딩 제거 —
  그 숫자는 basic 에서만 우연히 맞았고 Drive 경로엔 해당되지 않는다(서버는 5GB). 한도는 서버가 내려준다.
  ★ **전날 내가 "운영 nginx 상한 10MB" 라고 보고한 것은 틀렸다** — 실측 **2GB**(60MB 도 통과).
- **좌측 하단 블록이 메뉴를 먹고 있었다** — 창 높이와 무관하게 213px 고정. 560px 창에서 메뉴 **4/20**.
  접기 + 짧은 창 기본 접힘 + 펼쳐도 절반 이하 → **8/20**, 블록 101px.
- **Q sale 전면 정돈** — 탭을 우상단 연회색 알약(Q task 것을 **베끼지 않고 공용 추출**) · 탭→Cue→필터→
  칩→리스트 · 검색+필터 한 줄 **전부 36px** · 축 이름을 셀렉트 **밖**에("전체 전체 전체" 해소) ·
  같은 축(단계·접근·담당)을 **상담 탭에도**(서버 `accessWhere` 재사용) · 추가 버튼은 필터 줄 오른쪽 끝.
- **메모를 댓글로** — [메모] 가 고객응대 내역 **원장 전체**를 펼치던 것을 `NoteThread`(채팅방·메일과
  같은 컴포넌트)로. 저장도 `project_notes` 재사용 + `client_id` 한 칸(새 표 안 만들었다).
- **Cue 바를 Q task 와 같게** — Enter 전송(IME 가드) · 정리 → 확인 → 추가 3단계 · 별표 아이콘 ·
  우측 패널 자동열림 제거. 껍데기는 `cueBarShell` 로 공용 추출.
- **보관함·치우기** — 보관한 상담이 전체 목록에 남던 것 제외(집계는 유지) · 보관함 ✕ = 영구 제외
  (**메일 자체는 Q mail 에 그대로**) · 손으로 추가한 상담에도 ✕(단계만 «없음»).
- **아이콘 모양 통일 + 프로필 사진** — 원형 12곳 → 0곳, **가드 `avatarshape` 신설**(깨뜨려 확인).
  사진은 `users.avatar_url` 이 **이미 있어** 스키마 변경 0 — 없던 것은 올리는 길뿐이었다.
- **확인필요 영업 링크** — 고객 패널 대신 `/sale?q=이름`(상담 탭에서 그 건만 걸러진 상태).
  문구도 목록 칩과 같은 «대응 필요 — 첫 답장 / 새 문의 / 다음 할 일 없음» 으로.
- **한도 표시**(정식 4/100 · 문의 4/300) — 통째로 버튼이라 누르면 결제로 튀었다 → 표시 + 설명 +
  80% 초과 시에만 [한도 늘리기].
- 내 프로필: 글씨 크기를 **맨 아래 1열 · 목소리 등록 위**로.
- 문서: DEVELOPMENT_PLAN · CLAUDE.md(껍데기 공용 추출·아바타 모양 절 신설) · Q위키 아티클 2건
  (`profile-photo`·`notification-email-off`) · 릴리즈노트 v1.52.0(10항목 ko/en) · 개발 현황 id=94

### ★ 자체 검증이 잡은 결함 3건 (전부 양성 대조군이 뒤집어 준 것)
1. **보관함 숫자가 목록과 갈라졌다** — `counts.dismissed` 대입이 `want.includes('dismissed')` 뒤에 있어
   기본 목록에서 0 이 됐다(칩엔 1건). **0건 픽스처로는 통과**했고 실제로 하나 보관해 보니 드러났다.
2. **단계 칸이 행마다 20px 어긋났다** — `[보기]` 라벨 길이 차이(메일 보기 112 vs 고객 상세 보기 139)가
   액션 묶음 폭을 흔들었다. 고정 폭 칸으로 전 행 **827 하나**. ★ **내 판정 기준도 틀렸었다** —
   칩 left 로 재면 "중앙배치" 를 어긋남으로 오판한다. 칸(열)으로 잰다.
3. **사진이 mimetype 만 보고 위장 파일을 저장했다** — `<script>` 가 `u5.png` 로 저장됐다.
   앞 12바이트 매직으로 판정해 거부(실행 위험은 `nosniff`+`image/*` 로 이전에도 없었다).

### 검증 (Fable 미검증 — 자체 검증. 호출 **5회 전부 429**, Irene 지시로 진행)
> Irene 2026-09-14: *"지금 fable 안돼. 자체 검증해."*
- 메일 에스컬레이션 **7/7** — 크론 본체 · 운영과 같은 설정 · **실제 발송 0통**(함수 가로채 계수) · 전부 원복
- **격리·사진 반증 21/21** — 상담 메모 3종(client·email_thread·conversation) × 3경로 **9건 전부 차단**
  (남의 엔티티 404 · 남의 워크스페이스 번호 403). 토큰이 실제로 통하는지 먼저 확인했다 —
  안 그러면 403 이 "격리" 가 아니라 "인증 실패" 다. 사진: 본인 200 / **미공유자 403** / 무인증 401 /
  경로 탈출 없음 / 파일명 `u{id}.{ext}` 뿐 / 삭제 후 404
- 실HTTP 15/15 · 보관함 양성 대조군 6/6 · 화면 실측 20/20(1440·560)
- 카나리 `publiclang` 6/6 · Cue 바 4/4 · `drafts,salepanel`·`tenant` 실패 0
- 빌드 EXIT 0 / `error TS` 0 · 가드 EXIT 0(53/54) · health-check 44/44
- 운영 사후: `project_notes.client_id` + 인덱스 존재 · 헬스 200 · PM2 1.52.0 · 릴리즈노트 200

### ★ 커밋했지만 **미배포** (`/배포` 한 마디면 나갑니다)
- **프로필 사진 매직바이트 검사** (`ec4ffe25`) — 자체 검증이 찾은 구멍. mimetype 만 보고 있어
  `Content-Type: image/png` 을 붙인 아무 바이트나 저장됐다. 앞 12바이트로 PNG·JPEG·WebP 를 확인한다.

### 다음 할 일
- **프로젝트 탭마다 레이아웃이 갈린다** — 탭바 아래 내용 시작점·좌우 여백·스크롤 주체가 탭별로 다르다.
  Irene 2026-09-14 재확인 요청. **신고 장부·개발현황 어디에도 없던 것**이라 이번에 backlog 에 넣었다.
  공용 껍데기(`assetTabLayout`)를 쓰는 탭과 각자 그린 탭이 섞여 있는 것으로 보인다 — 실측부터.
- **탭 메뉴 추가**(사용자가 프로젝트 탭을 추가) — 현재 탭은 코드 고정
  (dashboard/tasks/clients/files/docs/info/transactions/details). 범위·권한·게스트 노출을 먼저 정해야 한다.
- **iOS 빌드 만료 알림은 존재하지 않는다** — `platform_settings` 에 앱 컬럼은 `app_ios_url`·
  `app_android_url` 둘뿐이고 만료일 칸도 크론도 없다(전수 확인). 만들려면 만료일 칸 1개 +
  D-14/7/3/1 메일(기존 `platformNotify` 재사용). **Irene 지시 대기.**
- **안드로이드** — Google Play 조직 계정 **서류 승인 여부만** 알면 다음 단계(내부 테스트 → 비공개
  테스트 → 프로덕션 심사)로 간다. 그것만 서버에서 확인이 안 된다. 심사 답변·데이터 안전성·
  계정 삭제 URL·데모 계정(`appreview@planq.kr`)은 이미 확정돼 있다.
- **Fable 게이트 대기열 26~36** — `/usage-credits` 로 풀리면 한 번에 올린다. 특히
  ①상담 메모 3라우트의 멀티테넌트 격리 ②사진 `canSeeAvatar`·열거 가능성 ③마이그레이션 재실행 안전성
  ④"영구히 빼기" 문구와 실동작(코드엔 되돌릴 길이 남아 있다).
- 알림 메일 모양은 **실제 메일 클라이언트로 못 봤다**(Gmail·Outlook·iOS Mail).
- 버튼 전수검사 2~3라운드 — `data-testid` 부여는 Irene 승인 대기.

---

## 복구 가이드

새 Claude 세션 시작 시 아래 내용을 붙여넣으세요:

```
이전 세션 이어서 작업하고 싶어.
/opt/planq/.claude/session-state.md 읽어줘.
```
