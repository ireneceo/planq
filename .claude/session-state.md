# PlanQ 세션 상태

## 현재 작업 상태
**마지막 업데이트:** 2026-08-27 (KST) 저녁 — Irene 귀가로 중단, 아이맥에서 재개 예정
**작업 상태:** 커밋 완료. **dev 반영까지 됨. 운영 미배포.**
**다음 세션 첫 할 일 = `/배포`** — 8/27 수정분(A그룹 5건 + 오늘분 3건)이 전부 dev 에만 있다.

---

## 🔴 지금 가장 중요한 것 — 운영 미배포분이 쌓여 있다

운영 `feedback_items` **pending 33건** 중 **8건이 이미 고쳐져 있고 배포만 안 됐다.**
배포하면 pending 이 33 → 25 로 떨어진다. (`scripts/close-deployed-feedback.js`)

| # | 신고 | 커밋 |
|---|---|---|
| #299·#389 | 한글 자모 분리 입력 (한 달째 재발) | `8abbd7e1` |
| #388 | 혼자인데 "다른 탭/기기에서 녹음 중" | `8abbd7e1` |
| #387 | Q info 항목명이 입력되지 않음 | `8abbd7e1` |
| #386 | 확인권장 숫자가 안 빠짐 | `8abbd7e1` |
| #376 | 메일 분류 라벨 잘림 | `8abbd7e1` |
| #375 | 외부컨펌 건 업무가 내 주간에서 사라짐 | 오늘분 |
| #377 | 메일 업무추출 버튼 무반응 | 오늘분 |
| #380 | 채팅 입력창 이모지 | 오늘분 |

---

## ✅ 오늘(2026-08-27) 완료분

### 1. 끊긴 세션 복구
SSH broken pipe 로 중단됐으나 **손실 0** — A그룹 5건(`8abbd7e1`)·개인정보 URL(`d8fbb877`)
모두 커밋돼 있었고 dev 빌드도 08:30 에 반영돼 있었다. 반쪽 상태 없음.

### 2. App Store 심사용 데모 워크스페이스 (`54a9eb78`)
`scripts/seed-appstore-demo.js` — **전부 실 HTTP API** 로 만든다(직접 INSERT 금지 —
라우트 훅·감사로그·broadcast 를 태워야 실제 사용과 같은 상태가 된다). 파일도 multipart 실업로드.

- dev 실측(biz 122): 고객 3 · 프로젝트 2 · 대화 3(메시지 11) · 업무 8 · 일정 4 · 문서 3 · 청구서 2 · 파일 3
- **dev 확인 가능** — https://dev.planq.kr · `appreview@planq.kr` / `PlanQ-Review-2026!`
- 멱등(3회 연속 실행 확인). 데이터는 전부 가상(하나커피·미래건설·스튜디오온), 고객 메일은 `example.com`(예약 TLD → 발송 게이트 skip)
- ★ `vat_rate` 는 `decimal(4,3)` **분율**이다 — 10 을 넣으면 "Out of range" 500
- ★ 첫 판은 파일만 매번 재업로드됐다(필드명을 `original_name` 으로 넘겨짚음, 실제는 `file_name`).
  dedup 이 행은 안 늘려서 **로그·행수로는 통과처럼 보였다** — DB `ref_count` 가 2로 오른 걸로 잡았다

### 3. 운영 신고 3건 (오늘 커밋)

**#375 외부컨펌 — 내가 건 업무가 내 주간에서 사라짐**
- 원인: 잔류 규칙이 **담당자 분기에만** 있었다. 외부컨펌은 담당자·작성자·owner·admin 이 걸 수 있는데
  (`canChangeStatus`), 의뢰자가 자기가 건 것을 자기 주간에서 잃었다
- 조치: `services/weekTaskSet.js myWeekWhere` 에 `{status:'external_review', 의뢰자|작성자}` 분기 +
  `QTaskPage.inWeekCanonical` **미러**(한쪽만 고치면 우선순위 번호가 화면마다 갈린다)
- 실HTTP 반증 5축: ①의뢰한 외부컨펌 없음→있음 ②담당 대조군 항상 있음(검사기 생존 증명)
  ③보류는 여전히 제외(의도 유지) ④담당자도 계속 보임 ⑤타 워크스페이스 403
- ★ 집계(`mine`)는 `assignee_id` 기준 그대로 — **가용시간·진척 그래프 불변**(목록 4 vs 집계 2 로 확인)

**#377 메일 업무추출 버튼 무반응**
- 원인은 AI 도 권한도 아니었다. **이벤트가 청취자보다 먼저 발사된다.**
  툴바 버튼이 (a) 패널 여는 setState (b) 곧바로 window 이벤트를 쏘는데, 이 패널은 접혀 있으면
  **아예 마운트되지 않아**(MailPage 조건부 렌더) addEventListener 가 아직 없다 → 이벤트 소멸.
  패널이 이미 열려 있을 때만 동작해서 "될 때도 있고 안 될 때도" 로 보였다
- 조치: 정본을 **모듈 스코프 pendingExtract** 로 이동(TaskDetailDrawer 의 `pendingResumeFocusTaskId` 패턴).
  window 이벤트는 이미 뜬 패널을 깨우는 신호일 뿐, 늦게 뜬 패널은 마운트 시점에 소비. TTL 30초

**#380 채팅 입력창 이모지**
- 리액션(#138 메시지에 **다는** 것)은 이미 있었다. 없던 건 **보내는** 쪽
- `pages/QTalk/EmojiPickerButton.tsx` 신규(ChatPanel 3,600줄 god-file 래칫 회피). 백엔드 변경 0
- 커서 위치 삽입 + draft 계약 유지 + 커서 복원
- ★ 실브라우저 카나리 신설 `scripts/e2e/canary-chat-emoji.js` — 팝업이 **위로** 열려 클리핑 위험이
  있었다(같은 파일 '더보기' 메뉴는 그 이유로 portal 로 뺐던 전례). `elementFromPoint` 로
  덮임·클립 반증 + 검사 개수 출력. 실측 통과(rect 288×152 · 셀 32개 · 삽입 "" → "👍")
- ★ 가드가 내 실수를 잡았다 — 셀 32px 이 컨트롤 높이 토큰(36/40/44) 밖. 36px 로 교정

---

## 다음 할 일 (순서)

1. **`/배포`** — 위 8건이 운영에 안 가 있다. 배포 후 `close-deployed-feedback.js` 로 pending 정리
2. **데모 워크스페이스 운영 시드** — 스토어 빌드는 `planq.kr` 을 보므로 운영에 있어야 한다.
   `node scripts/seed-appstore-demo.js --base=https://planq.kr --email=appreview@planq.kr --password='...' --admin-email=... --admin-password=...`
   ⚠️ `--admin-*` 를 꼭 같이 준다 — 안 주면 체험 14일 만료 후 심사관이 잠긴 화면을 본다
   (네이티브는 3.1.1 로 구매 표면이 렌더 안 돼 막다른 길). **이 면제 단계만 미검증** —
   platform_admin 자격이 없어 라우트 존재(403, 404 아님)까지만 확인했다
3. **운영 신고 남은 25건** — 아래 분류 참조

### 운영 pending 33건 분류 (2026-08-27 실측, 원문 직접 읽음)

**배포하면 닫힘 (8):** #299 #375 #376 #377 #380 #386 #387 #388 #389
**이미 코드에 있음 — 확인 후 닫기 (2):** #273(외부컨펌 권한 — 상태 전제였고 잠금 안내로 처리됨) ·
  #385(진척 그래프 — 2026-08-24 Δ 기준선 제거가 이 신고 대응이었다. **그래프는 동결 — 손대기 전 Irene 확인**)
**구체적 버그·소~중 (5):** #284(Q info 상세 좁음·복사) · #289/#346(배포 시 자동 공지·업데이트 내역) ·
  #378(문서 본문 이미지·파일이 파일메뉴에 안 잡힘) · #384(메일 팔로업 리마인더)
**Fable 설계 필요 — 신규 시스템 (18):** #211 #227 #230 #233 #235 #259 #343 #353 #354 #355 #358
  #360 #361 #379(GDrive 양방향) #381/#382(Q sale) #383(Q Note 녹음 업로드)

### Irene 조치 대기
- Apple Developer Program · Codemagic 가입 · App Store Connect API 키(`.p8`·Key ID·Issuer ID)
- 운영 데모 시드 실행 여부 (운영 DB 쓰기라 지시 대기)
- Google Drive 재연동 (`invalid_grant`)

---

## 참고 — 이번 세션 교훈
- **로그·행수가 통과처럼 보여도 DB 를 봐야 한다** — dedup 이 중복 행을 안 만들어서 파일 재업로드가
  안 보였다. `ref_count` 가 유일한 증거였다
- **이벤트를 쏘기 전에 청취자가 있는지 봐야 한다** — 조건부 렌더 컴포넌트에 window 이벤트를 쏘는
  패턴은 "될 때도 있고 안 될 때도 있는" 회귀를 만든다. 정본은 모듈 스코프 값에
- **가드가 나를 잡았다** — 32px 셀. 통과할 때까지 돌리는 게 아니라 **잡히면 내가 틀린 것**
- 테스트 워크스페이스 `T375 검증`(biz 123, plan basic 로 올림) + `t375-owner@example.com` /
  `t375-member@example.com` 은 dev 에 남겨뒀다 (#375 재검증용)

## 복구 가이드
```
이전 세션 이어서 작업하고 싶어.
/opt/planq/.claude/session-state.md 읽어줘.
```
