# PlanQ 세션 상태

## 현재 상태
**마지막 업데이트:** 2026-08-28 (KST) 09:50
**운영 커밋:** `1be926c0` (v1.48.5) · **dev 에 커밋 14개 미배포**
**운영 피드백:** pending 22건

---

## 🔴 다음 세션 첫 할 일 = `/배포`
dev 에만 있는 커밋 14개. 특히 **판매 포인트 기능(드라이브 역방향 동기화)** 이 통째로 미배포다.

---

## 오늘 한 일

### 배포 완료분 (운영 반영됨, 커밋 7개)
초대 쿼터 3건(F1/F2/F2b) · 좌초 계정(F3) · 탭 이름 · Talk 뱃지 · 모바일 드로어 ·
#378 인라인 이미지 · #346·#289 릴리즈 노트 자동 발행 · v1.48.5 노트 12항목

### dev 미배포분 (커밋 7개)
| 건 | 내용 |
|---|---|
| 탭 이름 회귀 | 탭 5개 넘으면 LRU 정지(alive:false)로 **언마운트** → cleanup 이 이름을 지웠다. 정지는 이탈이 아니다 |
| 문서 4건 | 편집 중 리스트 접기 **흰 화면**(그리드 0px 칸) · **나만보기가 프로젝트에서 실종**(범위 바꾸면 project_id 를 지웠다) · 제목 2줄 · 카드 본문 미리보기 |
| 다운로드 | 네이티브 저장 실패를 조용히 삼키던 것 → 문구 노출 |
| Cue 정직성 | 안 보는 영역을 **"없다" 고 단언**했다(문서 10건·파일 2,353건·메일 4,775건인데). COVERED/UNCOVERED 단일 원천 + 프롬프트 규칙 |
| Cue 일정 3겹 | 화면은 ①워크스페이스 ②**개인 구글(저장 안 함, 실시간)** ③업무마감 세 겹인데 Cue 는 ①만 봤다 |
| 메일 번역 | 기본 대상이 UI 언어라 **한→한 번역**. 눌러도 아무 일이 없던 게 정상 동작이었다 |
| **Drive 역방향 B1** | 아래 별도 |

### 🎯 Drive 역방향 동기화 (판매 포인트) — B1 핵심 완료
- `services/gdriveApply.js` **신규** — 감지만 하고 **적용이 없었다.** 정책 4가지 반증 통과:
  정본 축 비대칭(PlanQ 정본은 Drive 사본 삭제 시 **미러 해제만**, 원본 보존) · soft delete ·
  **멱등 비교로 에코 흡수** · LWW
- `gdrive.listChanges` 필드 확장 — `parents`·`md5Checksum` 이 없어 **이름변경 말고는 감지 불가**였다
- `models/GdriveSyncLog` + `gdrive_sync_logs` · `file_folders.gdrive_folder_id` ·
  `scripts/migrate-gdrive-sync.js`(멱등 재실행 0건 확인)
- 🔴 **`services/gdriveWatchCron.js` 신규 — 감시 채널을 여는 사람이 아무도 없었다.**
  `startChangesWatch` 는 수동 엔드포인트에서만 호출 → 연결해도 동기화가 **시작조차 안 됨**.
  크론이 채널 없는 연동도 열어준다. **실동작 확인**(opened:1, 구글 API 성공)
- 프론트 `gdrive:changed` 수신부 — **받는 곳이 0건**이었다. DocsTab 에 추가

---

## 🔴 Irene 대기 (전체 일정의 지배 변수 — 오늘 병렬 시작 권장)
| | 무엇 | 왜 |
|---|---|---|
| A1 | 운영 데모 워크스페이스 시드 — **platform_admin 자격증명** | 심사관 로그인 계정. 없으면 App Store 제출 불가 |
| A2 | App Store Connect **Beta App Review 정보**(피드백 이메일·담당자·전화) | 없으면 외부 테스터 제출이 마지막 단계에서만 실패 |
| A5 | 운영 **APNs `.p8` 키** 존재 확인 | 푸시가 판매 포인트인데 실발송 미검증 |
| A8 | Play Console **$25 등록** | 앱 서명 키 SHA-256 이 나와야 Android 진행 |
| B2-1 | **Drive 전용 GCP 프로젝트 분리** | Calendar 심사가 Restricted 에 휘말리지 않게. 선행 없음 |
| #385 | 그래프 (가)입력먼저/(나)공식먼저/(다)둘다/(라)그대로 | Fable 은 (다) 권고 — 동결 해제 정당 판정 |
| #259 | 고객 채팅 매직링크 | 원문이 "나에게 상의를 해줘" 라 **컨펌 1회 필요** |

## 남은 작업 (Fable 승인 순서)
1. **B1 잔여** — "Drive 에서 삭제됨" 사유 UI · 실 Drive 2탭 종단 검증(dev 연동 폴더 오류로 미실행)
2. **B2** — Restricted `drive` scope. dev 완성 즉시 심사 신청(**4~8주 외부 대기 = 크리티컬 패스**)
3. **Cue 커버리지 2/2** — 문서·파일·메일 카운트+상세, 고객이력, llm 상한 40K, 통합검색에 "Cue 에게 묻기"
   ★ 파일은 `deleted_at: null` **따로 걸어야** 함 — 기존 술어가 soft-delete 를 모른다
4. #385 그래프 · #378 나머지(첨부 귀속→백필→리사이즈) · 메일 전달 §5 해제
5. 구글 드라이브 첨부 picker · 음성노트 "바로 녹음"(A+B 안 추천) — Irene 아이디어 요청분

---

## 이번 세션 교훈 (박제됨)
- **[[feedback_tab_suspend_is_not_exit]]** — 탭 LRU 정지는 언마운트다. cleanup 에 "이탈" 의미 금지
- **[[feedback_canary_must_match_runner_contract]]** — 🔴 카나리 3개가 **게이트에서 영원히 통과**할 뻔했다.
  단독 ❌ 인데 러너 ✅. `r.fail` 계약 필수 + **러너로도 반증**할 것
- **[[feedback_actual_hours_focus_only]]** · **[[feedback_feedback_closes_trailer]]**
- **검사기가 하루에 10회 거짓말했다** — 공통 원인: 무엇을·몇 개를·어떻게 쟀는지 말하지 않으면 못 믿는다
- **styled 주석의 백틱**에 또 걸렸다(메모리에 있는데도)
- **스키마 바꾸면 `dump-schema` 재실행** — 가드가 "DB 에 없는 컬럼" 으로 잡아줬다(정본은 DB 스냅샷)
- **실행이 잡은 버그** — `startChangesWatch({businessId})` 규약 오류. 문법·타입 다 통과하는 종류

## 복구 가이드
```
이전 세션 이어서 작업하고 싶어.
/opt/planq/.claude/session-state.md 읽어줘.
```
