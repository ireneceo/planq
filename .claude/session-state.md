## 현재 작업 상태
**마지막 업데이트:** 2026-09-19 UTC
**작업 주체:** [Opus]
**작업 상태:** **완료 — 운영 배포 3회 (v1.52.13 · v1.52.14 · v1.52.15)** · 미배포 코드 0 ·
**Fable 토큰 소진** — v1.52.14 도중부터 전부 **자체 검증(Fable 미검증)**. 대기열 `docs/FABLE_GATE_QUEUE.md` 6건

### 진행 중인 작업
- 없음. 이 세션 코드는 전부 운영에 나갔다.

### 완료된 작업 (이번 세션)

| 배포 | 내용 | 검증 |
|---|---|---|
| v1.52.13 | **상담 = 고객 축의 기록** 확정(메일·채팅·Q note·수기 네 입구 → 한 원장) · 채팅 아바타 → 프로필 · 프로젝트 노트 탭 | **Fable PASS** (2라운드 — 실제 결함 4건을 반려받아 수리) |
| v1.52.14 | 「저장 응답이 화면을 지우던」 계열 **8곳** · 연결 칸 156→200px · 연결 문구 13곳 통일 | Fable 미검증(자체) |
| v1.52.15 | Q note **업무 추출 화면** · 일정 [미팅 정리] · 메일 확인권장 문구(#421) | Fable 미검증(자체) |

### 신고에 없던 결함 — 이번에 찾아낸 것

1. **저장 응답이 화면을 지운다 — 8곳.** `PUT` 응답은 `GET` 보다 좁은데 화면이 통째로 갈아끼웠다.
   **서버는 멀쩡하고 화면만 잃어** 새로고침하면 돌아오므로 오래 안 보였다. 신고(Q note 프로젝트
   연결)는 그중 **한 곳**이었다. → `applySessionPatch` · 호출부 병합 · **가드 `savemerge`** 신설.
   ★ 가드를 처음 만들었을 때 **정작 신고된 형태를 못 잡았다**(추출기가 여러 줄 시그니처의 매개변수
     타입 `{` 를 본문으로 오인). 경계를 고치니 추적 25 → 59개, 그 자리에서 6곳이 더 나왔다.
2. **공용 팝오버에 왼쪽 클램프가 없었다** — 화면 왼쪽 트리거의 메뉴가 `x=-111` 로 나가 「죽은 버튼」.
3. **고객 이름 라벨이 통째로 비어 있었다** — 응답 `name` 은 null, 정본은 `display_name`.
4. **Q note 업무추출이 서버만 있고 화면 0곳**(2026-06-05~, 3개월 반). 신고 #382 가 여기서 막혔다.
   붙이고 보니 후보 `project_id: null` 고정 · [다시 추출] 중복 누적도 드러났다.

### 운영 피드백
미처리 **14 → 8건**. #407·#413·#414·#416·#420·#421 답글 달고 닫음. #382 는 답글만(남은 항목 있음).
남은 8건은 **`docs/WORK_TODO_2026-09-19.md`** 에 «왜 안 됐나» 까지 적었다.
★ 그 문서 맨 위에 **내 오보 정정** — #417 의 «안 된 것» 을 커밋 메시지만 보고 추측해 틀리게 보고했다.

### 미해결로 남긴 것
- **업무추출 화면의 조회가 같은 밀리초에 2번.** 두 번 고치려다 실패, **듣지 않는 장치는 걷어냈다.**
  응답 둘 다 200 · 부모 조회는 1회 · DOM 1개 · 청크 1벌 · 프로덕션 빌드. 무해하나 원인 미규명.
- 서버 GET/PUT 응답 모양 대조 검사 · 연결 문구 13곳 중 11곳 화면 미측정 ·
  일정에서 시작한 노트를 그 일정에 연결하는 축(스키마 결정).

### 다음 할 일
1. **Fable 토큰이 생기면 `docs/FABLE_GATE_QUEUE.md` 부터.** 특히 «저장 응답이 화면을 지운다» 계열이
   **다른 화면에도** 있는지 전수 — 지금 가드는 직접 호출과 콜백 경계만 본다.
2. **#382 남은 둘 — Irene 판단 필요**: 요약 자동 생성 시점(종료 시 항상 / 길이 기준 / 수동) ·
   상담 기록에서 직접 업무추출(= `task_candidates` 컬럼 추가 → 운영 마이그레이션, R=1).
3. **#421 남은 질문 — Irene 판단 필요**: 「문의」 태그 기준. Q sale 유입 기준(`saleMailCriteria`)과
   **같은 술어**를 써야 한다 — 두 벌이 되면 갈라진다.
4. **#417 남은 둘 — Irene 판단 필요**: Q file 에서 프로젝트 폴더를 보는 방식(화면 구조) ·
   업로드 용량 한도를 올릴지 드라이브로 유도할지.
5. **안드로이드 심사 결과** — Irene 이 알려주면 운영 `platform_settings.app_android_url` 설정.

### 이 세션의 교훈 (박제함)
- **`feedback_green_without_measuring`** — 내 검사가 초록이면 «정말 잰 것인가» 부터 의심한다.
  하루에 **아홉 번** 거짓 초록(선택자 오집 · 픽스처 미생성 · keep-alive 로 같은 화면 5번 ·
  401 인데 통과 · 저장이 막혔는데 통과 …). 전부 «아무것도 재지 않아서» 초록이었다.
  → 판정 **앞에** «전제가 성립했는가» 를 따로 판정하고, 잰 것이 없으면 **⬜ 미측정**으로 찍는다.
- **`feedback_save_response_narrower_than_get`** — 저장 응답을 통째로 대입하지 않는다.

---

## 복구 가이드

새 Claude 세션 시작 시 아래 내용을 붙여넣으세요:

```
이전 세션 이어서 작업하고 싶어.
/opt/planq/.claude/session-state.md 읽어줘.
```
