## 현재 작업 상태
**마지막 업데이트:** 2026-09-22
**작업 상태:** 완료 (v1.56.0 · v1.56.1 운영 배포)

### 진행 중인 작업
- 없음

### 완료된 작업 (이번 세션)
- **서명란·서명본·우리 측 서명·거절 표시** (v1.56.0, `f873304d`) — 운영 배포 완료
  - 마이그레이션(`migrate-signature-slot`) → 코드 → 템플릿 seed → 릴리즈노트 순으로 진행
  - 조립은 `services/signedDocument.js` 한 곳 (앱 문서·공유 링크·공개 서명 화면·PDF)
  - 우리 측 서명은 로그인이 본인 확인 (`routes/signature_internal.js`)
  - 채팅 카드에서 서명 토큰 링크 제거 → 본인 이메일로만 발송
- **문서 복사 + Q docs 상단 정리** (v1.56.1, `eacfbda6`) — 운영 배포 완료
  - `POST /api/posts/:id/duplicate` · 상세 [편집] 옆 아이콘 버튼
  - [템플릿] 버튼을 [+] 드롭다운 «템플릿에서 시작» 으로 이동
- **검증 중 찾아 고친 결함 3건**
  - 고객이 자기 계약서의 서명본을 못 봄 (본문 200 / 서명본 403 → `canReadPost` 통합)
  - 거절이 문서·PDF·공유 링크에 «서명 전» 으로만 보임 → «서명 거절» 표시
  - 공개 화면 날짜가 `ko-KR` 하드코딩 → 화면 언어
- **회귀 카나리 3종 신설** — `--suite signature`(16) · `signflow`(15) · `docsdup`(5), 전부 실브라우저
- 위키 갱신: `collect-signature` 보강 + `signed-copy-pdf`·`duplicate-document` 신규
- CLAUDE.md 박제 2건: 서명본 조립 계약 · 복사 계약

### 다음 할 일
- **Fable 검증** — 서명(R=1: 법적 증빙·공개 무인증 라우트·운영 스키마)과 복사를 전부 자체 검증만 했다.
  한도 429 로 4회 시도 모두 실패. 한도 회복 시 `/fable-검증` 으로 한 번에 올린다.
  판단 대기 항목 `docs/FABLE_GATE_QUEUE.md` (공개 라우트 상한 · 고객이 보는 서명 범위 ·
  로그인=본인확인의 법적 무게 · 다회차 서명의 고정본 선택 · 마이그레이션 잠금시간 ·
  ref_count 가 첨부 링크 수를 안 셈 · 복사본 vlevel 승계 · 표 복사 강등 미고지)
- **다른 메뉴 복사** — 지금 복사가 있는 곳은 업무·문서 둘뿐. 청구서·프로젝트·Q info·일정·표·메일에는 없다.
  청구서는 금액·증빙이 얽혀 「무엇을 안 따라가게 할지」를 먼저 정해야 한다(Irene 요청 대기)
- 운영 첫 서명 요청 발송 때 EmailLog 로 실제 도달 확인 (검증은 example.com 으로만 했다)
- 옛 `Document` 모델의 «이름 입력» 서명 방식 정리 (이번 범위 밖)
- 죽은 i18n 키 정리 — `qbill.json` 의 `detail.header.actions.duplicate` 를 읽는 코드가 0곳

---

## 복구 가이드

새 Claude 세션 시작 시 아래 내용을 붙여넣으세요:

```
이전 세션 이어서 작업하고 싶어.
/opt/planq/.claude/session-state.md 읽어줘.
```
