## 현재 작업 상태
**마지막 업데이트:** 2026-08-29
**작업 상태:** 완료 (운영 배포 3회 · 미배포 커밋 3개)

### 진행 중인 작업
- 없음

### 완료된 작업 (이번 세션)

**파일 휴지통 신설 — 삭제를 되돌릴 수 있게** (`85489d30`, 배포됨)
- ★ **"soft delete" 가 바이트까지 지우고 있었다.** `deleted_at` 은 찍혔지만 같은 함수가
  `fs.unlinkSync` + Drive/S3 삭제까지 했다. 복구 화면만 붙였으면 **복구 버튼이 거짓말**을 했다.
- 삭제=휴지통행(기록만) / 영구삭제(ref_count·unlink·원격삭제·purged_at) 2단계 분리.
  영구삭제는 `services/filePurge.js` **단일 착지점** — cron 과 사용자 조작이 같은 함수를 쓴다.
- 갈라져 있던 것: 자동 정리 cron 이 `ref_count<=0` 만 봐서 **휴지통을 영영 못 비우는** 상태였다.
- 옛 삭제분은 **디스크를 한 건씩 확인해** 없는 것만 stamp(운영 527건 중 **되살릴 수 있는 2건을 살렸다**).
- 화면: `pages/QProject/TrashDrawer.tsx` — 워크스페이스·프로젝트·개인 보관함 공통.
  목록에는 **되돌릴 수 있는 것만** 보인다.
- 쿼터는 삭제 즉시 반환(현행 유지) — Free 1GB 에서 "지웠는데 용량이 안 준다" 는 즉시 막힘.

**파일 화면 요청 폭주 — 한 번 열면 사용자가 잠기던 것** (`fc01170b`, 배포됨)
- 실측 **`/files` 1회 로드 = API 1,099건 / 한도 600건per분**. 새로고침하면 refresh 가 429 →
  **로그인 화면으로 튕김.** 실사용자도 그대로 닿았다.
- 원인은 썸네일. `background-image` 라 **화면 밖 카드까지 전부** 받았다 → `<img loading="lazy">`.
- 이미지 전용 rate 버킷 **분리**(면제 아님). ★ `app.use` 로 앞에 걸어도 **연쇄 실행**이라
  뒤 limiter 가 또 센다 — `skip` 으로 빼야 실제로 분리된다. 운영 헤더로 확인(3000 vs 600).
- 결과 **1,099 → 24건**.

**#390 공유 링크가 아예 안 만들어지던 것** (`f8cb422e`, 배포됨)
- `entityId={Number('direct-4525')}` = **NaN** → `/api/files/NaN/share`(없는 경로) → 404 HTML →
  화면엔 아무 말도 안 뜸. 반증: 옛 404 / 새 200 + share_token.
- 비직접 파일(채팅·업무 첨부)은 id 체계가 달라 같은 실패 → **공유 버튼 자체를 숨김**.

**#284 문서→Info** (`bb18020b` `e930259b`, **미배포**)
- ①② AI 항목 분리 엔진(`kb/ai-ingest`)은 **이미 있었고 이 경로만 안 썼다**. `extract_only` 로
  본문 평문을 **서버에서** 받아 같은 모달로 연결 + 출처(`source_post_id`) 기록(소유권 검증 포함).
- ③④ 값 칸이 좁던 원인은 삭제(×)가 값 줄에 있던 것 → 항목명 줄로 이동. 상세에도 복사 추가
  (`useCopy` 단일 원천 — 리스트와 같은 것을 쓴다).

**모바일 체감 4건** (`e0390db0`, 배포됨)
- 앱 콜드스타트가 지난 화면으로 납치되던 것 → 탭이 켜지는 창에서만 복원(쓰기도 차단)
- 부팅 왕복 2→1 (`refresh` 가 이미 주던 user 를 버리고 `/me` 를 또 불렀다)
- 메뉴 재진입 즉시 표시 (`lib/pageCache.ts` SWR·메모리)
- Cue 입력이 키보드 뒤로 들어가던 것 → `DetailDrawer` 의 `--vv-top`/`--vvh` 계약 적용
- Q Note **바로 녹음** 신설(장부에 없던 요청) + 녹음 중 409 를 한 번 되찾아 보고 판단

**Android App Links** (`b186ed85`, 배포됨)
- host 전체를 가로채 **공유문서·서명·공개위키 링크까지 앱으로 끌려갔다** → iOS AASA 와 같은 5경로로 제한

**신설 카나리**
- `--suite mobileboot` — 콜드스타트 착지(양성 대조군)·재진입 즉시표시·Cue 키보드
- `--suite trash` — **화면에서** 되돌릴 수 있는가. 진입점 제거 반증으로 러너 빨개짐 확인 후 등록

### ⚠️ 남은 것 / 정직하게 적는 것

1. **미배포 커밋 3개** — `5014a665`(카나리 등록) · `bb18020b`·`e930259b`(#284). 다음 배포 대상.
2. **#284 ③④ 반증 무효** — "원복이 우선" 이라 판단해 **반증 빌드가 컴파일되는 중에 원복**해서
   정상 코드가 빌드돼 측정됐다(내 경합). ✅ 4건은 정상 코드 결과라 제품 판정엔 영향 없고,
   run.js 게이트가 아니라 스크래치패드 측정이라 거짓 초록 게이트도 안 남는다. **다음에 다시 반증.**
3. **Fable 게이트 사각** — 게이트 검사는 **미커밋** 변경만 본다. 커밋하면 그냥 통과한다.
   이번 세션의 휴지통(마이그레이션 포함 배포)·rate limiter(보안 경계) 는 CLAUDE.md 상
   **Fable 대상**인데 Opus 직접 검증으로 진행했다(실HTTP 14/14·반증·운영 실측). 사실대로 기록한다.
4. **#383 Q Note 녹음파일 업로드** — 배관은 재사용 가능하나 **ffmpeg/ffprobe 가 dev·운영 둘 다 없다.**
   길이를 모르면 **과금 전 사전 검사(쿼터 초과 차단)를 못 한다** → 돈에 닿으므로 Fable 설계 게이트 대상.

---

## 🔴 다음 섹션 최우선 — Irene 지시 (2026-08-29)

> "다음엔 꼭 **구글드라이브랑 네이티브앱 사용 가능**하게 다 해야 해."

### ① Google Drive 양방향 (#379)
- **지금 상태:** 역방향 동기화 배관은 살아 있다(감시 채널·커서 정상). 운영 최근 7일 로그
  `skip/unknown_file 6 · content 3`. gdrive 파일 39개 · 연동 워크스페이스 1곳.
- **막힌 이유:** scope 가 `drive.file`(앱이 만든 것만, 코드 4곳 전부 동일). Drive 에서 직접
  올린 파일은 구조적으로 `unknown_file` — 버그가 아니라 권한 경계.
- **🔑 Irene 만 가능:** Drive 전용 GCP 프로젝트 분리 → Restricted `drive` scope 심사. **4~8주.**
  곁가지: Gmail OAuth 가 캘린더 심사용으로 일시 차단 중(`project_gmail_oauth_disabled_restore`) —
  프로젝트 분리 시 같이 정리 가능.
- **내 몫(심사 전에도 가능):** 심사 통과 즉시 켤 수 있게 인제스트 경로·충돌 정책·UI 를 미리 완성.

### ② iOS — 막힌 지점은 **Codemagic 가입 하나**
- **서버는 다 됐다(실측):** APNs env 5건 · AASA `application/json` + Team ID `H2HW8BHXNW` 치환 ·
  등록 기기 1대 · 최근 7일 푸시 sent 219. ASC API 키도 발급 완료(`AuthKey_A68786VG7D.p8`).
- **🔑 Irene (브라우저만, 맥 불필요):**
  1. codemagic.io 가입 (GitHub 계정) — ⚠️ **Team 만들지 말 것**(무료 500분 소멸)
  2. Teams > Integrations > App Store Connect 에 키 등록 — **이름 정확히 `PlanQ ASC`**
  3. 저장소 `ireneceo/planq` 연결 → 워크플로 **`ios-testflight`** 실행
- 첫 빌드는 서명 프로파일 자동 생성으로 한 번 실패할 수 있다 — 로그 주면 조정.
- 내부 테스터(100명) 심사 없음 / 외부 테스터부터 Beta App Review.

### ③ Android — iOS 보다 3단계 뒤
| 항목 | 상태 |
|---|---|
| Capacitor 프로젝트 (`app.planq`) | ✅ |
| App Links 경로 제한 | ✅ 이번에 수정(`b186ed85`) |
| `assetlinks.json` | ❌ `__ANDROID_SHA256_CERT__` **자리표시자** — App Links 미발화 |
| Codemagic android 워크플로 | ❌ 없음 (`ios-testflight` 하나뿐) |
| release 서명 | ❌ 없음 |
| versionCode/Name | ❌ `1`/`1.0` 고정 |
- **🔑 Irene:** Play Console **$25** 등록 → 앱 생성 → **앱 서명 키 SHA-256 지문**을 주면
  내가 assetlinks 치환 + android 워크플로 + release 서명을 한 번에 붙인다.
- 신규 개인 개발자 계정은 **20명 내부 테스트 14일**이 선행 조건 — 등록을 빨리 걸수록 좋다.

### 그 외 후보 (운영 장부 pending 22건 중)
- **#383** Q Note 녹음파일 업로드 → 텍스트화 (위 ⚠️4 참조 — ffprobe + Fable 게이트 선행)
- **#284 ③④ 반증** 다시 하기
- **#382·#381 Q Sale** — 신규 시스템이라 Fable 설계 게이트

---

## 복구 가이드

새 Claude 세션 시작 시 아래 내용을 붙여넣으세요:

```
이전 세션 이어서 작업하고 싶어.
/opt/planq/.claude/session-state.md 읽어줘.
```
