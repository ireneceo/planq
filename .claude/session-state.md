## 현재 작업 상태
**마지막 업데이트:** 2026-08-21
**작업 상태:** 완료 (배포 2회차 진행)

### 진행 중인 작업
- 없음

### 이번 세션의 규칙 (중요)
- **Irene 지시로 이 사이클은 Fable 미사용.** 전부 Opus 자체 검증.
  → 인수인계 문서 `docs/FABLE_VERIFY_QUEUE.md` 에 고위험 4건 · 미검증 축 · 재현법 · 함정을 남겼다.
  → 정지 훅(fable-gate)이 계속 막았으나 지시 우선. 넘기려면 `touch /opt/planq/.claude/.fable-gate-skip`.

### 완료된 작업 (이번 세션)

**1차 배포 완료 (`20260821_063416`) — 커밋 5건**
- **문서 자동저장 무한 루프** — 변경 판정 기준이 "편집 진입 스냅샷" 하나뿐이라 한 글자만 고쳐도
  영원히 changed=true → 저장이 끝날 때마다 2초 타이머를 다시 걸어 **자기를 무한 재예약**.
  기준을 둘로 분리(editSnapshotRef=취소 되돌림 / lastSavedRef=자동저장 발화). 반증: 12회+무한 → 1회.
- **편집 중 페이지가 새로고침돼 편집이 닫히던 것** — `isReloadSafe()` 가 formDirty 만 봤는데
  그 플래그는 **자동저장이 끝나는 순간 꺼진다**. 그 틈에 새 빌드가 감지되면 reload.
  `body[data-editing-active]` 계약 신설(문서·메모·메모팝업). 새 빌드는 편집을 닫고 이동할 때 적용.
- **#357 운영 백엔드가 하루 12회 크래시** — 신고는 "로그 노이즈" 였으나 실제로는 프로세스 사망.
  removeAllListeners 후 늦은 소켓 error 를 받을 리스너가 없어 uncaughtException.
  no-op 리스너 재부착(5지점) + 끊긴 연결에 end() 생략. 8/21 로그의 유일한 Error 12건 = 재시작 횟수.
- **#351 캐치업 · #348 상속(+운영 백필 3건) · #350 알림 다이제스트** — recurringTaskGenerator.
- **#365 Q docs 첨부** — 편집 모드에서 첨부가 **한 번도 저장되지 않았다**(전 파일). 업로드 실패가
  문서 저장 전체를 실패시키던 것도 차단(nginx 413 은 HTML 이라 r.json() 이 throw).
- **#362 OG 서버 렌더** · **#366 검색 완료업무** · **#359 오등록 유발 주석**
- **채팅 3건** — 고객이 참여자로 안 들어감 / @목록이 워크스페이스 전원 / 방 이름이 프로젝트 prefix 만큼 잘림
- **에디터 4건** — #341 임시저장본 중복(자동저장 in-flight 경쟁) · #337 엑셀→이미지 · #304 코드박스 쪼개짐 · #363 정렬

**2차 배포분 (이번에 배포) — 커밋 8건**
- **Q info 12건** — #316 #319 #320 #321 #322(가져오기 파이프라인) + #323 #325 #326 #327 #328
  #329 #330 #331 #332 #333(항목 화면). 모달 배관까지 맞춤.
  ★ 부수 성과: CSV 의 "비밀번호" 열이 자동으로 secret 으로 잡히고 **그 값은 색인·번역에서 제외**.
- **#334** 통합검색이 시크릿 값까지 훑던 것 — secret 항목에서만 맞은 문서는 결과에서 제외.
- **#310** AI 문서의 프로젝트 미연결 · **#336** 새 표가 편집으로 안 열리던 것(커밋 타이밍 경쟁).
- **메일 전달 시 본문 소실** — 상세 응답이 base64 이미지를 `cid:planq-embed-N` 으로 바꿔 내려주는데
  전달이 그 자리표시자를 그대로 보냈다. restoreEmbeddedImages() 로 원본 복원.
- **검색 전면** — 메일 관련도 정렬(제목>보낸사람>그외) · 띄어쓰기 무시 양방향(메일·통합) ·
  통합검색 와일드카드 방어(`%` 하나로 전건 매칭되던 것).
- **고객 검색이 처음부터 죽어 있던 것** — `Client.email` 유령 컬럼. `.catch(()=>[])` 가 삼켜
  화면에도 로그에도 안 남았다. invite_email/billing_contact_email 로 교체. 8쿼리 전수 점검 0건.
- **문서 편집 권한을 공개 범위에 맞춤** — 볼 수 있으면 고칠 수 있다(L3→워크스페이스, L2→프로젝트).
  L1 개인은 작성자만, **삭제는 작성자·owner 유지**(파괴적이라 축이 다르다).

### 다음 할 일
`docs/WORK_TODO_2026-08-21.md` 의 권장 순서대로.

0. **Irene 결정·조치 3건** — 답만 주면 즉시 마무리
   - **#356** 운영 `EMAIL_ENCRYPTION_KEY` 미설정(JWT_SECRET 파생 fallback, `decrypt failed` 2회).
     키 교체 전 **기존 암호문 재암호화가 선행**. 운영 .env 변경 = Irene
   - **#362** nginx 한 줄 추가(root 권한) — `docs/OG_PREVIEW_NGINX.md`. 이게 있어야 SNS 미리보기가 산다
   - **#359** "Q record" 사용자 표기 존폐
1. **A-1 입력 유실**(#222 #367) — 쓴 글이 사라지는 계열
2. **A-2 그래프 숫자**(#254 #300 #255) — 공식 정본화 선행
3. **A-3 Q mail 7건**
4. **B-1 실시간 공동 편집** — 오늘 신규 요구("노션처럼"). 권한은 열었고 동시 편집이 남음.
   협업 패키지 미도입 확인. TipTap Collaboration + Yjs + 기존 Socket.IO. 설계 선행.

### 이 세션에서 확정된 것
- **조용한 실패가 세 번 나왔다** — ①`Client.email` 유령 컬럼(고객 검색이 늘 0건)
  ②업로드 실패가 문서 저장 전체를 죽임 ③IMAP 소켓 error 로 프로세스 사망.
  공통점: `.catch(()=>[])` · 삼켜진 예외 · 로그에도 안 남음. **양성 대조군 없이는 못 잡는다.**
- **가드가 네 번 내 실수를 잡았다** — t() 밖 한국어 라벨(래칫 384) · raw `<select>` ·
  멀티테넌트 마커가 탐지범위 밖으로 밀림 2회. 전부 커밋 전 교정. 무스코프 래칫 28 → 26.
- **함정 4가지(문서에 박제)** — DATEONLY 가 Date 객체로 와서 거짓 FAIL ·
  `PostEditor` 청크가 본체+스텁 2개라 스텁을 grep 하면 거짓 판정 ·
  /tmp 에서 node 실행 시 MODULE_NOT_FOUND · 배포 스크립트 exit 1 은 부수 신호(3점 검증).

---

## 복구 가이드

새 Claude 세션 시작 시 아래 내용을 붙여넣으세요:

```
이전 세션 이어서 작업하고 싶어.
/opt/planq/.claude/session-state.md 읽어줘.
그리고 docs/WORK_TODO_2026-08-21.md 랑 docs/FABLE_VERIFY_QUEUE.md 도 읽어줘.
```
