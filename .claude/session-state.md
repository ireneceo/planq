## 현재 작업 상태
**마지막 업데이트:** 2026-09-09 19:00 UTC (Opus 5)
**작업 상태:** **완료** · 진행 중인 작업 없음
**배포:** **v1.48.17** · commit `1d734cc8` · 20260909_171342 (322초, EXIT 0)
**백업(운영):** `/opt/planq/backups/20260909_171342`
**롤백:** `ssh irene@87.106.78.146 'tar -xzf /opt/planq/backups/20260909_171342/backend.tar.gz -C /opt/planq && pm2 reload planq-prod-backend'`

### 진행 중인 작업
- 없음

---

## 완료된 작업 (이번 세션 · 커밋 16건 · 운영 배포 1회)

관통 주제: **"같은 것을 여러 곳이 각자 적어 두면 갈라진다"**

1. **프로젝트 파일 탭 API 1,219건 폭주** — DocsTab `scope` 가 렌더마다 새 객체라
   useEffect 가 자기를 다시 부르는 고리가 됐다. 커넥션 풀 20/20 이 묶여 다음 화면이 45초 대기.
   → **1,219 → 31건**, 6.8s → 1.45s
2. **모바일 모달 15곳이 각자 `60px`** — 진짜 값은 `--pq-mobile-chrome`(56 + safe-top).
   앱 80 vs 60 = 20px 잘림. **브라우저는 4px 차이라 모든 검사를 통과**했다.
   가드(chromeoffset) + 카나리(네이티브 상태바 모사) 신설
3. **자동저장 ✓ 37건 → 7건** — `AutoSaveField` 는 껍데기라 아무것도 강제하지 않았다.
   가드(autosave 래칫) 신설. 예외는 **코드에 이유를 적어** 선언(`// autosave-exempt:`)
4. **종류별 휴가** — `leave_grants`·`leave_requests.category` ENUM 4종. 잔여·승인이 종류별로 갈린다.
   운영 마이그레이션 적용 확인
5. **휴가 잔여 안내** — 신청 자리에서 잔여·부족분을 말한다(전에는 승인 눌러야 알았다)
6. **#407 부분** — 채팅 원문이 LLM 으로 나가기 전 마스킹(`services/secretMask.js`)
7. **메일** — 주소 글자단위 줄바꿈 · 모바일 본문 흔들림 · 요약 주체 뒤집힘 ·
   액션 3개 아이콘화 · 초안 상태줄 · 주소 메뉴 2개 복구
8. **Q talk 헤더** — 공용 2밴드 규격으로(폰 3줄 → 2밴드)
9. **크래시 카나리(crash185)** — 운영 #185 8건의 경로를 양방향으로 돈다

---

## ▶ 다음 할 일

### 1. Google Picker — **키 발급 완료, 붙이는 것만 남음** (가장 먼저)
`/opt/planq/dev-frontend/.env` 배선 완료(gitignore 라 커밋 안 됨):
```
VITE_GOOGLE_PICKER_API_KEY   ✅ (39자)
VITE_GOOGLE_APP_ID           ✅ 765630237305
→ OAuth 클라이언트 ID 앞 숫자와 일치 확인 (다르면 Picker 가 파일을 안 넘긴다)
```
★ 프로젝트 번호는 콘솔이 아니라 **OAuth 클라이언트 ID 앞부분**에서 뽑았다
  (`765630237305-xxxx.apps.googleusercontent.com`). 다음에도 이렇게 하면 된다.

**왜 Picker 인가:** 우리 OAuth 는 `drive.file` 이라 **우리가 만든 파일만** 보인다.
사용자가 직접 올린 파일은 목록에 안 떠서 "이미 드라이브에 있는 걸 연결" 이 불가능했다
(2026-09-07 에 Irene 이 그래서 버튼을 빼게 했다 — 서버 라우트 `POST /api/drive/import` 와
`DriveImportSection` 은 살려 뒀다). Picker 로 고르면 그 파일이 `drive.file` 범위에 **들어온다**
→ Drive 전체 권한(Restricted, 구글 심사)을 안 받아도 된다.

**결정할 것 — Irene 안("연결만, 프로젝트로 안 들어오게")이 성립하는가:**
- Picker 로 고르면 바이트를 안 가져와도 우리가 읽을 수 있다 → "연결만" 이 기술적으로 가능
- 그런데 **팀원이 열려면** `permissions.create` 로 링크 공유를 켜야 한다 =
  **고객 드라이브 파일의 공유 설정을 우리가 바꾸는 것**. Irene 확인 필요
- 안 켜면 **올린 본인만 열린다**(2026-09-03 에 공유 링크가 죽은 원인이 정확히 이것)
- Irene 문구안 *"플랜큐 이외의 폴더 파일을 미리봅니다(위치이동 및 자동저장 없음)"* 는
  **"다른 사람은 못 연다"** 를 말해 주지 못한다 → 보완 필요
- 붙이는 자리는 `components/Common/AttachmentField.tsx` **한 곳**(업무·메일·문서 동시 적용)

★ Irene 확인 필요: 키의 **리퍼러 제한**이 저장됐는지
  (Websites: `planq.kr/*` · `dev.planq.kr/*` + API restrictions: Google Picker API).
  키는 빌드된 JS 에 공개되므로 그 제한이 **유일한 자물쇠**다.

### 2. Irene 결정 대기
- **#407 나머지 두 축** — 둘 다 **R=1**
  · **대화방 접근 좁히기**: `access_scope.js:160` 이 `isMemberOrAbove → true` 라
    **직원이면 그 워크스페이스 어느 대화방이든 읽는다**. 참여자로 좁히면 지금 보이던 것이
    안 보이게 되고 이미 나간 링크의 도달성이 바뀐다
  · **본문 암호화**: 서버가 열쇠를 쥔 암호화는 **직원이 읽는 것을 못 막는다**(그건 위 항목).
    막는 것은 DB 덤프 유출이다. 종단간으로 가면 검색·번역캐시·Cue 색인·알림 미리보기가 죽는다.
    **설계의 본체는 "무엇을 포기할까"** 다
  · 권고 순서: ①채팅에 비밀번호를 안 쓰게(Q info secret 유도) ②접근 좁히기 ③DB 암호화
- **#381·#382 Q sale** — 설계 v2(942줄)가 이번 주 코드 변경으로 낡았을 가능성.
  **재검토 먼저** 권고(§12-5 권한 게이트·§12-6 탭/라우팅이 이번 주 건드린 파일들)
- **main 을 origin 에 푸시할지** — 로컬이 앞서 있다

### 3. Fable 토큰 생기면 — 묶어서 한 라운드 (`docs/FABLE_GATE_QUEUE.md` **5건**)
기존 4건 + **5번 종류별 휴가**(운영 스키마 변경, R=1, **이미 배포됨** — 사후 검토)

### 4. 남은 부채
- 자동저장 ✓ **7건** — 전부 좁은 표 셀·목록 행이라 22px 배지가 값을 가린다(의도적으로 안 함)
- **UISPEC 검사기** — 장식용 span 높이를 컨트롤 높이로 센다. 요소 종류로 좁히려면
  703건 베이스라인 재작성 필요

### 5. 지켜볼 것
- **React #185** — `--suite crash185` 가 이제 막는다.
  운영은 `grep client-crash ~/.pm2/logs/planq-prod-backend-error*.log`
- 배포 후 확인 완료(18:31 UTC): **신규 크래시 0 · 신규 신고 0** · v1.48.17 72분 안정.
  마지막 크래시는 09-08T21:26(배포 전). **내일 한 번 더 확인**하면 좋다

---

## Git 상태
- `main` = 커밋 완료 · 워킹트리 깨끗
- 로컬 main 이 `origin/main` 보다 앞섬 — 푸시 여부는 Irene 결정

## 게이트 상태 (개발완료 시점)
- health-check **41/41** · 불변식 가드 **41/42** (미통과 1 = 문서 신선도 경고 전용)
- e2e `tenant` 실패 0 · 배포 전 `crash185,chromeoffset,mailband,mailframe,tenant,l1,sticky` 실패 0
- 빌드 **EXIT 0 / error TS 0** · Q위키 커버리지 통과(운영 반영 완료)
- ⚠️ **Fable 미검증** — 종류별 휴가는 R=1 인데 토큰이 없어 자체 검증만 했다

## 복구 가이드

새 Claude 세션 시작 시 아래를 붙여넣으세요:

```
이전 세션 이어서 작업하고 싶어.
/opt/planq/.claude/session-state.md 읽어줘.
```

운영이 이상하면 롤백(맨 위 명령). 백업은 `/opt/planq/backups/20260909_171342`.
배포 로그: `ssh irene@87.106.78.146 'pm2 logs planq-prod-backend --lines 50'`
