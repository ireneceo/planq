## 현재 작업 상태
**마지막 업데이트:** 2026-08-20
**작업 상태:** 완료 (섹션 닫음 — 노트북에서 이어서)

### 진행 중인 작업
- 없음

### 완료된 작업 (이번 세션)
- **팝아웃 핀 재설계** — 여는 것과 고정하는 것 분리(열 때마다 뜨던 교체 확인 팝업 제거) · 해제는 일반 창 복귀 / **고정창 X 는 전부 닫힘** · 홀더를 고정창 한가운데 은닉 · 고정창이 rAF 로 좌표를 밀어주는 즉시 추종(400px 점프 노출 3→0) · 다른 모니터 추종(창 관리 권한) · 첫 고정 Δ(0,0) · 세션 복원 잔재 창 자가 복구 · 좁은 폭 팝아웃에서 핀 유지 · 해제 시 보던 대화 유지. 신고 11건, 운영 배포 6회.
- **보안 3건 차단(전부 운영 반영·404 실측)**
  - 채팅 이미지: `/api/message-attachments/:id/raw` 삭제 + 응답 8표면에 서버 계산 `preview_url` 주입 + `/public` 접미사 매칭 차단
  - 업무 첨부: `/api/tasks/attachments/:id/raw` 삭제 (백필 전제를 dev·운영 640컬럼 전수 스캔 0행으로 반증)
  - 자료실: `/api/files/public-image/:storedName` 이 `e.png` 두 글자로도 200 이던 것
- **메일** — 로그인·인증 코드 메일을 확인권장으로. 원인은 패턴이 아니라 분기(대형 발송사가 수신거부 헤더를 달아 marketing 으로 먼저 분류돼 규칙이 도달 못 함).
- **할 일 총정리** — 운영 미완료 92건 원문 전수 → `docs/WORK_TODO_2026-08-20.md` (A군 43 / B군 / C군)
- **피드백 장부** — #280·#301·#305 답글 + done (운영 pending 91→88)
- 위키에 "팝아웃 창을 항상 위에 고정하기" 아티클 추가 (첫 고정 시 권한 안내 포함)

### 다음 할 일
`docs/WORK_TODO_2026-08-20.md` 의 권장 순서대로.

0. **Irene 결정 2건** — 답만 주면 바로 구현으로 넘어간다
   - 출퇴근·휴가 설계 승인 (`docs/ATTENDANCE_LEAVE_DESIGN.md`, Fable 2026-08-18 작성, 승인 대기)
   - 게스트 링크 미결 질문 6건 (`docs/GUEST_LINK_DESIGN.md` §12)
1. **A-1 Q info 12건** — `routes/kb.js` 의 batch/ai-ingest/csv-ingest 3경로가 공통 뿌리. 한 배치로.
2. **A-3 #261 메일 수신 누락** — 특정 메일이 아예 안 들어옴(데이터 손실이라 우선)
3. **A-2 주간 진척 그래프** — 리스트와 그래프 숫자 불일치. 공식 정본화가 선행
4. **B-5 보안 4건** — Q info 자격증명이 워크스페이스 전원에게 보이고 임베딩·번역으로 외부 LLM 에 나감
5. 나머지 A군 → B군

### 이 세션에서 확정된 규칙
- **Fable 은 한정 자원** — 돈·보안 경계·마이그레이션·신규 시스템에만. 나머지는 Opus 가 직접 검증하고 **"Fable 미검증(자체 검증)"** 을 명시한다. 같은 기능의 후속 수정은 묶어서 한 라운드로(팝아웃 하루 8회가 소모의 주범이었다).
- 브라우저 물리(되풀이 금지): 고정창은 권한이 있어도 스크립트로 못 옮긴다 · 권한 없으면 moveTo 가 현재 모니터에 갇힌다 · 가려진 창은 타이머가 늦춰진다 · PiP 배치는 우하단 EDGE 16 **outer 기준** · 자리 학습은 모니터를 넘기면 안 된다.

---

## 복구 가이드

새 Claude 세션 시작 시 아래 내용을 붙여넣으세요:

```
이전 세션 이어서 작업하고 싶어.
/opt/planq/.claude/session-state.md 읽어줘.
그리고 docs/WORK_TODO_2026-08-20.md 도 읽어줘.
```
