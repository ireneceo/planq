## 현재 작업 상태
**마지막 업데이트:** 2026-08-30 21:40 UTC (3차 세션)
**작업 상태:** 배포 **4회** 완료 · 커밋 21건 · **미배포 0건** · 미커밋 0건

### 진행 중인 작업
- 없음. #354·#227·#233·#378 전부 Fable 게이트 통과 + 운영 배포 완료

### 다음에 바로 할 것 — Fable 이 정한 순서
1. **#360 + #354 부활분** — 표는 **문서 종속 유지**(독립 승격 반려 — 운영 rows 6행뿐).
   ①Q docs 목록에 표(kind='table') 진입로 ②업무 산출물 저장 위치 지정 ③지정된 표에 완료 시
   행 append(문서 **개설**은 계속 사람이) ④죽은 화면 2개 삭제 **← 완료** ⑤빈 표 잔재 **← 완료**
   - 조사해 둔 사실: 행 추가 경로 `POST /api/records/:id/rows` 이미 있음(멤버검사+감사) ·
     `TaskDeliverableVersion` 은 이제 살아 있음(호출부 10곳·화면 3곳·운영 8건) ·
     `Task.cue_context_ref` JSON 주석에 이미 `post_ids` 자리가 있음
2. **#259 Phase 1** — 작성까지 허용 · 토큰 = 특정 Client 접속 수단 · `client_access_tokens` 신규 ·
   다운로드 허용/업로드 Phase 2 · **만료 30일 고정 + 회수 버튼**(결정 완료).
   고위험 5호라 절단면 → 설계 게이트 → 구현
3. #379 잔여 확정 · 4. #358 · 5. #353 잔여+#355 · 6. Q Sale(#381·#382)

### 하지 말 것 (Fable 지정)
- q_records 독립 메뉴 부활 · #259 Phase 1 에 게스트 **업로드** 포함 · #211/#361 자료 선제 작성

### 📌 다음 배포에 같이 나갈 것
- **Q위키 신규 아티클 2건** — `보면서 물어보기(채팅·메일 우측 패널 Cue)` · `Cue가 파일 내용을 읽고 답합니다`
  + 기존 `what-is-cue` 에 "Cue 를 부를 수 있는 세 곳" 갱신. dev 반영 완료(53건 업서트),
  **운영은 다음 배포 후** `ssh …prod "cd /opt/planq/backend && node seed-wiki-content.js"` 필요.
  (기능은 이미 운영에 나갔고 도움말만 아직이다)

### ⚠️ Irene 확인 사항 1건 (신규)
**L4(외부 공개) 문서를 공유하면 첨부 파일이 무인증으로 전부 내려받아진다.**
`posts.js:1510` 공개 첨부 다운로드가 **파일 등급을 보지 않고** share_token 만으로 서빙한다 —
"나만 보기"(L1) 첨부까지 나간다. 기왕·의도 설계로 보여 라우트는 건드리지 않았고,
**화면 문구만 사실로** 바꿨다("공개 링크를 여는 사람도 내려받을 수 있어요").
→ 이게 의도인지, 아니면 등급을 봐서 막아야 하는지 결정 필요.

### 차기 정리 대상 (Fable 기록 요청)
- **힌트를 승급 공식에서 직접 도출**로 — 지금은 격자 근사라 셀이 늘 때마다 같은 사고가 난다
  (L4+project · L1+project 셀은 아직 근사. dev·운영 0건이라 무증상)
- 기왕 무자격 SQL literal 2곳(`access_scope.js:403` calendar · `:588` postList) — 다음 접촉 시 자격
- post **목록** 술어의 청중없는 L2 비대칭(작성자/fullView 만) — 기왕, 노출 아님

## 이번 세션에 한 것

### 배포 1회 — v1.48.5 (17:09~17:14, 315초)
3점 검증 통과: 운영 헬스 ok(production) · 청크 해시 dev=prod `index-BKTEMYgh.js` · PM2 3개 재기동.
스크립트 exit 1 은 부수 신호(로그는 Deployment Complete). 릴리즈 노트 v1.48.5 자동 발행(ko/en 24블록,
`/insights/update-1-48-5`).

**배포 후 수동 백필 실행 완료** — 본문 이미지 9건 되살림(File #1123~#1131, 7.1MB 집계).
재실행 시 `본문 참조 36 · 이미 등록 36 · 되살릴 것 0` 으로 멱등 확인.

### 커밋 2건
| 커밋 | 내용 |
|------|------|
| `f2e7779c` | **#354 AI 루틴 설계 모드** — 영역·반복·지침·부하 + 모바일 좌측 메뉴 여백 |
| `870a4f3b` | 루틴 계약 안내가 언어를 안 가리던 것 — code 로 내리고 문구는 프론트가 |

### Fable 게이트 3회
- **#354 설계** → 조건부 승인. 원문 스키마에서 **3건 절단**(q_records 자동개설 · deliverable 컬럼 ·
  metrics 생성). 근거: 운영에 고아 q_record 실존 + `QRecordsPage.tsx` 는 **import 0곳인 죽은 코드**.
  #355 가 검증 데이터로 지목한 `q_records #9~#11` 은 **운영에 존재하지 않음**(내 조사 오류를 Fable 이 잡음)
- **#354 구현** → 조건부 PASS. 필수 조건 = **LLM 응답 절단 시 조용한 0건**
  (실호출 3회 중 1회가 85초 뒤 후보 0건 → "더 구체적으로 입력하라" 는 정반대 처방)
- **#354 재검증** → **PASS**. 스텁 6/6 + 실 HTTP 422. 특히 **부분 절단(tasks 온전)은 안 버림** 확인
- **#227 설계** → 진행 중

### 오늘 드러난 것 — "청중이 둘인데 한 벌만 있던" 계열
| 발견 | 정체 |
|------|------|
| `mode` 낙하 | `mode==='quick'?'quick':null` — 모르는 모드가 **조용히** 일반 분해로. 응답 에코로 봉쇄 |
| `Project.findByPk` | business 필터 없이 **남의 워크스페이스 프로젝트 설명**이 LLM 프롬프트에 |
| confirm 후보 상한 | **아예 없었다**. 조작 요청 하나로 수백 건 생성 가능 |
| LLM 이 null 원소 | `buildCandidates` 크래시 — 일반 분해도 같은 위험이었다 |
| 재요구가 결과 악화 | 영역 3→1 로 떨어뜨린 것을 내 코드가 **무조건 덮어썼다** |
| 재요구 JSON 절단 | "지침 더 길게" 가 12,000토큰을 꽉 채워 잘림 → 재요구 대상을 구조 결함으로 축소 |
| `finish_reason` | 게이트웨이가 **바로 이 용도로** 노출하는데 플래너가 안 읽었다 |
| shortfall 문구 | 한 문자열에 **LLM 과 사용자** 두 청중 → 영어 사용자에게 한국어가 나갔다 |
| `canvas:updated` | 내가 **수신부 0곳인 이벤트를 지어낼 뻔**. 실제 계약은 `project:updated` |
| `createAuditLog` | 이 파일은 import 안 한다 — 그대로 뒀으면 영역 생성 분기만 ReferenceError |
| 모바일 사이드바 | 메뉴 261px 인데 아래 **213px 를 시계·근태·계정이 고정 점유**(19개 중 5개만 보임) |
| safe-area 이중 | Sidebar 와 Footer 가 **둘 다** 넣어 실기기에서 그만큼 더 비었다 |

### 검증 수치 (전부 실행 결과)
실 HTTP 음성 8/8 · 양성 전경로 · 실브라우저 카나리 8/8 · 회차 상속 5/5 · 절단 반증 6/6 +
실 HTTP 422 · 가드 28/28 · health 37/37 · 빌드 exit 0 / TS 0 · 모바일 실측(메뉴 261→407px,
보이는 항목 5→9개).

### ⚠️ 내 판정기가 또 거짓말했다
- **`tsc --noEmit` 이 거짓 통과** — 빌드의 `tsc -b` 는 같은 코드에서 오류 3건을 잡았다.
  이후 검사는 **빌드와 같은 명령**으로만
- **모바일 측정 시 사이드바가 안 열린 상태로 쟀다** — 햄버거 대신 "메뉴 닫기" 를 눌렀다.
  이후 `data-testid` 로만 연다(텍스트 휴리스틱 금지)
- **빌드가 두 번 exit 143** — OOM(137) 아니고 세션 프로세스 그룹에 딸려 죽은 것.
  `setsid` 로 떼어내 해결

### Fable 이 나를 지적한 것
- **게이트 진행 중에 커밋했다** — 배포-중-수정 계열 위험. 다음부터 판정 후에 커밋한다
  (다행히 커밋본 `llm.js` 가 Fable 백업과 md5 동일이라 오염 없음)

### 장부 정리 — pending 19 → 14건
| 번호 | 처리 |
|------|------|
| #235 | 닫음 — 채팅 자동추출 실측(60초 디바운스·5건 즉시·3건 미만 skip, 후보 35 중 27이 자동) + 메일은 **계정 단위** scope |
| #273 | 닫음 — 외부컨펌 비활성 버튼 + 이유 표시 |
| #383 | 닫음 — Q Note 녹음 업로드(운영 원장 1,553초 단발 = 업로드 경로) |
| #230 | 닫음 — Today's 브리핑 실브라우저 확인(접힘 상태 요약 한 줄) |
| #233 | **안 닫음** — 부분 해결(검색·Cue 연결은 됨, "결과가 텍스트로" 는 미구현) |

---

## 정직하게 적는 것
1. **#354 는 장부 미닫음** — 커밋에 `Feedback-Closes: 230` 만. #354·#353 은 답글이 없어
   자동 닫힘 대상이 아니다. 운영에서 실사용 후 판단이 낫다
2. **#233 은 절반이다** — 회신에 그렇게 적었다
3. **UI 가드 모순** — `scripts/guard-invariants.js:1239` 메시지는 "토큰 36/40/44 밖" 인데
   정규식 `4[0-3]` 은 **40~43 도 위반으로 센다**. 내 코드가 걸린 건이라 가드를 손대지 않고
   44px 로 맞췄다. 가드를 고칠지는 별도 판단
4. **softCheck 문구는 프론트가 만든다** — 서버는 code 만. hard check 영문은 LLM 프롬프트 전용

## 🔑 Irene 만 할 수 있는 것
| 항목 | 내용 |
|------|------|
| **#259 결정** | 게스트 채팅 링크 — 열람만 vs 작성까지 |
| **#378 마지막 판단** | "청중 없는 L2" 문서 첨부를 어디까지 |
| **auto_skip 켜기** | 정기업무 상세 "지난 회차 자동 넘김" (운영 20 시리즈 전부 carry) |
| **아이폰 / Android / iOS 심사 / Google Drive** | 이전 세션과 동일 |

> ⚠️ Apple Developer 계정은 **help@wor-pro.com** — 2단계 인증 6자리가 그 메일함으로만 온다.

---

## Git 상태 (2026-08-30 19:30 UTC)
- 미커밋: **0건**
- HEAD: `da3ef8f2`
- 운영 반영 지점: `da3ef8f2` — **미배포 0건**
- 백업: `/opt/planq/backups/20260830_192205` (운영)

## 2차 배포 검증 (19:22~19:27, 320초)
3점 + 신규 의존성까지: 운영 헬스 ok(production) · 청크 해시 dev=prod `index-m6jbpcPm.js` ·
PM2 3개 재기동 · **pdf-parse 운영 설치 확인**(`PDFParse` 로드 = function — 이게 이번 배포에서
가장 조용히 실패할 수 있던 지점) · `from_address` 잔재 0 · 에러 로그 0 ·
프론트 testid `qtalk-cue-section`/`qmail-cue-section` + i18n ko/en 도달 ·
운영 실브라우저 200 / 페이지 오류 0 / 실패 요청 0.
- 백업: `/opt/planq/backups/20260830_170925` (운영)

## 복구 가이드
```
이전 세션 이어서 작업하고 싶어.
/opt/planq/.claude/session-state.md 읽어줘.
```
