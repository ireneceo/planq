## 현재 작업 상태
**마지막 업데이트:** 2026-09-09 17:30 UTC (Opus 5)
**작업 상태:** **완료** · 진행 중인 작업 없음
**배포:** **v1.48.17** · commit `1d734cc8` · 20260909_171342 (322초, EXIT 0)
**백업(운영):** `/opt/planq/backups/20260909_171342`
**롤백:** `ssh irene@87.106.78.146 'tar -xzf /opt/planq/backups/20260909_171342/backend.tar.gz -C /opt/planq && pm2 reload planq-prod-backend'`

### 이번 세션 — Irene 신고 묶음 + 종류별 휴가 + #407 부분

커밋 16건. 관통 주제는 **"같은 것을 여러 곳이 각자 적어 두면 갈라진다"**.

1. **프로젝트 파일 탭 API 1,219건 폭주** — DocsTab 의 `scope` 가 렌더마다 새 객체라
   useEffect 가 자기를 다시 부르는 고리가 됐다. 커넥션 풀 20/20 이 묶여 다음 화면이 45초 대기.
   → 1,219 → 31건, 6.8s → 1.45s
2. **모바일 모달 15곳이 각자 `60px`** — 진짜 값은 `--pq-mobile-chrome`(56 + safe-top).
   앱 80 vs 60 = 20px 잘림, 브라우저는 4px 차이뿐이라 **모든 브라우저 검사를 통과**했다.
   가드(chromeoffset) + 카나리(네이티브 상태바 모사) 신설
3. **자동저장 ✓ 37건 → 7건** — AutoSaveField 는 껍데기라 아무것도 강제하지 않았다.
   가드(autosave 래칫) 신설. 예외는 베이스라인이 아니라 **코드에 이유를 적어** 선언
4. **휴가 종류별 부여** — leave_grants/requests 에 category ENUM 4종. 잔여·승인이 종류별로 갈린다.
   운영 마이그레이션 적용 확인. **R=1 인데 Fable 토큰이 없어 자체 검증만 했다**
5. **#407 부분** — 채팅 원문이 LLM 으로 나가기 전 비밀값 마스킹(services/secretMask.js).
   나머지 두 축(접근 좁히기·본문 암호화)은 R=1 로 남는다
6. **크래시 카나리(crash185)** — 운영 #185 8건의 경로를 양방향으로 돈다

### 이번 세션의 교훈 (내 측정이 **일곱 번** 거짓말했다)
- **아무것도 안 하고 초록** — 경로가 안 바뀐 채 통과, 뷰포트 밖 클릭, 탭에 떠 있는 **다른 화면**의
  텍스트를 읽음. → 검사는 표식으로 겨냥하고, **"실제로 했는가" 를 따로 판정**한다
- **조상은 언제나 자식을 포함한다** — `✓` 를 가진 93px 래퍼를 22px 알약으로 집어 "가림=true"
- **양성 대조군이 틀릴 수 있다** — useEffect 무한 루프는 React 가 예외를 안 던진다(#185 는 렌더 중 setState)
- **빨간불이 하니스 탓처럼 보이게 두지 마라** — 앱이 죽어 selector 가 타임아웃하자 "하니스 오류" 로
  나왔다. 사람이 무시한다 → "🔴 앱 크래시" 로 구별하게 고쳤다
- **검사기가 남긴 데이터가 다음 검사를 죽인다** — 스레드에 연결한 고객을 원복 안 해 mailband 2건 빨간불
- **배포 스크립트는 마이그레이션을 이름으로 부른다** — 목록에 안 넣으면 영영 안 돈다
- ★ 운영 실측: 실제 사용자가 **하루 6시간 근무**다. 프론트에 8을 하드코딩했으면
  3시간 휴가를 "0.4일" 로 안내하고 서버는 0.5일을 깎았을 것이다

---

## ▶ 다음 세션 할 일

### 1. Irene 결정 대기
- **#407 나머지 두 축** — 대화방 접근을 참여자로 좁히기 / 본문 암호화. 둘 다 **R=1**
- **#381·#382 Q sale** — 설계 v2(942줄)가 이번 주 코드 변경으로 낡았을 가능성.
  **재검토 먼저**를 권고했다(§12-5 권한 게이트·§12-6 탭/라우팅이 이번 주 건드린 파일들)
- **main 을 origin 에 푸시할지** — 로컬이 앞서 있다

### 2. Fable 토큰 생기면 — 묶어서 한 라운드 (`docs/FABLE_GATE_QUEUE.md`)
기존 4건 + **휴가 종류(운영 스키마 변경)** 추가

### 3. 남은 부채
- 자동저장 ✓ **7건** — 전부 좁은 표 셀·목록 행이라 22px 배지가 값을 가린다(의도적으로 안 함)
- **UISPEC 검사기** — 장식용 span 높이를 컨트롤 높이로 센다. 요소 종류로 좁히려면
  703건 베이스라인 재작성 필요

### 4. 지켜볼 것
- **React #185** — `--suite crash185` 가 이제 막는다. 운영은 `grep client-crash ~/.pm2/logs/planq-prod-backend-error*.log`
- 배포 직후이므로 하루 내 신규 크래시·신고 확인

---

## Git 상태 (저장 시점)
- `main` = `1d734cc8` (워킹트리 깨끗)
- 로컬 main 이 `origin/main` 보다 앞섬 — 미푸시

## 게이트 상태 (배포 시점)
- health-check **41/41** · 불변식 가드 **41/42** (미통과 1 = 문서 신선도 경고)
- 빌드 **EXIT 0 / error TS 0**
- e2e `crash185,chromeoffset,mailband,mailframe,tenant,l1,sticky` **실패 0**
- 배포 후 운영 실동작 **4/4** (종류별 잔여 · daily_work_hours · 종류 필터 · 비밀값 마스킹)

## 복구 가이드
운영이 이상하면 롤백(위 명령). 백업은 `/opt/planq/backups/20260909_171342`.
배포 로그: `ssh irene@87.106.78.146 'pm2 logs planq-prod-backend --lines 50'`
