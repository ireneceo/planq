## 현재 작업 상태
**마지막 업데이트:** 2026-08-31 (Opus 5, 1M)
**작업 상태:** 완료 — 운영 배포 5회 · 커밋 6건

### 진행 중인 작업
- 없음

### 완료된 작업 (이번 세션)

**앱 출시 (심사 대기 중 — 이미 끝난 단계를 다시 시키지 말 것)**
- iOS: 빌드 **1.0(8)** 외부 그룹 `Beta Customers` 에 추가 → **베타 심사 대기**. 내부 테스트는 Irene 이 이미 완료
- 네이티브 앱이 **운영(planq.kr)을 가리키는 것 확인** — prod `refresh_tokens` 에 `client_kind='ios'` 31건
- Google Play: **조직 계정 생성 완료**(Account ID 6744981296049524067, DUNS 694053164) → **서류 승인 대기**
  · 승인 전에는 앱 생성·전화번호 인증이 잠긴다(오류 아님)
  · 승인 후 공개 Developer phone 을 회사번호(+827080803664)로 교체할 것
- 심사용 데모 계정 `appreview@planq.kr` / `PlanQReview2026!` (워크스페이스 `PlanQ Demo`, 대화·업무·문서 채움)
- `planq.kr/beta` 공개 페이지 배포 — 링크는 **관리자 > 플랫폼 설정 > "앱 다운로드 — iOS/Android"** 에
  붙여넣으면 **재배포 없이** 버튼이 켜진다 (값 없으면 "준비 중")
- 계정 정보는 운영 **Q info 표 #12 "앱 스토어 개발자 계정"**(워프로랩·나만 보기)에 박제

**기능·버그**
- 파일 앱에서 열기: HTML(샌드박스)·CSV/TSV(표)·마크다운·텍스트/코드
- Q File 워크스페이스 폴더 + Drive 폴더 동기화(생성·이름변경·이동) · 중복 트리 제거 · `+` 항상 표시
- 뒤로 가기: 모바일 헤더 + **데스크탑 탭 바**(오른쪽 끝, 화살표 아이콘, 자리 고정)
- 문서 저장 후 화면 갱신 · 첨부 문서 미리보기 공용화 · 업무 문서 첨부 결손 3곳
- 프로젝트 채팅·메일 버튼: 없으면 잠금 + "없음" · 채팅 자동 열기는 고객에게만
- Q task 상세 열기 `>` 잘림 수정(고정폭 자기 칸)
- 안드로이드 빌드 준비: `android-beta-check.js`(운영 고정 가드) · `android-set-cert.js` · Codemagic `android-play`

### 다음 할 일
1. **애플·구글 승인 대기** — 결과는 메일로. 승인 시 링크 복사 → 관리자 설정 붙여넣기(30초)
2. **모든 파일을 드라이브로** (Irene 승인된 방향, 미착수)
   - 드라이브 연결 시 워크스페이스 파일함·문서 첨부도 드라이브 경로로 (지금은 프로젝트·채팅만)
   - 그러면 자체 스토리지 0 · 사본 두 벌 문제 해소 · 영상 5GB
   - 개인 드라이브 연결(`ExternalConnection.owner_scope='user'`) — 나만보기 파일은 **본인이 연결한** 드라이브로
   - 드라이브 파일은 구글 편집기로 **워드·엑셀 열어 편집·저장**
   - 미연결이면 "수정하려면 연결이 필요합니다" 안내
3. **SMTP DKIM/DMARC 미설정** (Irene 콘솔 작업) — planq.kr DNS 에 레코드 없음 → 스팸 격리 위험
4. 구글 restricted 권한(드라이브 역방향 인제스트)은 **보류 결정** — 연 단위 CASA 비용

---

## 복구 가이드

새 Claude 세션 시작 시 아래 내용을 붙여넣으세요:

```
이전 세션 이어서 작업하고 싶어.
/opt/planq/.claude/session-state.md 읽어줘.
```
