## 현재 작업 상태
**마지막 업데이트:** 2026-09-23
**작업 상태:** 완료 (v1.58.0 운영 배포 — commit `f321b950`, 20260923_193046)

### 진행 중인 작업
- 없음

### 완료된 작업 (이번 세션)
- **선불 2개월 체험 선택지** — 「무료 14일 / 지금 결제하고 1개월 추가」. 자동 정기청구 **없이**
  첫 기간만 2개월. 정책 정본 `config/plans.js TRIAL_OPTIONS` · 기간 공식 `billing.js computePeriodEnd()` 하나 ·
  자격 `isFirstPlanPayment()` · 컬럼 `subscriptions.bonus_months`(멱등 마이그레이션, deploy 슬롯 등록)
- **#425 메일 제목 `[PlanQ]` 중복 제거** — 발신자가 이미 PlanQ. `subjectPrefix` 빈문자열 + 하드코딩 8곳 +
  `providerCredit` 2곳. 앞 공백은 `sendEmail` 한 곳. `[워크스페이스명]` 은 유지
- **#424 휴가 알림 딥링크** — `?leave=<id>` 를 읽는 곳이 0곳이었다. 관리자·신청자 양쪽 + 처리된 건 안내 +
  옛 링크(`?tab=team&leave=`) 착지 확인
- 카나리 2종 신설 — `prepay`(15) · `leavelink`(14). 둘 다 자기가 만든 데이터를 되돌린다

### ★ 이번 세션에서 내가 틀린 것 (반복 금지)
1. **범위를 부풀렸다** — 「자동구독」을 전제로 R=1 판정하고 Fable 을 부르려 했다.
   Irene: *"카드 결제 자동구독이 중요한게 아니라 결제를 시키는 것이 중요한건데."* 선불이면 필요 없다.
2. **"정기구독이 없다"를 크게 말해** Stripe 결제 자체가 안 되는 것처럼 들리게 했다.
   카드 1회 결제는 완성·운영 활성이고 2026-07-08 완료 보고는 거짓이 아니었다.
3. **거짓 초록 1회** — 반증에서 빌드가 `exit 2` 인데 「실패 0」을 읽어 **옛 번들**을 쟀다.
   이후 빌드 성공 확인 뒤에만 카나리가 돌도록 묶었다.
4. **판정기가 네 번 틀렸다** — 고정 sleep(렌더 전 측정) · 탭 keep-alive(숨은 사본) ·
   감싸는 상자를 카드로 오인 · 두 카나리가 DB 연결 공유. 전부 «기능 고장» 으로 보고할 뻔했다.

### 다음 할 일
- **#425 장부가 안 닫혔다** — 답글이 없으면 닫지 않는 규칙. 답글을 달아야 done 이 된다(문구 초안은 대화에 있음)
- **#424 본체는 미착수** — 휴가·휴일의 Q task 근무일수 반영 · 국가별 휴일 관리 · 기본 근무일수 통합.
  Irene 이 Fable 설계를 지시한 부분(S=1 · F=0). 현재 휴일은 Q task 손입력 숫자 하나(`weekly_holidays`)뿐
- **약관·개인정보·결제 안내** 에 선불 2개월·환불 기준 반영(Irene 지시, 기능 자리 잡은 뒤).
  ★ 이때 운영 `terms_version` 을 같이 올린다 — 지금 `1.0` 이라 2026-09-14 개정 약관도 재동의 모달이 안 뜬다
- **Fable 대기열 25건** — 오늘 429 4회. 한도 회복 시 2026-09-23 항목 4건(A 선불 · B 메일제목 ·
  C 딥링크 · D 배포배선)을 **한 번에**. D 는 배포 로그로 마이그레이션 순서 확인 필요
- 운영 신고 **#417 Q file**(폴더명 잘림·중첩 폴더·프로젝트 폴더 미표시·전체 다운로드·업로드 용량 안내) ← 다음 작업
- iOS 심사 — 9/22 제출, 승인·반려 메일 아직 없음(연결 메일함 동기화 정상). 승인되면 Irene 이 [출시] →
  운영 `platform_settings.app_ios_url` 교체는 내가

### 운영 반영 완료 (2026-09-23)
- v1.58.0 배포 · `migrate-subscription-bonus-months` 실행(컬럼 생성, 기존 12행 전부 0) ·
  릴리즈노트 발행 · 개발현황 id=163 · health/CSP/PDF 전부 OK
- **Fable 미검증(자체 검증)** — 자체 수치: API 21/21 · cron 9/9 · 메일 호출부 14곳 전수 24/24 ·
  딥링크 14/14 · UI/UX 24/24 · 반증 7종 · health 45/45 · guard 58/59 · build EXIT 0 · error TS 0

---

## 복구 가이드

새 Claude 세션 시작 시 아래 내용을 붙여넣으세요:

```
이전 세션 이어서 작업하고 싶어.
/opt/planq/.claude/session-state.md 읽어줘.
```
