## 현재 작업 상태
**마지막 업데이트:** 2026-08-29 (2차 세션)
**작업 상태:** 완료 (운영 배포 9회 · 커밋 25건 · 미배포 1건)

### 진행 중인 작업
- 없음

### 완료된 작업 (이번 세션)

**Q Note — 녹음이 안 되던 것 (Irene 신고)**
- `PCMStreamer` 가 `AudioContext` 를 만들고 **`resume()` 을 안 했다.** 정지 상태에서는
  `onaudioprocess` 가 **아예 발화하지 않는다** → 오디오 0바이트. WebSocket 은 열려 있어
  **오류 없이 조용히** 아무것도 녹음되지 않았다(운영 세션 #42: Deepgram 붙고 35초 뒤 종료, 오디오 0건).
- 실브라우저 실증: **정지=콜백 0회 / 깨움=13회**. 무음 감시(5초 안 0건)도 같이 붙였다.

**Q Note — 번역이 말 끝나야 나오던 것 (Fable 판정 이행)**
- 번역 트리거가 **발화 종료 하나뿐**이었다. Fable 이 고른 안: `is_final` 조각 경계에서만
  **커밋을 앞당긴다**(구두점+50자 / 200자 / 10초). 부분 번역·교체·스트리밍·모델 교체는 전부 기각.
- 60초 무휴지 발화에서 **첫 번역 60초 → 10초** · 짧은 대화는 강제 커밋 0회(회귀 없음).

**Q Mail**
- #384 **완결** — 답장 없는 메일 알림 + **메일마다 기간 설정**(3/7/14/30일·끄기)
- ★ **운영에서만 500** 이던 것 — `task_candidates` 스키마 드리프트(운영만 NOT NULL).
  **dev 는 영영 재현 안 되는 종류.** 전수 대조 마이그레이션으로 일반화
- 메일 업무후보가 **업무 인박스에 안 오던 것** (project 축으로만 걸렀다)
- 자동추출 Phase 1 — 계정별 `auto_extract_scope`(기본 off) · 10분 디바운스 · 백필 제외 ·
  사람 메일만 · 하루 20스레드 캡 · 설정 화면

**Q Drive v2 (#379)** — Fable 설계·구현 게이트 2회 통과
- 정본축 분리(`origin_provider`) · 조상 걷기 격리(fail-closed) · 인제스트 본체(쿼터·가시성) ·
  배선(권한 파생) · 화면(대기/활성). 운영 마이그레이션 적용 완료(백필 41행·md5 오염 3건 정리)

**UI 마감 (전수 조사 2회)**
- 1차: 눌림 98.3%·전환 71.2%·모달 등장 67% 없음 → `:where()` 명시도 0 으로
  **컴포넌트 1,362개를 안 건드리고** 바닥만 올림
- 2차: 드롭다운 등장(`data-popover` 계약) · 18×18 버튼 누르는 영역 44px(보이는 크기 유지)
- **CLS 는 전 화면 양호(≤0.04) → 손대지 않음** · 터치 타겟은 Phase 8 반응형으로 미룸
- 서버 오류가 **"데이터 없음"으로 위장**되던 것 4개 화면 · 업무 액션 실패 알림

**인프라**
- 푸시 **6% 유실**(재시도가 인증 실패에만 걸림) · webhook fail-open 봉합 ·
  업무추출 cron 의 `[Op.or]` 덮어쓰기 · 위키 캡처가 한 번도 저장된 적 없던 것

### ⚠️ 남은 것 / 정직하게 적는 것

1. **미배포 커밋 1건** — `4d9af99e` 메일 자동추출 Phase 1. 마이그레이션 2건 포함
   (`auto_extract_scope` 기본 off · `last_extracted_email_message_id`). 배포 파이프 등록됨.
2. **Drive v2 는 아직 안 켜진다** — Restricted `drive` scope 심사 전. 절차와 **활성화 전 필수 4건**은
   `docs/GDRIVE_FULL_SCOPE_ENABLE.md` 에 박제(detach 배선 · 최초 전체 스캔 · scope_exit · 웹훅 루프 상한).
   활성화 시점에 **Fable 게이트 재통과 필수**.
3. **한국어를 영어로 받아쓴 건** — 원인 미확정. 계정 언어는 둘 다 ko 인데 세션이 en 으로 시작됐다.
   회의 모달이 localStorage 초안에서 언어·번역까지 복원하는 것이 유력하나 **사용자 저장소를 볼 수 없어
   확증 못 함**. 어느 쪽이든 옳은 안전장치로 **24시간 지난 초안 폐기**를 넣어 뒀다.
4. **절차 반성** — Drive v2 배포를 Fable 게이트 **판정 전에** 개시했다(Fable 지적). 내용물은 안전
   판정이 나왔지만 순서가 틀렸다. 다음부터는 게이트 통과 후 배포.

### 다음 할 일

- **메일 자동추출 Phase 1 배포** 후 운영에서 계정 하나 켜서 실동작 확인(하루 20스레드 캡 실측 포함)
- **#383 Q Note 녹음파일 업로드** — ffmpeg/ffprobe 가 dev·운영 둘 다 없다. 길이를 모르면
  **과금 전 사전 검사를 못 한다** → 돈에 닿으므로 Fable 설계 게이트 선행
- **#382·#381 Q Sale** — 신규 시스템이라 Fable 설계 게이트
- 남은 운영 피드백 17건 (#378 · #361 · #360 · #358 · #355 · #354 · #353 · #343 · #273 · #259 · #233 · #230 · #227 · #211)

### 🔑 Irene 만 할 수 있는 것

| 항목 | 내용 |
|------|------|
| **Android** | Play Console $25 등록 → **앱 서명 키 SHA-256 지문**을 주면 assetlinks 치환 + 워크플로 + release 서명을 한 번에 붙임 |
| **iOS 심사** | App Store Connect → 「배포」 맨 아래 **App 심사 정보**에 데모 계정 입력 (`appreview@planq.kr` / `PlanQReview!2026` — 이미 만들어 둠) · 스크린샷 6.9" · App Privacy 설문 · 카테고리/연령등급은 「App 정보」 |
| **Google Drive** | Drive 전용 GCP 프로젝트 분리 + Restricted `drive` scope 심사 (4~8주) |

> ⚠️ Apple Developer 계정은 **help@wor-pro.com** — 2단계 인증 6자리가 그 메일함으로만 온다.

---

## 복구 가이드

새 Claude 세션 시작 시 아래 내용을 붙여넣으세요:

```
이전 세션 이어서 작업하고 싶어.
/opt/planq/.claude/session-state.md 읽어줘.
```
