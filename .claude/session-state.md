# PlanQ 세션 상태

## 현재 작업 상태
**마지막 업데이트:** 2026-07-31 (Opus 5, 1M)
**작업 상태:** 완료 — **운영 배포 4회 완주** (최종 `980af56`). 미배포 0.

### 진행 중인 작업
- 없음

### 🚀 2026-07-31 배포 4회
1. `cbee3ac` (208s) — 핀 홀더창·이월결함5·시간엔진 재설계·KB FK (+`actual_hours` 백필 6건·위키 시드 14/41)
2. `6d33587` (220s) — #242 Meet
3. `b06e261` (226s) — #216 · #226 메일 레이아웃 · #223 진척그래프
4. `980af56` (214s) — #234 · #219 · #224 · #218 · #243

### 완료된 작업 (이번 세션)

**배포 1 — `cbee3ac`**
- 팝아웃 핀 홀더창 구현 (`utils/pinHost.ts`) — 7-28 확정 설계의 구현.
  ★ 모바일 판정에 뷰포트 폭(`max-width:768px`)을 쓰면 안 된다 — 팝아웃 창 자체가 520px 라 데스크탑에서도
  항상 참이 되어 핀 버튼이 영영 안 뜬다. `(hover:none),(pointer:coarse)` 로 교체.
- 시간 자동누적 엔진 재설계 — status 체류 합산 폐기, focus 세션 실측만 (`taskActualHours.js`).
  `TaskStatusHistory.afterCreate` 훅 제거 (X-lock 트랜잭션 밖에서 tasks 를 UPDATE 하던 락 지뢰)
- 이월 결함 5건 + KB FK 정합
- 배포 후: `actual_hours` 백필 6행 (task 24: 153.6h→0.0h, 사용자 직접입력 42 보존, 재실행 변경 0), 운영 smoke

**배포 2 — `6d33587`**
- #242 Meet — 생성 블록을 트랜잭션 **밖으로** + 롤백 대신 `meetWarning` 코드,
  reconcile 게이트 `!params.autoCreateMeeting` → `!finalGcalEventId`

**배포 3 — `b06e261`**
- #216
- **#226 메일 HTML 붕괴** — 진범은 DOMPurify 의 `ALLOWED_URI_REGEXP` 가 URI 속성이 아니라
  **모든 속성값**에 적용된다는 것(purify.js:1805). `align`/`width`/`colspan` 이 전멸해 레이아웃이 무너졌다.
  두 정화기가 공유하는 `SAFE_URI_RE` 로 교체.
- **#223 주간 진척 그래프 평평** — 화면은 죽은 코드(`WeeklyReviewView.tsx` 는 어디서도 import 안 됨)가
  아니라 `report_units` 였다. `TaskDailyProgress` 행이 일별 증분이 아니라 **task 생애 누적**이라
  주간 창을 잘라도 선이 눕는다 → task별 기준선 차감 + `Math.max(0,…)` 클램프 + 미래일 절단.
  운영 실측 196.7h 평평 → 0→3.7h 상승 확인.

**배포 4 — `980af56`**
- **#234** 문서→Q info 이관이 한 덩어리로 붙음 — kb import 가 posts 의 **검색용 평탄화 필드**
  (`content_text`)를 우선 써서 개행이 애초에 없었다. `content_json` 우선으로 뒤집고 `<p>`/`<br/>` 변환 저장.
- **#219** RichEditor 가 평문 개행을 삼킴 → `plainTextToHtml` 신설 (기존 HTML·taskList `data-type` 무손상)
- **#224** 구글 캘린더 팝업 안 닫힘 — accounts.google.com 의 **COOP 가 opener 를 끊어** postMessage 와
  팝업 자신의 `close()` 가 함께 죽는다 → BroadcastChannel·localStorage 로 알리고 **부모가 close**
- **#218** 프로젝트 셀렉트 라벨 축약 / **#243** 긴 이메일 주소 드롭다운 줄바꿈

**그 외**
- 운영 피드백 전수 점검 — 이미 구현된 22건 실코드 대조 후 `done` 처리. 미처리 **54 → 27**
- Fable 게이트 FAIL 2회 모두 실제 결함이었고 수정 후 재검증 PASS
  (#234 는 내 수정이 `content_text` 가 빈 예외 경로에서만 도는 **죽은 코드**였다)
- ★ 배포2의 프론트 빌드가 도는 중에 #216 을 편집해 **검증 안 된 코드가 운영에 실렸다**
  (사후 검증 PASS 로 롤백은 면함). **배포 중 소스 수정 금지**를 규칙으로 채택

### 다음 할 일

**Irene 조치 (코드로 불가)**
1. **#224 실팝업 확인** — 구글 캘린더 연동해 팝업이 자동으로 닫히는지 (내일 예정)
2. **워프로랩 Google Calendar 재연동** — 운영 토큰 스코프가 `userinfo.email openid` 뿐이라
   재연동 전에는 #242 Meet 코드가 배포돼 있어도 동작 안 함

**개발 (잔여 피드백 27건)**
- 확인된 미수정: #215 메일 첨부파일(미리보기·다운로드) · #217 증빙 발행마킹 후 탭 숫자 미갱신 + 발행 알림메일 ·
  #221 메일 분류 기준 재정비 + 탭 명칭 통일("검토권장"→"확인권장") + AI 답변 초안 버튼 전 탭 노출 ·
  #222 새 메일 폼 자동저장·이탈 · #225 문서 워드/PDF/엑셀 다운로드
- 설계 선행(Fable): #208 출퇴근·휴가 · #227 Cue 우측패널 · #229 프로젝트 히스토리 · #230 Today's 브리핑 ·
  #233 통합검색 AI · #236 업무 태그 · #237 오늘 나의 업무 탭 · #239 문서 컨펌 · #240 프로젝트 완료 알림

**후속 정리 (Fable 관찰)**
- kb body 렌더 sanitize 일괄 적용 검토 — 본문에 문자 그대로 `<b>` 를 타이핑하면 HTML 판정 휴리스틱이
  escape 를 건너뛴다 (이번에 생긴 표면 아님, 쓰기 주체는 워크스페이스 멤버 한정)
- 프로젝트 스코프 `progress_series` 에도 #223 과 같은 기준선 차감 적용
- 라이브 그래프·SPI 의 Δ 정의를 리포트(주간 Δ)와 정렬 — 현재 과도기 비대칭

---

## 복구 가이드

새 Claude 세션 시작 시 아래 내용을 붙여넣으세요:

```
이전 세션 이어서 작업하고 싶어.
/opt/planq/.claude/session-state.md 읽어줘.
```
