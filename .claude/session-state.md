## 현재 작업 상태
**마지막 업데이트:** 2026-08-30 (2차 세션)
**작업 상태:** 커밋 3건 완료 · **미배포 4건** · #378 조사 진행 중

### 진행 중인 작업
- **#378 잔여분 조사** — 문서 상세의 첨부·본문 인라인 이미지가 파일 메뉴에 안 잡히는 것.
  조사 항목: post 첨부가 File 레코드를 만드는가 · 인라인 이미지 업로드 엔드포인트 ·
  파일 목록 WHERE 조건 · 기존 정본 패턴(task/email 첨부) · 운영 실측 수치.

### 완료된 작업 (이번 세션) — 커밋 3건, 전부 미배포

**① 정기업무 — 지난 회차 자동 정리가 "생성 대상에만" 돌던 것 (`25da0469`)**
- #349 는 주석까지 달아 놓고 `generateOneSeries` **안쪽** 조기반환만 고쳤다. 바깥 parent 조회의
  `next_occurrence_at <= 오늘+7` 이 남아 정리는 여전히 생성의 곁가지였다.
- 피해 폭: 월간 한 달 중 3주 · 분기 11주 · 연간 51주 · **종결 시리즈(next NULL)는 영영**.
- auto_skip 시리즈를 독립 조회해 정리. `out.skipped` 집계 노출.
- **운영은 20개 시리즈 전부 `carry` 라 지금 바뀌는 데이터 0건** — "켜면 도는" 상태를 만든 것.
- ★ Fable 이 운영에서 **자기참조 시리즈 4건**(id 168~171, biz 6) 발견 — `recurrence_parent_id =
  자기 자신` 이라 cron 에서 영영 빠지는데 회차 통계는 오염. **parent_id 만 풀면 next 가 과거(7/28)라
  캐치업이 즉시 과거 회차를 쏟아낸다** → 세 컬럼 동시 NULL.
  `dev-backend/scripts/fix-self-referencing-recurrence.js` (dry-run 기본 · `--expect=N` 안전핀 · 백업 JSON).

**② #353 AI 업무추가 배관 4개 (`c9581578`)**
- 반복(RRULE 직접 수신) · 업무그룹 배치(배관만 끊겨 있었다) · 장문 지침(8,000자, 초과 시 재생성 1회 후 절단+표시) ·
  의존 링크 착지(여태 `depends_on_index` 가 확정 시 **버려졌다**).
- priority(5번)는 **제외** — 컬럼 신설 필요 + priority_order 오염(Fable 판정). **부분 해결이라 장부 미닫음.**
- ★ RRULE **단일 관문** `sanitizeRRule` 을 세 문(AI 확정·일반 생성·수정)이 같이 쓴다.
  ★★ Fable 이 **이중 FREQ 우회**를 실증했다 — `FREQ=DAILY;FREQ=HOURLY` 는 정규식이 앞을,
  파서가 뒤를 채택해 통과했다. 판정을 **파싱 결과 freq** 기준으로 바꾸고 FREQ 중복 자체를 거절.
- 구현 중 `ProjectWorkstream` 컬럼이 `name` 이 아니라 **`title`** 이라 첫 코드가 SQL 에서 죽는 상태였다
  (그대로면 AI 업무추가 전체 500). 검증에서 잡음.

**③ Cue 검색어가 팝아웃 창에 전달 안 되던 것 (`90cd854d`, Irene 신고)**
- `cue:ask` 가 **window 이벤트**라 창 하나 안에서만 산다. Q helper 는 별도 창(/help-popout)으로
  띄워 쓰는 도구라, 팝아웃 사용자에게는 검색어가 뒤에 가려진 본 창 드로어로 갔다(실측: 팝아웃 "" / 본 창 값 있음).
- `utils/cueAsk.ts` 단일 진입점 — 팝아웃 심박(localStorage 3초/9초 만료) 살아 있으면
  `planq:popout` BroadcastChannel 로 전달 + 그 창 포커스, 본 창 드로어는 안 연다. 없으면 종전대로.
- 호출부 4곳(통합검색·HelpDot·Q task 빈 상태·확인필요)을 `askCue()` 로 통일.

### Fable 게이트 2회
- **설계** → 1~3단계는 이미 구현돼 있음을 운영 실증으로 확인, #349 반쪽 구멍 + 자기참조 4건 발견.
  백필 잔여(완료된 과거 회차 start_date 10건 · reviewer 3건)와 auto_skip 일괄 전환은 **하지 않기로** 판정.
- **구현 검증** → **조건부 PASS**. 조건(이중 FREQ 우회) 이행 완료 + 권고 3건 반영
  (같은 관문을 생성·수정 경로에 이식 / 중간 실패 시 링크 미착지를 한계로 박제 / 거짓 주석 정정).

### 검증 수치 (전부 실행 결과)
정기업무 10/10 · 정리 스크립트 11/11 · #353 실HTTP 20/20 · 결정적 반증 19/19 ·
우회 봉쇄 9/9 · 무회귀 6/6 · advanced 보존 15/15 · Cue 실브라우저 7/7 ·
가드 28/28 · health-check 37/37 · 빌드 exit 0 / TS 0.
★ advanced 보존은 첫 판정이 **PUT 404 인데도 "통과"** 로 읽혔다(거짓 통과) — 양성 대조군(제목이 실제로
  저장됐는가)을 넣어 잡았다. 검증 스크립트는 전부 삭제, fixture 는 원복 확인.

### ⚠️ 배포 시 해야 할 것 (배포 스크립트 **밖** 수동 단계)
1. **위키 시드** — `ssh …prod "cd /opt/planq/backend && node seed-wiki-content.js"` (멱등).
   운영 `help_articles` 44건에 `upload-recording`·`text-size` **없음** 실측.
2. **자기참조 4건 정리** — 배포 후 `node scripts/fix-self-referencing-recurrence.js` (dry-run, 4건 확인)
   → `--apply --expect=4`. 백업은 `backups/self-ref-recurrence-*.json` 자동.
   ※ 배포 스크립트에 넣지 않았다 — 일회성 데이터 정정이고, expect 불일치 시 배포를 중단시키면 안 된다.
3. 마이그레이션은 **불필요** (Fable 확인 — 필요한 컬럼 전부 운영에 실존).

### 다음 할 일
- **#378** 문서 상세의 파일·이미지를 파일 메뉴에도 (조사 중)
- **#354** 루틴 설계 모드 — #353 배관이 착지했으므로 이제 가능 (Fable 이 "다음 사이클" 로 판정)
- **#382 · #381 Q Sale** — 신규 시스템이라 Fable 설계 게이트 선행
- 남은 운영 피드백 19건 (#383 #382 #381 #379 #378 #361 #360 #358 #355 #354 #353 #343 #273 #259 #235 #233 #230 #227 #211)
- **#383 은 기능이 운영에서 동작 확인됐으나 장부는 pending** — 답글이 없어 자동 닫힘 대상이 아니다

### 🔑 Irene 만 할 수 있는 것
| 항목 | 내용 |
|------|------|
| **배포** | 미배포 4건(위키 시드 포함). `/배포` 지시 필요 |
| **auto_skip 켜기** | 정기업무 상세에서 "지난 회차 자동 넘김". 프로젝트 10 의 평일 데일리 3건(#225·226·227)이 적합 후보 — 켜면 다음 자정에 밀린 5건(244·246·247·251·253)이 자동 정리된다. 임의로 켜지 않았다 |
| **아이폰 확인** | 글씨 크기 · 메일 알림 딥링크 · 입력창 포커스 확대 |
| **Android / iOS 심사 / Google Drive** | 이전 세션 상태 참조 (변동 없음) |

---

## 복구 가이드

```
이전 세션 이어서 작업하고 싶어.
/opt/planq/.claude/session-state.md 읽어줘.
```
