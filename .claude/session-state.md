## 현재 작업 상태
**마지막 업데이트:** 2026-09-07 15:20 UTC (3차 · 운영 배포 완료)
**작업 상태:** 완료 · **운영 배포함** (`8d7d7fc2` 15:04 + 알림 수정 `2bdd48ce`)
가드 35/35 · health 41/41 · 카나리 실패 0 · 빌드 EXIT 0

### 진행 중인 작업
- 없음

### 완료된 작업 (이번 세션)
- **모든 팝업이 최상위로** — `StandardModal` → body 포털. 사이드바가 만든 층(z-index:100 + transform)에
  갇혀 우측 패널 뒤로 깔리고 태블릿·폰에선 화면 밖이었다. 전면 백드롭 74개 중 66개 미포털 → `modalportal` 래칫
- **메일 본문 높이 (CSP 회귀)** — `srcdoc` 은 부모 CSP 를 물려받는다. `script-src 'self'` 가 높이 보고
  인라인 스크립트를 차단해 636px 내용이 200px 추정치 상자에. 스크립트를 `public/mail-frame.js` 로 분리
- **업무 책임선 — 직급 예외 제거** — description=작성자만 / body=담당자만. 운영 권한(삭제·이관)은 유지
- **업무 시작·재개 실시간** — 포커스 위젯에 소켓 리스너가 없었다(30초 폴링) → broadcast + 리스너. 302ms
- **웹 IA** — /beta·/app 통합 · 사용가이드를 인사이트에서 제거(위키에 이미 있었다) · 새 소식을 인사이트로 · 도움말 새 탭
- **브랜드스토리** — 지어낸 인용문을 운영 Q docs 원문으로 교체(14문단 ko/en)
- **모바일 다발** — 메일 별표 제거·라벨→태그·밴드2 1줄 / 프로젝트 필터 3줄·뒤로가기·핀 서버 이관 /
  Q Task 제목 편집 제거·행 끝 화살표 제거·태그 메뉴 키보드 / Q Note 접기 위치·참여자 바 중복·`ended` 상태
- **아이폰 알림 유도** — 거부 시 "설정 열기"(iOS) + 사운드 안내
- **Q info 추출** — 문서 제목 전달 + 값·분리 규칙. 14항목·값 0 → **25항목·값 18**
- **배포마다 릴리즈 노트 초안 자동 생성** (미발행 — 사람이 다듬어 발행)
- **휴가 부여** — 이미 구현돼 있었고 정상 작동 확인(실호출 7/7)
- 신규 검사기 5종 + 가드 1종, 전부 양성 대조군으로 반증 확인

### 완료된 작업 (2차 세션 — "Q sale 말고 하던 거 다해줘")
- **확인 필요 3건이 2건으로 보이던 것** — `collectTasks` 에 '보낸 업무요청' 수집기가 **아예 없었다**.
  QTaskPage `panelCounts.sent` 와 같은 술어로 추가. 내가 pending 컨펌자면 중복으로 안 센다
- **Q Task 사이드바 배지** — 다른 메뉴엔 다 있는데 `/tasks` 엔 배지 코드 자체가 없었다.
  응답에 `taskCount` 추가(= collectTasks 산출물 수) + NavItem 배지. 실측 "3" @(184,344) 보임=true
- **폴더 드래그&드롭** — 서버 이동 API 는 있었고 화면에 드롭 존이 없었다. 전용 MIME 으로만 받는다
- **다운로드 %** — 목록 행에 다운로드 버튼 신설(전엔 미리보기를 열어야만 받았다) · ZIP 진행률 · 메일 첨부 진행률
- **이미지 느림** — 리사이즈는 잘 되고 있었다(1.37MB→31KB). 라이트박스가 원본을 그대로 열던 것이 원인.
  1600px 먼저 → "원본 보기" 또는 확대 시 원본
- **구글 드라이브에서 가져와 첨부** — `POST /api/me/drive/import` 신설, 통합 컴포넌트에 넣어 전 화면 적용.
  워크스페이스 미러와 **같은 함수**(`services/driveImport`) — 쿼터·dedup·감사·브로드캐스트 공유
- `external_connections.js` 539줄 → `routes/personal_drive.js` 분리 (라우트 유실 0)

### ⚠️ 2차에서 새로 확인된 것
1. ~~dev Drive 연동 끊김~~ → **오진이었다(3차에서 정정).** 끊긴 적 없고 옛 폴더 id 문제였다.
   자가복구를 넣어 해소. `gdrivesync` 카나리는 다시 돌려볼 것.
2. **RM 은 통화 설정이 아니라 운영 문서 원문**이다 — Vin Design(말레이시아 업체) 견적서에 적힌 링깃.
   코드상 통화는 KRW/USD/EUR/JPY/CNY 5종뿐이고 RM 은 없다. 1차 답변을 정정한다.
3. **문서 편집 열기(Word·Excel)는 안 했다** — 정책 결정이 필요하다(아래 '다음 할 일' 참조)

### 완료된 작업 (3차 — 배포 + 드라이브 정정)
- **내가 틀린 것: 드라이브는 끊긴 적이 없다.** 토큰·계정·폴더 전부 멀쩡했고 **DB 가 옛 폴더 id**
  를 가리켜 404 가 났다. `ensureRootFolder` 자가복구 신설 — 이름으로 찾아 재연결한다
  (실측 `1UB_jef…`→`1a1lOPK…` 복구)
- **공유(팀) 드라이브가 구조적으로 안 보이고 있었다** — Drive API 는 `supportsAllDrives` 없이
  공유 드라이브 항목을 **무조건 404** 로 준다. 그 파라미터가 코드에 **하나도 없었다**. 전 호출에 추가
- **첨부 범위 정정** — 2차에서 개인 Drive 를 공용 첨부에 붙인 것이 정책 위반이었다
  (memory `project_gdrive_policy` 옵션 B). 공용=팀 드라이브 / 개인 Drive=개인 자리만. 서버도 차단
- **컨펌 요청이 요청자에게 안 갔다** — notify 대상이 컨펌자뿐이었다. "컨펌 진행" 알림 추가,
  요청자==컨펌자면 한 번만 울린다. 카나리 `reviewnotify` 4/4

### ⚠️ 다음 세션이 먼저 볼 것
1. **Fable 게이트 구멍** — `project_pinned_docs` 운영 마이그레이션은 R=1 이라 올렸어야 했는데
   올리지 않고 배포했다(멱등·추가 전용·실행 확인은 했다). 게이트 지문은 *미커밋 diff* 에 묶여
   **커밋을 먼저 하면 조용히 초록**이 된다 — 다음부터 **커밋 전에** `/fable-검증`.
2. **Q Task 사이드바 배지 미구현** — Q talk·Q mail·Q Bill·확인필요엔 있는데 Q Task 엔 배지 코드가 없다
3. **확인필요 집계** — `받은 업무요청` + `확인 요청 받음` 만 센다. `보낸 업무요청` 은 빠져 있다(정책 판단 필요)
4. **파일/드라이브 4종 결손** — ①폴더 드래그&드롭 없음 ②다운로드 % 진행률 없음 ③문서 편집 열기 없음
   ④구글 드라이브에서 가져와 첨부 없음. 미리보기는 이미지/PDF/텍스트/zip 지원, **리사이즈는 이미지에만**
   (이미지가 느린 이유). 통합 컴포넌트(`FilePicker` + `PreviewArea`)가 하나라 한 곳에서 넣을 수 있다
5. Q info 아주 긴 가격표는 60항목 상한에 닿을 수 있다(닿으면 화면이 알린다)

### 다음 할 일
1. **문서 편집 열기 — Irene 결정 필요.** 브라우저에서 Word·Excel 을 편집하려면 외부 편집기가 필요하다.
   Drive 링크는 **연결한 구글 계정 본인에게만** 열린다(우리는 `permissions.create` 를 안 한다) —
   2026-09-03 에 공유 링크가 죽은 그 구조다. 선택지: ① Google Docs 연동 + 공유 권한 부여
   ② Office Online(Microsoft 계약 필요) ③ 자체 에디터로 변환(서식 손실). 임의로 안 골랐다.
2. **Q Sale 사이클 1 착수** — `docs/Q_SALE_DESIGN.md` (942줄). 설계 끝, 구현 0.
3. **운영 릴리즈 노트 초안 다듬어 발행** (id=66 등 미발행)
4. **팀 드라이브 전면 지원 점검** — `supportsAllDrives` 는 넣었지만 **공유 드라이브에 PlanQ 폴더를
   두는 흐름**(연결 시 위치 선택)은 아직 없다. 지금은 연결 계정의 My Drive 에 만든다.
   공유 드라이브 **목록** 조회는 `drive.file` scope 로 불가(scope 결정 필요).

### 실기기 확인 대기
- 메일 상세 빈 여백 해소 · 태블릿↔데스크탑 업무재개 즉시 반영 · 아이폰 알림 소리와 "설정 열기"
- 브랜드스토리/앱/인사이트 화면 · 태블릿 세로 메뉴 열고 출근 알림

### Git 상태
- 브랜치 main
- `5ae6eeb1` fix: 메일 본문 높이 — CSP 가 높이 보고 스크립트를 막고 있었다 (운영 배포 09:15)
- `ac1f2853` fix: 업무 시작·재개 실시간 반영 · focus/stop 500 · 검사기 오염 제거 (운영 배포 08:16)
- `48d879df` fix: 팝업 최상위 · 업무 책임선 · 모바일 다발 · Q info 추출 · 웹 IA (운영 배포 06:52)
- 운영 롤백: `ssh irene@87.106.78.146 'tar -xzf /opt/planq/backups/20260907_091*/backend.tar.gz -C /opt/planq && pm2 reload planq-prod-backend'`

---

## 복구 가이드

새 Claude 세션 시작 시 아래 내용을 붙여넣으세요:

```
이전 세션 이어서 작업하고 싶어.
/opt/planq/.claude/session-state.md 읽어줘.
```
