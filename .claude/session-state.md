# PlanQ 세션 상태

**마지막 업데이트:** 2026-08-19 (2차 세션, Opus 5 1M)
**작업 트리:** 깨끗 (미커밋 0) · **미배포 1커밋** — `ab6567b1` (Fable PASS, 배포 지시 대기)

---

## 다음 섹션에서 할 일 (Irene 지시 2026-08-19)

1. **팝아웃 핀 — 설계부터 Fable 과 상의** ("마음대로 거지같이 만들지 마. 나중에 fable이랑 상의해")
   → `docs/PENDING_FABLE_VERIFICATION.md` **C 섹션**에 신고 원문(#26·#43·#258·#280·#286)·
     실측한 브라우저 물리 4가지·선택지 3개를 전부 박제해 뒀다. Opus 가 임의로 고르지 않는다.
   → 핵심 미해결 질문: **"항상 위" 창을 2개 이상 두는 것은 웹에서 불가능하다.**
     #286 이 이미 물었다 — "이게 PWA 문제면 그냥 알려줘. 네이티브앱을 ios나 윈도우 데스크탑
     용도 만들어야 한다는 거면." **이 질문에 대한 답을 Irene 에게 정식으로 줘야 한다.**
   → 오늘 만들다 되돌린 작업본: scratchpad `pin-wip/` (세션 종료 시 소멸 — 다시 만들면 된다)

2. **오늘 개발분 Fable 검증** — `docs/PENDING_FABLE_VERIFICATION.md` A 섹션(소급 5건) + B-1.
   오늘 마지막 배치 `ab6567b1` 은 Fable PASS 완료(①~④), **배포만 남았다.**

3. 그 뒤 B군 개발 재개. **오늘은 여기서 중단했다** — #305(검색 전 최근 항목) 백엔드 착수분은
   Irene 지시로 되돌렸다(커밋 없음).

---

## (이전 세션이 남긴) 다음 작업 후보

1. **`docs/MAIL_SIGNATURE_I18N_DESIGN.md` 작성** (Irene 지시 2026-08-19)
   서명·표시 이름을 언어별(ko/en)로. 지금은 세 단계(별칭·메일함·워크스페이스) 모두 컬럼 1개라
   **AI 초안과 인용 머리말은 영어로 나가는데 서명만 한국어**가 붙는다.
   - 저장: PlanQ 관례인 JSON 다국어 컬럼(`name_localized` 계열) → `signature_localized {ko,en}`.
     기존 `signature_html` 은 ko 기본값으로 남겨 옛 데이터 무손상
   - 언어 선택 기준은 **이미 있는 것 재사용**: 답장 = 받은 메일 언어(`detectLang`, AI 초안·인용
     머리말과 같은 기준) / 새 메일 = 작성 화면 언어 + 컴포저에 언어 선택
   - 표시 이름도 같이 간다 — 서명만 영어인데 발신자가 `워프로랩` 이면 더 어색하고,
     워크스페이스엔 이미 `brand_name_en` 이 있어 기준이 갈라진다. 단 en 비면 ko 폴백
   - 자동 발송 메일은 1개 유지
   - ★ **스키마 변경 + 발신 경로(resolveSender) 수정 = Fable 3단 게이트 필수.** 설계 문서만 쓰고 대기
2. **`docs/PENDING_FABLE_VERIFICATION.md` 신설** — 검증 대기 목록.
   ① 오늘 검증 없이 배포된 4커밋(소급) ② 이후 개발해 커밋만 해둔 것
3. 아래 B군 착수 (Fable 없이 가능)

---

## 오늘 완료 — 커밋 11건, 전부 운영 반영

**메일 (사이클 2A·2R·3·4)**
- 별칭 주소로 답장이 안 나가던 것 · 표 인라인(에디터 표에 `pq-table` 계약) · 답장 인용(받은 메일 언어 자동)
- 읽기 전면: 최신이 위 · **인용 접기**(986KB 메일 가시 텍스트 71%→13%) · 펼침 앵커(보낸메일함만 outbound)
- 발송 낙관 반영(실패 시 롤백) · 주소/도메인 편집 · 편집기 툴바 · 설정 IA(회사 메일함/자동 발송 메일)
- **도메인 인증 진단(SPF/DKIM/DMARC)** — DKIM 에 "없음" 판정을 만들지 않는다(selector 미상이면 부재 증명 불가)
- ★ **성능**: 상세 응답 2,036KB → **8.6KB**. 원인은 렌더가 아니라 회선 — 본문의 97~100%가 base64 이미지였고
  nginx `gzip_types` 주석 처리로 JSON 이 **압축조차 안 되고** 있었다. compression 미들웨어 + 본문에서
  이미지 분리(펼친 메시지 것만 지연 로드, DB 무변형)
- 주소별 서명 UI(백엔드는 원래 지원 — 화면만 없었다) · 별칭 문구(`이 메일함 주소`/`새 메일 기본`)

**캘린더 / Google 심사**
- 워크스페이스 토큰 암호화 + 해제 시 revoke(개인 연동과 기준 통일, 옛 평문은 첫 사용 시 자동 전환)
- gcal 카드 `(calendar.events scope)` 개발용어 제거 + en 키 신설
- ★ **설정→파일·외부 연동 화면이 7/27부터 React #310 으로 통째로 크래시** 중이었다(심사자가 볼 화면)
- Gmail 동의 요청만 차단(기존 연결 수신·갱신 무영향) — 복원 절차 `docs/GMAIL_OAUTH_RESTORE.md`
- 홈페이지 반려 2건 대응(앱 이름 텍스트 노출·목적 문장·noscript) · 개인정보처리방침 Gmail 조항 정정

**설정 레이아웃**
- 카드 내부 폭(455→878px) · `+ 계정 추가` 헤더 우측 · 920px 폭 제한 철회(설정만 혼자 좁았다)

---

## 남은 일 — 3 트랙

### A. Irene 작업
- **Google 심사 촬영·제출** — 촬영 직전 myaccount.google.com/permissions 에서 PlanQ 권한 삭제 필수
- 우리 도메인 5개 등록(`irenewp.com`·`irenecompany.com`·`wor-pro.com`·`planq.kr`·`k-bizhub.com`)
- DMARC TXT 추가(선택·심사 무관): `_dmarc` / TXT / `v=DMARC1; p=none; rua=mailto:help@irenewp.com`
- 각 별칭의 **한국어 서명 입력** (지금 7개 전부 비어 있음 — 나중에 ko 값으로 그대로 승계)
- 회사 캘린더 재연결(선택) — 워프로랩 gcal 토큰에 캘린더 권한 없음

### B. 설계 게이트 없이 바로 구현 가능 — 26건
★ **구현 검증은 예외 없이 Fable 몫이다** (Irene 2026-08-19: "fable 필요없어서 개발해도 검증을
  fable이 해야지"). 설계가 필요 없다는 뜻일 뿐, 검증을 건너뛴다는 뜻이 아니다.
→ **개발 → 커밋 → 배포 금지 → 다음 세션에서 Fable 검증 후 배포.**
운영 피드백 미처리 38건 중.
1. **#258·#280 팝아웃 핀 → 새 팝업** ← **3번째 반복 신고, 최우선**
2. **#300·#255·#254 주간 업무 진척 그래프** (같은 뿌리 가능성)
3. **#301·#305 검색** 즉시 반응 · 검색 전 최신글 노출
4. #306 새소식 위치 · #287 알림 아이콘 · #289 공지 업로드 위치
5. #304 코드박스 분할 · #284 Info AI 정리 · #257 문서 최신순 기준 · #267 모바일 비디오 첨부 · #232 드래그드롭
6. #263 메일 업무추출 버튼·담당자 · #261 발신자 기준 리스트 · #260 풀버전(※ 이미 있을 수 있음) ·
   #222 새 메일 폼 이탈 · #220 외부 답변 표시 · #213 필터 접기
7. #250·#236 업무 태그 (※ `TaskTag`·`TaskTagLink` 모델 **이미 존재** — 팝아웃 노출만 남았을 수 있음)
8. #230 확인필요 상단 접어두기

### C. 설계 게이트부터 Fable 이 필요 — 12건 + 설계 2건
(B군은 구현 검증만, C군은 **설계·구현·배포 3단 전부** Fable)
| 항목 | 설계 게이트가 필요한 사유 |
|---|---|
| #302·#273 외부컨펌 · #271 확인요청 댓글 | `services/taskTransition.js` — **상태 전이 = 보호 영역** |
| #259 채팅방 링크(비로그인 접근) | **보안 경계 변경** — 공개 라우트 신설 |
| #285·#208 출퇴근·휴가관리 | 신규 시스템 + 스키마 + 근무시간 계산. **설계 게이트부터** |
| #299 노션·슬랙 가져오기 | 신규 시스템 + 외부 연동 + 대량 데이터 |
| #227 Cue 파일 검토 답변 · #303 Cue 검증 | 에이전트 권한 경계 + 비용 / 검증 자체가 Fable 몫 |
| #225 문서 워드·PDF·엑셀 다운로드 | 새 렌더 파이프라인 |
| **서명·표시이름 다국어** | 스키마 + 발신 경로 |
| **암호화 키 이전** | `EMAIL_ENCRYPTION_KEY` 미설정(JWT_SECRET 파생 fallback). **구키 복호화→신키 재암호화 절차 필요.** 지금 키만 바꾸면 기존 메일 비밀번호 복호화 불가 — **최고위험** |

**진행 규칙 (Irene 확정 2026-08-19)**
1. **구현 검증은 무조건 Fable.** Fable 사용량이 소진된 동안은 **어떤 것도 배포하지 않는다.**
   Opus 자체 검증은 개발 중 품질 유지용일 뿐 완료 근거가 아니다.
2. B군: 개발 → **커밋만** → `docs/PENDING_FABLE_VERIFICATION.md` 에 항목별 반증 목록 기록 →
   다음 세션에서 Fable 검증 → 통과분만 배포.
3. C군 중 **신규 시스템**(출퇴근·가져오기·채팅방 링크)은 **개발도 미룬다** — 설계 게이트 없이
   만들면 방향이 틀렸을 때 통째로 버린다.
4. 검증 대기 커밋이 쌓이면 **한 번에 몰아 검증**하지 말고 사이클 단위로 나눠 기록한다 —
   FAIL 이 나면 어느 사이클인지 특정할 수 있어야 한다.

---

## Irene 지시 (유효)
1. **판단·설계·검증은 Fable** — Opus 는 사실조사·구현·실행·보고. (지금은 사용량 소진으로 자체 검증 + 명시)
2. **장부에 남아 있어도 이미 고쳐진 것이 섞여 있다** — 착수 전 재현 확인부터 (#260 이 그 예)
3. 옵션 나열 금지 — 직접 판단해서 진행
