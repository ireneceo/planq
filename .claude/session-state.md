## 현재 작업 상태
**마지막 업데이트:** 2026-08-30
**작업 상태:** 운영 배포 5회 · 커밋 12건

### 진행 중인 작업
- 상세 열림 카나리 마무리 (문서 유효 ID 케이스 추가분 검증 중)

### 완료된 작업 (이번 세션)

**모바일 긴 제목 잘림 (Irene 신고) — 배포됨**
- 메일 상세 제목이 헤더 밖으로 흘러 앞뒤가 잘리고 아래 버튼을 덮던 것.
  원인은 서로 다른 날 쓰인 두 규칙의 충돌 — 제목은 줄바꿈 허용인데 Bar 는 height 56px 고정.
  실측: 제목 112px / 헤더 56px → **+27px 겹침**, 가운데 세 줄만 보였다.
- Bar 를 min-height 로 풀고 제목 2줄 clamp + 탭 펼침. 목록 행 말줄임은 정상 설계로 유지.
- 조사 중 **Q docs 제목이 한 글자씩 세로로 쌓이던 것** 발견 — 액션 8개가 375px 를 다 먹어
  제목 칸이 **0px**(폭 15px/높이 202px). Bar flex-wrap 으로 해소.

**글씨 크기 키우기 — 배포됨. Fable 설계·구현 게이트 2회 통과**
- rem **0곳** · font-size 전부 px 하드코딩 → OS·브라우저 글꼴 설정이 이 앱에만 안 먹었다.
  모바일 렌더 769개 중 ≤11px 26.9% / ≤13px 62.5% / 크기 20종 이상.
- px→rem **4,022곳 / 314파일** + 루트 배율 + 설정 3단(기기별 localStorage).
  검증: 13화면 · 텍스트 11,363개 · **비교 22,726쌍 0오차** · 1.3× 가로스크롤 0.
- 세트: viewport `maximum-scale` 제거 + 폰 입력칸 16px(24화면·모달24개·입력칸124개 전부 ≥16px).
- 가드 **fontpx** 신설(베이스 0 fail-closed, 6형태 각각 반증).
- ★Fable 이 배포 차단 조건으로 입력칸 구멍 2개를 잡았다 — textarea/select 특이도 패배 +
  react-select 인라인 `font: inherit`. 그대로 배포했으면 iOS 자동확대 회귀가 살아났다.

**#383 Q Note 녹음파일 업로드 — 배포됨. 운영 실동작 확인**
- 아이폰 m4a 25분 → **9초에 378 발화** 전사. 과금 원장 정확히 1건, 멱등 확인.
- ★ffmpeg 진단이 절반 틀렸다 — mutagen(순수 파이썬)이 m4a 헤더를 읽는다. ffprobe 불요.
- ★배포 스크립트가 **첫 배포에만** pip install 을 돌려 파이썬 의존성이 운영에 영영 안 깔렸다.
  requirements 해시 비교로 수정, 운영에서 3방향 반증.
- 과금은 Deepgram 실측(metadata.duration), 로컬 측정은 게이트용. 업로드 게이트는 fail-closed.

**"상세가 딱딱 안 열린다" (Irene) — 배포됨. Fable 판정 3커밋**
- 1층: **링크는 만드는데 읽는 곳이 0곳** — `/files?file=`(생산자 5곳)·`?client=`·`?sig=`·
  `/knowledge?doc=`(리다이렉트가 쿼리 버림). 파일 알림·전역검색은 **100%** 안 열렸다.
  공통 훅 `useDetailParam` 으로 정본화.
- 2층: 404·403·429·500·순단이 **전부 같은 침묵**. 업무는 영문 "Loading..." 에 영원히,
  메일은 "메일에서 시작해 보세요" 온보딩(알림으로 온 사용자에겐 거짓말).
  `useDetailResource` + `DetailFallback` 로 "찾을 수 없음/권한 없음/불러오지 못함(다시 시도)".
  이관: 업무·메일·문서·Q info·프로젝트.
- 카나리 `--suite detailopen` 신설 — **침묵이 실패**. 러너로 반증 완료.
- 3층(알 수 없는 상태값 → 조용한 기본값)은 CLAUDE.md "상태값 규약" + memory 로 박제.

### ⚠️ 남은 것 / 정직하게 적는 것

1. **메일 알림 딥링크 근본원인 미확정** — 폴백으로 막아둔 상태. 아이폰에서 또 안 열리면
   폴백 이전 단계(탭 이벤트가 앱에 안 닿는 것)로 좁혀야 한다.
2. **iOS 자동확대 실기기 미확인** — 유발 조건(16px 미만)은 전 화면 0건으로 실측했다.
3. **Q docs 액션 "템플릿으로 저장" 버튼이 화면 밖** — 모바일에서 누를 수 없다. 기존 결함.
4. **메일 자동추출은 전 계정 off(기본값)** — 켜면 업무 후보가 실제로 생성된다. 임의로 안 켰다.
5. **내 검사기가 이번 세션에 10회 이상 거짓말했다** — 헤더 버튼을 눌렀고, 없는 ID 로도 통과했고,
   파라미터 이름을 틀렸고, **탭으로 살아 있는 이전 화면의 폴백을 주웠고**, 배열 인덱스가 밀려
   엉뚱한 케이스에 id 를 넣었고, 빌드 교체 중에 돌려 거짓 실패했다. 판정은 전부
   **data-testid + 가시성**으로 바꿨고, 카나리 전에 **서빙 중인 번들 해시**를 대조한다.

### 다음 할 일

- **#382·#381 Q Sale** — 신규 시스템이라 Fable 설계 게이트
- 남은 운영 피드백 18건 (#379 #378 #361 #360 #358 #355 #354 #353 #343 #273 #259 #235 #233 #230 #227 #211)

### 🔑 Irene 만 할 수 있는 것

| 항목 | 내용 |
|------|------|
| **Android** | Play Console $25 등록 → **앱 서명 키 SHA-256 지문**을 주면 assetlinks 치환 + 워크플로 + release 서명을 한 번에 붙임 |
| **iOS 심사** | App Store Connect → 「배포」 맨 아래 **App 심사 정보**에 데모 계정 입력 (`appreview@planq.kr` / `PlanQReview!2026` — 이미 만들어 둠) · 스크린샷 6.9" · App Privacy 설문 · 카테고리/연령등급은 「App 정보」 |
| **Google Drive** | Drive 전용 GCP 프로젝트 분리 + Restricted `drive` scope 심사 (4~8주) |

> ⚠️ Apple Developer 계정은 **help@wor-pro.com** — 2단계 인증 6자리가 그 메일함으로만 온다.

---

## 복구 가이드

새 Claude 세션 시작 시 아래 내용을 붙여넣으세요:

```
이전 세션 이어서 작업하고 싶어.
/opt/planq/.claude/session-state.md 읽어줘.
```
