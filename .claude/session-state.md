## 현재 작업 상태
**마지막 업데이트:** 2026-08-28
**작업 상태:** 완료 (미배포 커밋 5개 — 다음 세션에서 `/배포` 필요)

### 진행 중인 작업
- 없음

### 완료된 작업 (이번 세션)

**운영 배포 1회 (v1.48.5 · commit `125f06e6`)**
- 3점 검증 통과 — 청크 dev=prod `index-1fnWBTza.js` · https health 200 · PM2 3개 재기동
- **#385 진행률 백필 운영 적용: 업무 37건 · 스냅샷 1172행** (재실행 0건 = 멱등 실증)
- 장부 #385 done, #378 은 열린 채 유지

**메일 2건 (운영 신고)**
- 전달이 **운영에서만 전부 503** — `.env` 가 배포 rsync 보호 대상이라 플래그가 안 따라옴.
  플래그 4개 전수 대조 결과 기본값 꺼짐은 전달 하나뿐. **기본값을 켜짐으로 뒤집고** 킬스위치 유지.
  실HTTP 양방향 반증(플래그 없음 → 400 · `=0` → 503)
- AI 초안이 **받은 답장을 되돌려주던 것** — 역할 명시 + 되풀이 금지 + 대화 흐름(`[US]/[THEM]`).
  같은 스레드 실측: 옛 프롬프트 4/4 실패 → 새 프롬프트 4/4 정상

**#378 마무리 (3건 중 2건)**
- 첨부의 프로젝트 귀속 + 노출 범위 정렬(넓히기 한 방향). 운영 백필 3건 배포 목록 등록
- `persistAttachmentsRef` 가 선언만 되고 대입된 적 없어 "나가면 첨부 저장" 이 죽어 있던 것 살림
- 이미지 **끌어서 크기 조절** 신설 (S/M/L 프리셋은 원래 정상이었음 — 실측으로 확인)

**네비게이션 통일**
- Cue 진입점을 통합검색 옆에 — 기존 Q helper 팝아웃을 그대로 연다
- 알림 ↔ 새 소식 드롭다운 통일 (`dropdownShell.ts`) + 전체보기 페이지 링크 해석 결손 수정
- 열기 = **새 탭** (`tabStore.openInNewTab`) — 하던 일이 사라지지 않게

**검증 (코드 변경 없음)**
- 근무·휴가 실HTTP 24항목 — 잔여 검사는 승인 시점이 올바른 설계. 양성/음성 대조군 통과
- 네이티브 앱: APNs 설정됨 · Team ID 치환 · AASA 정상 · 등록 기기 1대 · 7일 push sent 219
- Drive 역동기화: 감시 채널 열림 · 오늘 변경 4건 도착(전부 `skip/unknown_file` = 정상) ·
  종단 카나리 6/6 (운영에서 "아는 파일 적용" 기록은 아직 0건)
- 하니스가 멀쩡한 `/files` 를 거짓 실패시키던 것 교정 + 빈 화면 반증

### 미배포 커밋 (5개 — 다음 세션 첫 작업)
```
23a20f84 test(e2e): 하니스 거짓 실패 교정
d83743aa feat(nav): Cue 진입점 · 드롭다운 통일 · 새 탭
7cb5dcb4 fix(qmail): 전달 503 — 킬스위치 기본값 켜짐   ← Irene 대기 중
7034fa6b fix(qmail): AI 초안 역할 뒤집힘              ← Irene 대기 중
125f06e6 fix(deploy): #385 백필 배포 목록
```
배포 시 `scripts/backfill-post-file-scope.js --apply` 가 자동 실행됨(운영 3건, 멱등).

### 다음 할 일

1. **`/배포`** — 위 5개 커밋. 배포 후 Irene 확인 요청 2건:
   - 메일 전달: 표·이미지 있는 메일을 아이린님 주소로 전달 → 받은 쪽 원문 보존 확인
     (문제 시 `QMAIL_FORWARD_ENABLED=0` 한 줄로 즉시 재차단)
   - Drive 역동기화: **PlanQ 에서 올린 파일**의 Drive 사본을 이름 변경 → PlanQ 반영 확인
2. **파일 휴지통** (Irene 지적으로 드러남 — 다음 세션 우선). `deleted_at` 데이터는 이미 쌓여 있고
   **화면과 복구 API 만 없다**(복구 라우트 0건 · UI 0건). Q File 필터에 "휴지통" + 복구/영구삭제.
   개인 보관함·프로젝트 파일 양쪽. 권한 = 삭제한 본인 + owner/admin.
   → 이게 된 뒤에야 "Drive 에서 삭제 → PlanQ 휴지통" 정책 변경 가능
3. **#378 남은 1건** — 메일 전달 본문 깨짐. 위 1번 확인 결과에 달림
4. **Irene 만 할 수 있는 것 (병행 착수 권장)**
   - **B2-1: Drive 전용 GCP 프로젝트 분리** — Restricted `drive` scope 심사 4~8주.
     지금 scope 는 `drive.file`(앱이 만든 것만). Drive 에서 직접 만든 파일 인제스트는 심사 후
   - A1 운영 데모 워크스페이스 시드(platform_admin 자격) · A2 App Store Connect 심사 정보
   - A8 Play Console $25 등록

---

## 복구 가이드

새 Claude 세션 시작 시 아래 내용을 붙여넣으세요:

```
이전 세션 이어서 작업하고 싶어.
/opt/planq/.claude/session-state.md 읽어줘.
```
