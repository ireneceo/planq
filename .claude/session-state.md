## 현재 작업 상태
**마지막 업데이트:** 2026-09-22
**작업 상태:** 완료 (v1.56.0 · v1.56.1 운영 배포)

### 진행 중인 작업
- 없음

### 완료된 작업 (이번 세션)
- **서명란·서명본·우리 측 서명·거절 표시** (v1.56.0, `f873304d`) — 운영 배포 완료
- **문서 복사 + Q docs 상단 정리** (v1.56.1, `eacfbda6`) — 운영 배포 완료
- **복사 전면 통일 — 청구서·프로젝트** (v1.57.0, `a89e6af6`) — 운영 배포 완료
  - 청구서: 번호 새 채번 · 발행/결제/증빙 이력 미복사 · draft 시작 · 계좌 재스냅샷 · 금액 재계산
  - 프로젝트: **설정만**(업무·문서·대화·청구서 미복사) · 자동청구 OFF · 초대토큰 재발급 · 플랜 쿼터
  - 글자 «복사» 통일 · 업무 복사에 빠져 있던 감사기록 추가 · qbill 죽은 키 부활
- **검증 중 찾아 고친 결함 5건** — 고객이 서명본 못 봄 / 거절이 «서명 전» / 공개 화면 날짜 ko-KR /
  업무 복사 감사기록 없음 / **i18n 가드가 `{ t: tl, i18n }` 를 못 읽어 정상 코드 13건을 부채로 셈**
- **회귀 카나리 3종** — `signature`(16) · `signflow`(15) · `docsdup`(8, 복사 전체), 전부 실브라우저
- 위키: 서명 3건 + 복사 3건(문서·청구서·프로젝트) — dev·운영 둘 다 반영(아티클 80건)
- CLAUDE.md 박제 2건: 서명본 조립 계약 · 복사 계약

### 다음 할 일
- **Fable 검증** — 서명(R=1: 법적 증빙·공개 무인증 라우트·운영 스키마)과 복사를 전부 자체 검증만 했다.
  한도 429 로 4회 시도 모두 실패. 한도 회복 시 `/fable-검증` 으로 한 번에 올린다.
  판단 대기 항목 `docs/FABLE_GATE_QUEUE.md` (공개 라우트 상한 · 고객이 보는 서명 범위 ·
  로그인=본인확인의 법적 무게 · 다회차 서명의 고정본 선택 · 마이그레이션 잠금시간 ·
  ref_count 가 첨부 링크 수를 안 셈 · 복사본 vlevel 승계 · 표 복사 강등 미고지)
- **복사 전체 통일 설계 — Fable 이 한 번에 본다** (Irene 2026-09-22: "2번은 fable이 체크해서 전체적으로
  통일해서 넣어주고"). 요청서는 `docs/FABLE_GATE_QUEUE.md` 맨 아래 [요청] 항목 —
  엔티티 8종의 «이력» 정의 · 진입점/글자 통일(복사 vs 복제) · 권한 기준 · 죽은 i18n 키.
  ★ **설계가 나오기 전에는 개별 엔티티에 복사를 넣지 않는다**(지금 넣으면 또 갈라진다)
- 운영 첫 서명 요청 발송 때 EmailLog 로 실제 도달 확인 (검증은 example.com 으로만 했다)
- 옛 `Document` 모델의 «이름 입력» 서명 방식 정리 (이번 범위 밖)
- 죽은 i18n 키 정리 — `qbill.json` 의 `detail.header.actions.duplicate` 를 읽는 코드가 0곳
  (위 복사 통일 설계에 포함해 함께 판단)

### 운영 반영 완료 (2026-09-22)
- v1.56.0 · v1.56.1 배포 · `migrate-signature-slot` 실행 · 템플릿 seed · 릴리즈노트 2건 발행
- **Q위키 운영 seed 완료** — 아티클 126건. 실측: `collect-signature` 200(인사이트 발행분이라 public 강제) ·
  `signed-copy-pdf` 401 · `duplicate-document` 401

---

## 복구 가이드

새 Claude 세션 시작 시 아래 내용을 붙여넣으세요:

```
이전 세션 이어서 작업하고 싶어.
/opt/planq/.claude/session-state.md 읽어줘.
```
