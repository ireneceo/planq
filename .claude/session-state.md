## 현재 작업 상태
**마지막 업데이트:** 2026-09-24 12:50 UTC
**작업 상태:** 완료 (운영 배포 2회 — v1.59.0 `6b25946` · v1.60.0 `f1f6875`)
**주체:** [Opus] Opus 5 (1M)

### 진행 중인 작업
- 없음

### 완료된 작업 (이번 세션)

**v1.59.0 — 사진 배치 업로드 절단 (운영 신고, 오전)**
- 업로드 `perMin: 30 → 300` · `perDay: 1500 → 5000`. **1파일=1요청 + 순차 전송**이라 분당 상한이
  곧 «한 번에 올릴 수 있는 파일 수» 였다. 운영 실측 48장 중 **정확히 30장**에서 절단
- 429 를 **실패가 아니라 «재시도 대기»** 로 읽고 자동 재개(MAX_WAITS 12). `xhrSend` 가 rate-limit
  헤더를 넘긴다(여태 Content-Type 만 베껴 대기가 늘 기본 15초였다 — 서버 실제 57초)
- 실패 목록 일괄 조치 [전체 재시도]·[실패 목록 지우기]·[모두 중단] + 머리줄 건수
- «대기 중»(순서)과 «재시도 대기»(상한) 라벨 분리 — 같은 글자라 왜 멈췄는지 알 수 없었다
- **운영 데이터 정리** — K-DINE 메뉴 폴더의 옛 사본 18장 휴지통(IPC 30장 보존, 바이트 공유 0 확인)

**v1.60.0 — Q file 폴더·중복 이름 (오후)**
- 폴더 삭제 시 [폴더만 삭제]/[파일도 함께 삭제]. **기존 공용 모달**(`useFolderEditing`)에 넣었다
- 옛 문구 「“직접 업로드 루트”로 옮겨집니다」는 **거짓**이었다 → 「바로 위 폴더」. 세는 것도 재귀로
- `contents=delete` 소켓 브로드캐스트 · move 분기 `business_id` · **폴더 CUD 감사 신설**(0건이었다)
- 폴더째 업로드(`dropEntries.ts`) · 상한 도달 시 화면이 말한다
- 같은 이름 재업로드 [덮어쓰기]/[이름 바꿔 저장]/[건너뛰기] — 한 번 묻고 배치 전체 적용
- i18n 가드가 **인자로 받은 번역기**를 t() 로 인정 · 베이스라인 i18n 만 조임(276→242)
- 카나리 `dupname`/`folderops` 분리 + 픽스처를 브라우저 밖 Node 로
- **Q위키 2건 추가** — `folder-upload-and-delete` · `same-name-file` (dev 시드 완료, 운영 미반영)

### ★ 이번 세션에서 내가 틀린 것 (반복 금지)
1. **내 수리가 더 나쁜 결함을 두 번 만들었다** — ①다른 프로젝트 파일을 지웠다 ②묻지 않고 지웠다.
   Fable 게이트 **4라운드**(1~3차 FAIL)가 배포 전에 전부 잡았다. memory `feedback_my_fix_created_worse_defect`
2. **중복 확인창을 새로 만들었다** — 두 트리가 공유하는 모달이 이미 있었다. 발견하고 철회
3. **「잔여 0」이 거짓이었다** — 카나리가 실행마다 파일을 남겨 52건 축적. **그것이 내 타임아웃의 원인**
4. **「UI 도달 불가」 오판** — `docs-project-row-<id>` 는 **이동 없이 펼쳐진다.** 내 `clickTreeRow` 가
   프로젝트 이름의 첫 `[title]` 을 집었는데 그건 트리 행이 아니었다
5. **검사기가 열 번 넘게 거짓말했다** — 러너 계약(무엇을 넣어도 ✅) · `DataTransfer` 헤드리스 미동작 ·
   **keep-alive 숨은 사본 4회** · 브라우저 안 fetch 타임아웃 · `querySelectorAll('*')` 메인 스레드 차단 ·
   **양성 대조군 자체가 틀림**(`open` 만 제거). memory `feedback_fixture_inside_browser_kills_harness`
6. **링크 재발급 원인 오진** — 「재사용 로직이 없어서」가 아니라 **「주소를 다시 볼 수 없어서」** 사람이
   새로 만든 것이었다(토큰은 해시만 저장). Fable 이 정정

### 다음 할 일 (우선순위)
1. **카나리 잔여 방지** — RUN 접미어 + `lib/cleanup.js` sweep 에 `zz%` 파일·`ZZ카나리` 폴더 추가
2. **⚪ 미측정을 실측으로** — 「워크스페이스 모드 + 프로젝트 폴더」. 올바른 손잡이
   (`docs-project-row-<id>` + 하위 폴더 `[title]`)로 고치고 **양성 대조군까지**
3. **고객 링크 P0** — 게스트 화면에 발신자(워크스페이스 이름·로고). **착수 지점 확정됨**:
   `services/guest_link.js:70·112`(attributes 3 + 반환에 business) · `routes/guest.js:57·115`
   (`workspace:{name, logo_url}` **두 필드만**) · `PublicPageShell.tsx`(prop **선택**으로 — 공개 6화면 공용) ·
   `GuestProjectPage.tsx:110`(Head `min-height:60px` 고정 — **3폭 실측 필수**) · locales ko/en · 카나리 신규.
   로고는 `/api/businesses/symbol/<uuid>` 가 **이미 무인증 공개**라 새 경로 불필요. `LetterAvatar` 재사용
   (로고 없으면 첫 글자). **절대 안 실을 것**: `business_id`·`slug`·법인 블록·연락처·요금/구독
4. **고객 링크 A·B** (Fable 결정, `docs/GUEST_PROJECT_VIEW_DECISIONS.md`) — 담당자 표시 기본 숨김
   (워크스페이스 이름으로, `businesses.permissions.client_show_assignee`) + 공유 토큰 **HMAC 파생 + 멱등 발급**.
   B 는 R=1 → Fable 게이트
5. **감사 로그 1순위** — **로그인 성공/실패 감사가 시스템 전체에 0건.** Stripe 웹훅 2개 · 멤버 역할 변경
   (두 문 중 한 문만 감사) · 프로젝트 멤버 전면 교체. 되돌릴 수 없는 사건이라 `logAudit`(실패를 삼킴) 대신
   **`writeAudit`**(실패하면 던짐)를 쓸지 판단 필요 → Fable
6. **커버리지 가드 신설** — 기존 `auditentry` 는 «잘못된 입구» 만 막고 «입구가 없는 것» 은 못 본다.
   변경 라우트 549 중 감사 227, **33파일이 감사 0건**
7. **온프렘 0단계** (`docs/ONPREM_DESIGN.md`) — 마이그레이션 원장화 · 감사 구멍 · 기능 플래그 ·
   도메인 하드코딩 제거. 권고는 **「지금은 팔지 않고 조건부 준비」**

### Irene 결정 대기
- **교차 모드 구멍** — 워크스페이스 모드로 프로젝트 폴더에 올린 행은 `project_id=null`·L1 이라
  **프로젝트 탭에 아예 안 나온다.** Fable 권고: 프로젝트 폴더에 올리면 그 프로젝트 파일이 되게.
  **가시성이 L1 → 프로젝트 멤버로 넓어지므로 결정 필요**
- 온프렘 0단계 착수 여부 · 고객 유입 P0~P2 순서

### Git 상태 (2026-09-24 12:50 UTC)
- `main` · 운영 버전 **1.60.0** · 배포 커밋 `f1f6875` · health 200
- Fable 마커 `by=fable` commit `10baf926` (4차 PASS)
- 롤백: `/opt/planq/backups/` 의 배포 시각 디렉터리

### 주요 변경사항
- **DB**: 스키마 무변경 (두 배포 모두)
- **동작 변경**: 업로드 분당 상한 30→300 · 429 는 실패가 아니라 대기 · 폴더 삭제가 묻는다 ·
  폴더째 업로드 · 같은 이름이면 묻는다
- **Fable 게이트**: v1.59.0 PASS(1라운드) · v1.60.0 PASS(**4라운드**)
- **Fable 설계 산출 3건**: `docs/ONPREM_DESIGN.md` · `docs/GUEST_PROJECT_VIEW_DECISIONS.md` ·
  `docs/CLIENT_ENTRY_DESIGN.md`

---

## 복구 가이드

새 Claude 세션 시작 시 아래 내용을 붙여넣으세요:

```
이전 세션 이어서 작업하고 싶어.
/opt/planq/.claude/session-state.md 읽어줘.
```
