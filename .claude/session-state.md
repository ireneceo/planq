## 현재 작업 상태
**마지막 업데이트:** 2026-09-18 UTC
**작업 주체:** [Opus]
**작업 상태:** **배포 완료 — v1.52.11 (커밋 `b0c0cb44`, 04:24)** · 미배포 코드 0 ·
**Fable 24차 PASS** (양성 대조군 2건을 Fable 이 직접 재현) · 운영 백필 2종 여전히 미실행

### 진행 중인 작업
- 없음. 오늘 코드는 **전부 운영에 나갔다**(v1.52.11 · `b0c0cb44`).

### 이번 세션(2026-09-18)에 한 것

| 커밋 | 내용 | 검증 | 배포 |
|---|---|---|---|
| `b0c0cb44` | 개발현황 «내용이 다 비어서 나오던» 것 + 일괄 쓰기 격리 **실호출** 카나리 | **Fable 24차 PASS** | ✅ v1.52.11 (04:24) |

#### ① 개발현황 화면이 내용을 안 보여주던 것 (신고: *"다 비어서 나와"*)
**저장은 처음부터 정상이었다.** `next.json` 이 대부분 섹션을 **문자열 배열**로 적었는데 화면은
`it.title || it.area || … || '—'` 로 **객체 필드**를 읽어 전부 '—' 로 떨어졌다. 건수(3·3·1·3·4)만
맞고 내용이 비는 모양이라 사용자에게는 «저장이 안 됐다» 로 보인다. 발행 검증도 `verified`·`severity`
같은 **선택 필드만** 보고 「제목이 있는가」를 한 번도 안 봤다 — 조용한 기본값 계열.
- `services/devStatusSections.js` **단일 원천**(섹션별 대표 키 표 + 정규화). 문자열은 **정식 입력**이다.
- **읽는 쪽에도 건다** — 그래야 이미 저장된 운영 v1.52.11 행이 **재발행 없이** 치유된다(백필 불필요).
- 발행은 제목 없는 항목에서 **멈춘다**.

#### ② 일괄 쓰기 격리 실호출 카나리 (22차 소견 해소)
정적 문자열 검사는 우회된다 — `resolveBulkTargetIds` 앞 한 줄이면 카나리 초록인데 남의 메시지가 바뀐다.
판정을 응답이 아니라 **DB** 로 옮겼다. `--suite tenantwrite` 7검사.
★ 종전 `--suite tenant` 는 **GET 전용**이라 쓰기 격리가 통째로 계측 밖이었다.

#### 양성 대조군 — Fable 이 직접 재현했다
- 우회 한 줄 → tenantwrite **6실패**, `200 {"updated":0}` 인데 메시지 6행(2087·2098·2104·2109·2378·2394) 실제 변경
- 정규화 제거 + FE 원복 + 재빌드 → devstatus **3실패**, 화면 「1/7 ← '—' 로 떨어졌다」 = 신고 모양 재현
- Fable 자체 우회(응답만 400, 뒤에서 갱신) → **6실패**로 잡힘

#### 이번에 걸린 함정 2개
- `pkill -f "vite build"` 가 **자기 명령줄**을 잡아 빌드가 exit 144 로 죽었다(memory `feedback_watcher_matches_itself`)
- 대조군 빌드가 **SIGTERM(143)으로 죽었는데** 카나리는 옛 번들을 보고 **초록**이었다.
  종료코드를 확인 안 했으면 "화면 대조군 통과"로 오보할 뻔했다.

#### ★ 다음 라운드 1순위 (Fable 24차 비차단 소견)
`account_id` 의 **«같은 워크스페이스 안 남의 사적 계정»** 축이 tenantwrite 의 계측 밖이다.
Fable 이 `acctIds.includes(picked)` 검사를 제거했는데 카나리가 **초록**이었다 — 크로스테넌트는
`business_id` 가 여전히 막아 표본이 안 바뀌기 때문. **dev 에 그 픽스처가 0건**이라 실증 자체가 불가.
→ ①배우 워크스페이스에 타인 소유 사적 계정+스레드 픽스처 ②②검사를 그 축으로 교체.
「검사가 도는데 비교 대상이 없어 늘 초록」 = memory `feedback_guard_key_never_matches` 계열.
상세: `docs/FABLE_GATE_QUEUE.md` 24차 항목.

### Google Play (2026-09-18 확인)
- **Submission 1 = Published (9/15 02:41)** — 앱 콘텐츠·스토어 등록정보 **심사 통과**.
- 그러나 **스토어에는 미등재**: `play.google.com/.../app.planq` **404**(대조군 Gmail 200·카카오톡 200·없는 패키지 404).
  운영 `GET /api/app-download` 의 `android_url` 도 **null**.
- 원인 추정: 통과한 11개 변경에 **앱 버전(릴리스) 항목이 없다.** AAB 는 내부 테스트(version code 28)에만 있다.
- → 다음 한 단계: Play Console **Test and release → Production** 화면 확인(릴리스 유무).

### 이번 세션에 한 것 (2026-09-17, Q mail 라운드)
신고 2건에서 시작해 **같은 계열 결함 5개**로 번졌다. 앞의 둘만 배포됐다.

| 커밋 | 내용 | 검증 | 배포 |
|---|---|---|---|
| `6f7b38d7` | 팔로우가 «확인완료» 로 지워지지 않게 · 목록의 「답변 필요」 두 뜻 분리 | Fable 16차 PASS | ✅ v1.52.10 |
| `d95b86d7` | 담당 폴더도 같은 술어로 · 카나리의 잉여 검사를 실효 검사로 | Fable 17차 PASS | ✅ v1.52.10 |
| `762de179` | v1.52.10 · 릴리즈노트·개발현황 · 카나리 «항상 참» 한 줄 수리 | Fable 18차 PASS | ✅ 배포됨 |
| `119bf573` | 폴더 탭 배지가 «무슨 수인지» 를 말한다(title·aria) | Fable 19차 PASS | ❌ 미배포 |
| `3fb8c777` | 배지 수를 **폰에서도 보이게** — 일괄 버튼에 건수 | Fable 20차 **FAIL** | ❌ 미배포 |
| `e1e1d9f4` | 20차 FAIL 수리 — 「모두 읽음」이 진짜 모두가 되게 | Fable 21차 **FAIL** | ❌ 미배포 |
| `40e973a7` | 개발완료 문서 · CLAUDE.md 「Q mail 폴더·배지 계약」 박제 | — | — |
| `31bbbd5c` | **21차 FAIL 수리 — 크로스테넌트 메시지 쓰기 · 상한에서 이어서** | **Fable 22차 PASS** | ❌ 미배포 |

#### 무엇이 실제로 고쳐졌나
1. **팔로우·담당 목록이 「확인 완료」로 비워지던 것** (신고 원문: *"내가 팔로우 해놨던 메일리스트가 없어졌어."*)
   데이터는 멀쩡했고 폴더 조건이 `archived` 를 걸러 냈다. 운영 실측 — 팔로우 3건이 전부 archived,
   2건은 9/16 16:48 같은 초([모두 확인완료] 한 번). 배포 후 Fable 이 운영 DB 로 확인: **옛 술어 1건 → 새 술어 4건.**
2. **「답변 필요」가 목록에 두 번 보이던 것** — 빨간 뱃지(상태)와 회색 버튼(행위)의 글자가 같았다.
   버튼만 「답변 필요로 표시」로. 상태는 명사, 행위는 동사.
3. **탭 배지의 축이 서로 달랐다** — 「답변 필요 50」·「확인 권장 3051」은 폴더 건수인데 「전체 3475」만 미읽음.
   title·aria + **일괄 버튼에 건수**(폰에서 보이는 단서).
4. **★ 「모두 읽음」이 모두가 아니었다 (원래 결함, 미배포)** — 라우트가 500 에서 조용히 잘라 한 번에 **499건**.
   3475건을 지우려면 7번 눌러야 했다. 사용자에게는 "눌렀는데 안 없어진다" 로 보인다.
5. **확인 문구가 다른 공식을 읽었다** — 라벨 3475 / 확인 3522. 둘 다 `bulkCount` 로.
   그리고 계정 필터가 실행에 안 실려 «적힌 수 ≠ 지우는 대상» 이었다.

#### 만든 검사
- `scripts/e2e/canary-maillabel.js` (신규, **14검사**) — 폴더 술어 · i18n 원천(ko·en) · 실화면 문구 충돌 ·
  배지 축 · **폰(375) 가시 단서** · 「누르기 전·후가 같은 수」
- 이 세션에서 **검사기를 세 번 고쳤다**: ①표본에 뱃지가 안 실려 양성 대조군이 통과 ②`JSON.stringify` 가
  Symbol(`Op.*`) 키를 버려 비교가 **항상 참** ③`uncertain` 폴더 패스가 구조적으로 항상 초록(잉여)
- **함정 박제**: 빌드된 locales 는 `gzip_static` 이라 **평문 `.json` 만 바꾼 대조군은 브라우저에 안 닿는다.**
  `.json` + `.json.gz` 를 같이 바꿔야 한다. 이것 때문에 "검사기가 못 잡는다" 로 오독할 뻔했다.

### 이번 라운드 마지막에 더해진 것
- `31bbbd5c` — 일괄 처리 격리 누수 + 상한 이어서 처리 수리 (Fable 22차 대기)
- CLAUDE.md 「Q mail 폴더·배지 계약」 **⑤항** 추가(일괄 처리는 id 를 거를 때 범위를 건다)
- memory `feedback_bulk_scope_on_ids_not_only_rows`

### 다음 할 일
1. **실호출 카나리 추가** — 위 22차 소견. 정적 검사는 우회된다(실증됨).
2. ~~미배포 4건 배포~~ **완료** — v1.52.11 로 전부 나갔다. 격리 누수는 **운영에서 닫혔다**(코드 기준) — Irene 의 `/배포` 명령 필요.
   ★ `e1e1d9f4` 는 **대량 UPDATE** 가 운영 데이터에 처음 도는 변경이다. 21차 PASS 전에 내보내지 말 것.
3. 담당 폴더는 이미 팔로우와 한 줄을 공유한다 — 더 갈라지지 않게 카나리가 지킨다.

### 사람이 해야 할 것 (막혀 있음 — 종전 그대로)
1. **운영 백필 2종** — 권한 정책(공유 리소스 변경)이 막아 Claude 가 실행 못 함. **순서 중요**
   ```
   ssh irene@87.106.78.146 'cd /opt/planq/backend && node scripts/recall-personal-mirrors.js --apply'
   ssh irene@87.106.78.146 'cd /opt/planq/backend && node scripts/recount-storage-usage.js --apply'
   ```
   - 회수 대상 **38건**(개인 L1 12 · 삭제됨 24 · 프로젝트 없는 L2 2). 안 돌리면 개인 파일이
     Drive 공유 폴더에 그대로 있다.
   - 재계수: biz1 588개/209.3MB → 614개/240.0MB (DB 카운터만)
2. **Drive 원본 2건(id 10·11, 개인 등급)이 공유 폴더 직계에 있다.** 스크립트가 못 지운다 —
   사본이 아니라 파일 자체다. **[파인더 공유] 를 켜기 전에** Drive 에서 옮기거나 등급을 바꿀 것.
3. **개발 전용 구글 계정** — dev 와 운영 워크스페이스 이름이 둘 다 「워프로랩」이라 **같은 Drive 폴더**를
   공유한다(같은 이름 폴더 재사용 규칙). 개발 쪽 이름을 바꾸면 분리된다. #417·#418·#419 가 여기 막혀 있다.

### Git 상태
- 브랜치 `main` · **미커밋 변경 0** (working tree clean)
- HEAD `e1e1d9f4`
- **운영에 나간 마지막 커밋: `762de179` (v1.52.10, 2026-09-17 18:24, 백업 `/opt/planq/backups/20260917_181855`)**
- 미배포: `119bf573` · `3fb8c777` · `e1e1d9f4`

### 이번 세션에서 배운 것 (memory 에 박제됨)
- `feedback_explicit_watch_survives_done` — 사람이 명시적으로 건 표시(팔로우·담당)는 «처리됨» 으로 지워지지 않는다
- `feedback_same_word_state_and_action` — 같은 글자가 상태 뱃지이자 액션 버튼이면 목록은 «두 번 나온다» 로 읽힌다
