## 현재 작업 상태
**마지막 업데이트:** 2026-08-21 (야간 사이클, Opus 5 1M)
**작업 상태:** 완료 — 운영 배포 2회 완료, 장부 정리 완료

### 진행 중인 작업
- 없음

### 완료된 작업 (이번 세션)
- **#254 #300 진척 그래프 기준선** — 기준선이 스냅샷의 옛 예측으로 만들어져 예측을 정정하면 그 주 진척이
  사라지거나 부풀던 결함. 기준선도 지금 예측으로 환산해 `Δ = est_now × (p − p_base)` 로 통일.
  정본 `services/progressBaseline.js` + 소비처 3곳(라이브 그래프·개인 주간보고·업무보고). **Fable PASS**
- **확정 시점 고지** — 기간 도중 확정 시 2일치 이하면 인라인 되물음, 확정본 그래프에 "n/m일치" 캡션 +
  되돌리기 안내. 옛 확정본은 배열이 잘려 있어 기대 일수를 `period` 에서 뽑도록 교정(운영 id111·122 로 실증)
- **#364 맥 파일명 검색** — NFD 로 저장돼 "스크린샷" 검색이 33건을 **0건**으로 내주던 조용한 실패.
  저장(공용 관문+메일첨부)·검색(입구 7곳)·백필 3축. **운영 56건 정리 후 검색 복구 실측**
- **#359 폐지된 레코드 잔재** — 검색에만 남아 같은 항목이 두 번 뜨고 고아 1건은 죽은 링크였다. 전 표면 제거
- **#309 팝아웃 빠른추가 옵션** — 보기 기준(태그/프로젝트)을 입력과 함께 선택. 등장한 것만 선택지
- **배포가 장부를 닫도록 내장** — `deploy-planq.sh` 에 `close_feedback` 단계. 본문 `#숫자` 를 긁으면
  인용한 업무 번호까지 닫아버리는 것을 실데이터로 발견 → **명시 트레일러 `Feedback-Closes:`** 로 교정.
  이미 배포된 옛 커밋의 잔여 번호는 `scripts/feedback-close-backlog.txt` 로 관리
- **라벨 통일** — 진척→**진척 (예상시간)** / 투입→**실제 업무시간**(툴팁 7곳 포함). 보고서와 같은 말로
- **여유 행 삭제** — 이미 쓴 시간을 안 빼 "20h 일하고도 여유 11h" 가 되던 값. Irene 지시로 제거
- **장부 정리** — 운영 도달을 번들·API 로 실측한 뒤 56건 마감(답글 38건 신규 작성). **pending 86 → 30**
- 병렬 세션이 남긴 A군 미커밋 54파일을 커밋해 추적 가능하게 만든 뒤 함께 배포

### 다음 할 일
1. **Fable 게이트 대기 — 주간 가용시간 바 계산**: 분자(진척)는 선택 기간을 따라 커지는데 분모(가용)는
   주 단위 고정이라 `105.3 / 24.0 = 439%` 가 나온다. 백엔드에 기간 환산 정본
   (`services/memberCapacity.periodHours` = 주간 × 기간일수/7)이 있는데 이 화면이 안 쓴다.
   **계산은 Irene 지시로 손대지 않았다** — Fable 판정 후 착수
2. **Irene 조치 대기**: #356 운영 `EMAIL_ENCRYPTION_KEY`(키 교체 전 기존 암호문 재암호화 선행) ·
   #362 nginx 한 줄(root 권한, `docs/OG_PREVIEW_NGINX.md`)
3. **Fable 설계 게이트**: #302 #273 외부컨펌 상태전이 매트릭스 · #349 반복 회차 miss_policy(스키마) · #235
4. **B군 설계 선행 23건**: 출퇴근·휴가(#208 #285, 설계 완료·승인 대기) · 게스트 링크(#259, 미결 6건) ·
   Cue 파일 읽기(#227) · 업무 결과물 버전관리(#271 #307) · 메일 정책(#344 #345 #261) · 워드/엑셀 내보내기(#225)
5. **#364 잔여**: 타이핑 중 자모 분리는 파일명과 **원인이 다르다**(호환 자모 = IME 미조합). 368건 중 2건
   (#299 본문이 증거)이라 재현 조건 미확보. 재발 시 화면·기기 정보 필요

### 이번 세션에서 박제한 함정 (memory)
- `feedback_measure_with_same_lens_as_code` — raw SQL 11행 vs Sequelize defaultScope 5행. 다른 도구로 센
  숫자로 진단하면 없는 병을 고친다
- `feedback_guard_scripts_hardcode_root` — 가드가 `ROOT='/opt/planq'` 하드코딩. 워크트리에서 돌린
  "25/25 통과" 는 내 코드와 무관. **머지 후 본 체크아웃에서 판정**
- `feedback_fixed_but_unreachable` 보강 — "소스에 있다" 는 도달 증명이 아니다. 운영 번들에서 직접 찾아라.
  초안은 절반만 로컬(메일=서버 EmailDraft / 채팅·댓글=로컬)
- `feedback_legacy_data_sample_verify` 보강 — 옛 데이터는 값이 아니라 **모양**이 다르다(잘린 배열 vs null 채움)

---

## 복구 가이드

새 Claude 세션 시작 시 아래 내용을 붙여넣으세요:

```
이전 세션 이어서 작업하고 싶어.
/opt/planq/.claude/session-state.md 읽어줘.
```
