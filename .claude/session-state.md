# PlanQ 세션 상태

## 현재 작업 상태
**마지막 업데이트:** 2026-08-28 (KST) 06:20 — **운영 배포 2회 완료.** 커밋 5개 전부 운영 반영됨.
**운영 커밋:** `57dfd16a` · **버전 1.48.5** · 운영 피드백 pending **22건** (33 → 24 → 22)

---

## 오늘 완료 (전부 운영 배포됨)

### Fable 인증·구독·초대 감사 → 결함 3건 발견·수정 (전부 Fable PASS)
| | 내용 |
|---|---|
| **F1** `81780888` | 초대 수락이 **자기 자신을 인원으로 세어** 마지막 자리가 잠김. 한도 5면 실질 4명. 발행은 통과·수락만 422 라 "자리 남았는데 왜" 로 보였다 |
| **F2** `8fd2dc7a` | 만료(30일) 초대가 자리를 영구 점유. 수락은 410 인데 카운트엔 잡혔다. 규칙을 `services/inviteExpiry.js` 단일 원천으로 |
| **F2b** `0e50f958` | ★ F2 의 판별자가 틀렸다 — `user_id` 가 아니라 **`joined_at`**. 초대 발행은 **이미 가입한 사용자**면 user_id 를 즉시 채우므로, 그 클래스가 안 고쳐진 채였다. (감사에서 처음 본 고아행 320/323 이 정확히 그 모양) |
| **F3** `8fd2dc7a` | 워크스페이스 0개면 `WorkspaceSwitcher` 가 `return null` → '새 워크스페이스 만들기' 로 가는 **유일한 문**이 닫혀 자력 복구 불가 |

### Irene 구두 신고 3건 (`8fd2dc7a` + `0e50f958`)
- **탭 이름이 전부 메뉴명** — `setTabTitle` 은 있는데 **호출부가 0곳**이었다. `hooks/useTabTitle` 신설 + 5화면 연결(docs/talk/note/mail/project). ★임베드된 PostsPage 가 프로젝트명을 지우던 것도 `enabled` 게이트로 봉합
- **Talk 뱃지 안 뜸** — keep-alive 탭이라 뒤 탭도 소켓을 듣는데 판정이 `document.visibilityState`(=브라우저 창 기준). `useReallyVisible()` 로 교체
- **모바일 업무추가** — 키보드가 덮은 홈인디케이터 자리를 푸터·본문이 **각자** 34px 씩 비워두던 유령 여백 68px 제거 + `--vv-top`(iOS visual viewport 밀림) 보정 + 폰 autoFocus 해제

### 운영 신고
- **#378** `780491ac` — 인라인 이미지가 `project_id` 없이 저장돼 '프로젝트 > 파일' 에 영영 안 잡힘 + **툴바 이미지 버튼만** businessId 를 안 넘겨 DB 행이 아예 안 남던 것. **부분 해결이라 열어둠**(드래그 크기조정·첨부 귀속·메일 전달 깨짐 남음)
- **#346 #289** `57dfd16a` — **배포 스크립트에 릴리즈 노트 단계가 아예 없었다.** `publish_release_note()` 추가 → 이번 배포에서 v1.48.5 실제 발행 확인. 둘 다 닫음
- **#385** — 답글만. **그래프 동결이라 대기 중** (아래 §대기)

---

## 🔴 Irene 결정 대기 2건

### 1) #385 진척 그래프 — 원인이 두 겹
- **① 입력 부재(더 큼):** 실제시간 자동 근거는 **포커스 타이머뿐**. 완료 145건 중 86건이 0, 그중 64건은 세션 자체가 없음. 실제시간 든 업무는 전체 67건뿐
- **② 공식:** `Σ(실제시간 × 진행률)` — 이번 주 기록 7.7h 중 그래프엔 1.2h
- 선택지 (가)①먼저 (나)②먼저 (다)둘다 (라)그대로 → 답글에 적어둠. **동결 약속대로 손대지 않음**

### 2) 고객 베타 — platform_admin 자격증명 필요
- **운영에 심사용 데모 워크스페이스 없음.** Beta App Review 통과에 필수.
  `node scripts/seed-appstore-demo.js --base=https://planq.kr --email=appreview@planq.kr --password='...' --admin-email=... --admin-password=...`
  ⚠️ `--admin-*` 필수 (없으면 체험 만료 후 심사관이 잠긴 화면 — 네이티브는 3.1.1 로 구매 표면이 없어 막다른 길)
- `codemagic.yaml` `submit_to_testflight: false` — 외부 테스터로 넓히려면 App Store Connect 에
  **Beta App Review 정보(피드백 이메일·담당자 이름·전화)** 부터 채워야 한다(안 채우면 업로드 다 끝난 뒤 그 단계만 실패)
- `triggering:` 없음 → push 로 빌드 안 돈다. Codemagic 에서 `ios-testflight` 직접 실행
- 운영 `platform_settings.app_ios_url` 비어 있음 → `/app` 다운로드 페이지가 링크를 못 준다
- 앱 자체는 준비됨: 네이티브 릴리스 가드 **18/18**(경고 1 = Android 지문, iOS 무관) · ios-beta-check 통과

---

## 다음 할 일 (순서 미정 — Irene 지시 대기)
1. #378 나머지 3건 (드래그 이미지 크기조정 · 첨부의 프로젝트 귀속 · 메일 전달 시 본문 깨짐)
2. 랜딩페이지 업데이트 내역 노출 (#346 의 남은 절반 — 지금은 앱 안 새 소식에만)
3. 운영 인라인 이미지 10건 `project_id` 백필 (쓰기측은 고쳤음)
4. 남은 운영 신고 20건 — 대부분 Fable 설계 필요 신규 시스템 (#211 #227 #230 #233 #235 #259
   #343 #353~#355 #358 #360 #361 #379 #381 #382 #383)

---

## 이번 세션 교훈 (박제됨)
- **[[feedback_actual_hours_focus_only]]** — 실제시간 자동 근거는 포커스 타이머뿐. 그래프가 빈 원인을
  **공식으로만 진단하면 반쪽**이다. 실제로 반쪽 답글을 냈다가 정정했다
- **[[feedback_feedback_closes_trailer]]** — 장부는 `Feedback-Closes:` 트레일러만 인정. 본문 `#숫자` 는
  의도적으로 안 긁는다. **답글 없는 건은 자동으로 건너뛴다**(이 가드가 실제로 #346 을 막았다)
- **빌드 exit 143 = SIGTERM = 완주 아님.** 산출물 mtime 이 옛날이면 그 상태로 배포하면 반쪽이 나간다
  (한 번 걸렸고, 재빌드 후 배포함)
- **Fable 이 내 수정에서 실결함 2건을 잡았다** — F2b(판별자 오류)와 탭 제목 이중 호출. 둘 다
  "내가 의심한 지점" 이었고 실제로 결함이었다. 의심되면 반드시 태울 것

## 복구 가이드
```
이전 세션 이어서 작업하고 싶어.
/opt/planq/.claude/session-state.md 읽어줘.
```
