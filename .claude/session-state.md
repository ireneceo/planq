## 현재 작업 상태
**마지막 업데이트:** 2026-09-01 07:30 UTC (Opus 5, 1M)
**작업 상태:** 완료 — 운영 배포 1회 · 커밋 1건

### 진행 중인 작업
- 없음

### 완료된 작업 (이번 세션)

**프로젝트 > 문서 편집이 임시저장 직후 닫히던 것 (Irene 직접 신고, 커밋 `264fae83`)**
- 증상: 기존 문서를 편집하면 `✓ 임시저장됨` 이 뜬 직후 편집창이 저절로 닫혀 보기 모드로 돌아감
- 원인 사슬 (실브라우저로 확정):
  1. published 문서 자동저장 PUT → 서버가 `post:updated` 방송 (`routes/posts.js:903`)
  2. **같은 화면의** `QProjectDetailPage` 가 그것을 듣고 자기 자신을 통째로 재조회 (`:477`)
  3. `load()` 첫 줄 `setLoading(true)` → `if (loading) return <로드 중…>` (`:626`) 로 페이지 전체 교체
  4. 안에 얹혀 있던 `PostsPage` **언마운트** → 편집 상태·본문 소멸 → 재마운트 시 보기 모드
- **신규 초안은 방송을 안 해서 재현되지 않았다** — "기존 문서 편집할 때만" 이던 이유.
  dev 프로젝트 57 의 유일한 문서가 draft 라 초기 재현이 세 번 실패했다(전제 오류).
- 수정 (`dev-frontend/src/pages/QProject/QProjectDetailPage.tsx` 1파일, +30/-14):
  · 배경 갱신(소켓·visibility 복귀)은 `load({ silent: true })` — 화면을 바꾸지 않는다
  · 로딩 화면은 `loading && !project` 일 때만 (이미 그려진 페이지를 스피너로 덮지 않는다)
  · 배경 갱신 실패는 `setProject(null)` 하지 않는다 — 순단·429·토큰회전 한 번에
    "찾을 수 없습니다" 로 바뀌며 쓰던 글까지 사라지던 두 번째 경로도 같이 닫음
  · `post:*` 리스너 제거 — 이 페이지는 문서 데이터를 한 줄도 렌더하지 않는데
    문서 저장마다 프로젝트·대화·업무·이슈·메모·멤버·고객을 전부 다시 불렀다
- 실측(실브라우저 60초 편집 · 자동저장 10회):
  · 편집창 닫힘 **240 표본 중 231회 → 0회**
  · 글 쓰는 중 API **130건/분 → 30건/분**
- **반증 완료**: 옛 코드로 되돌려 빌드 → 같은 검사기가 ❌(231회) · 수정본 ✅(0회).
  전제(문서 `published`)도 재현 직전에 DB 로 확인 — 거짓 통과 아님
- 회귀: 실시간 반영 0.50초 유지 · 없는 프로젝트 사유 표시 · 가드 28/28 · health-check 37/37
- 형제 화면 3곳(HistoryTab · ProjectCanvas · IntegratedReportView)은 이미 silent 패턴 — 이 페이지만 예외였다

**운영 배포 (`264fae83`, 07:17)**
- 같이 나간 것: `bf910fb6` 알림·새소식·Cue (제목 굵기 · 두 목록 도구 통일 · 더 보기 · 입력창 문구)
- 릴리즈 노트 **v1.48.5** 발행 (새 소식 노출)
- 착지 검증 5점: health 200 · `.last-deployed-commit`=264fae83 · PM2 uptime 07:17:50 ·
  청크 시각 09-01 07:17 · **서빙 청크 `QProjectDetailPage-CGzDltwN.js` 에 `post:deleted` 0건 / `silent` 1건**
  (옛 청크 대조군: `post:deleted` 1건 / `silent` 0건)
- 장부: 대상 7건(#213 #220 #222 #232 #257 #260 #287) 전부 이미 done — 새로 닫힌 건 없음

### 다음 할 일
1. **#402 (오늘 신규)** — 답변필요 메일에 답장하면 확인권장으로 옮겨 앉는다.
   원인 확정됨: `routes/email_threads.js:1032` 가 답장 시 `reply_needed:false` + `uncertain→open` 으로
   두는데, `services/mailFolders.js:34` 확인권장 정의가 그 상태를 그대로 포함한다.
   `mark-handled`(:651)은 `status:'archived'` 로 내려 양쪽에서 사라진다 — **답장이 같은 착지점을 안 쓴다.**
   방향: 답장 = 처리 완료(archived) → 전체·보낸메일에만 남김. 새 inbound 오면 기존 경로가 재점등.
2. 운영 미처리 피드백 14건 — 긴급 #353(AI 업무추가 배관) · #360(로그성 기록 담을 곳),
   설계 검토 요청 #211 · #233 · #259(고객 무로그인 채팅 링크, "아주 중대") · #361
3. **모든 파일을 드라이브로** (Irene 승인된 방향, 미착수)
4. **SMTP DKIM/DMARC 미설정** (Irene 콘솔 작업) — planq.kr DNS 레코드 없음 → 스팸 격리 위험
5. 애플·구글 승인 대기 — 승인 시 링크를 관리자 설정에 붙이면 재배포 없이 버튼이 켜짐

### 주요 변경사항
- 수정 파일: `dev-frontend/src/pages/QProject/QProjectDetailPage.tsx` (유일)
- Git: 커밋 `264fae83` (직전 `bf910fb6`), working tree clean
- DB 스키마 변경 없음 · 백엔드 코드 변경 없음
- 이번 세션 dev 테스트 문서(#422)는 삭제 완료

### 이번 세션의 교훈
- **재현이 안 될 때는 내 전제부터 의심한다.** 세 번 실패한 이유는 코드가 아니라
  내가 고른 문서가 draft 라 방송 조건이 성립하지 않아서였다. "재현 안 됨 = 버그 없음" 이 아니다.
- **부모가 자식을 언마운트하면 자식의 상태는 사라진다.** 편집창이 닫히는 원인이
  편집 컴포넌트 안에 없을 수 있다 — 닫는 주체를 코드로 **전수 열거**하고 하나씩 지웠다.
- **배경 갱신은 화면을 바꾸면 안 된다.** 스피너로 덮는 것도, 실패했다고 지우는 것도
  사용자에겐 "하던 일이 날아간 것" 이다.
- `pkill -f <패턴>` 은 자기 셸을 죽인다(또 당했다) · 청크 이름을 옛 index 번들에서 주우면
  08-25 짜리를 보고 "미반영" 이라 오판한다 — **현재 index.html 기준으로만** 잰다.

---

## 복구 가이드

새 Claude 세션 시작 시 아래 내용을 붙여넣으세요:

```
이전 세션 이어서 작업하고 싶어.
/opt/planq/.claude/session-state.md 읽어줘.
```
